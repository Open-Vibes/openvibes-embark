// GENERATED by scripts/sync-docs.ts — do not edit by hand.
// Source: https://github.com/blpsoares/agentistics (README.md + docs/*.md)

export interface DocHeading { id: string; text: string; level: number }
export interface DocPage {
  id: string; file: string; title: string; group: string;
  html: string; headings: DocHeading[]
}

export const DOC_ORDER: string[] = ["overview","cli","metrics","data-sources","harness-contract","architecture","deploy","github-actions","security","exposure","mcp","nay","opentelemetry"]

export const DOCS: Record<string, DocPage> = {
  "overview": {
    "id": "overview",
    "file": "README.md",
    "title": "Overview",
    "group": "Start here",
    "html": "<p align=\"center\">\n  <img src=\"https://github.com/blpsoares/agentistics/raw/main/packages/web/public/logo.png\" alt=\"agentistics\" width=\"180\" />\n</p><h1 align=\"center\">agentistics</h1><p align=\"center\">\n  <strong>Track · Analyze · Improve</strong><br/>\n  Local-first analytics for AI coding assistants — one machine, or a whole team\n</p><p align=\"center\">\n  <a href=\"https://github.com/blpsoares/agentistics/releases/latest\">\n    <img src=\"https://img.shields.io/github/v/release/blpsoares/agentistics?label=release&color=f97316\" alt=\"Latest release\" />\n  </a>\n  <a href=\"https://github.com/blpsoares/agentistics/actions/workflows/release.yml\">\n    <img src=\"https://img.shields.io/github/actions/workflow/status/blpsoares/agentistics/release.yml?label=build\" alt=\"Build status\" />\n  </a>\n  <a href=\"https://github.com/blpsoares/agentistics/blob/main/LICENSE\">\n    <img src=\"https://img.shields.io/github/license/blpsoares/agentistics?color=green\" alt=\"MIT License\" />\n  </a>\n  <img src=\"https://img.shields.io/badge/platform-Linux%20%7C%20Windows-lightgrey\" alt=\"Platform: Linux | Windows\" />\n  <img src=\"https://img.shields.io/badge/Bun-runtime-f9f1e1?logo=bun\" alt=\"Bun\" />\n</p><p align=\"center\">\n  <a href=\"#install\"><strong>Install →</strong></a>\n  &nbsp;·&nbsp;\n  <a href=\"#team-mode\"><strong>Team Mode →</strong></a>\n  &nbsp;·&nbsp;\n  <a href=\"#security\"><strong>Security model →</strong></a>\n  &nbsp;·&nbsp;\n  <a href=\"#cli\"><strong>CLI →</strong></a>\n</p><p align=\"center\">\n  <img src=\"/release/gifs/machine-dashboard.gif\" alt=\"The agentistics dashboard\" width=\"100%\" />\n</p><blockquote>\n<p>Every screenshot and recording in this README is produced from a <strong>synthetic demo fleet</strong>\n(<code>packages/server/scripts/seed-demo.ts</code>) — real numbers, fictional names. See\n<a href=\"#reproducing-the-media\">Reproducing the media</a>.</p>\n</blockquote>\n<hr>\n<h2 id=\"what-is-agentistics\">What is agentistics?</h2>\n<p>agentistics reads what your AI coding assistants already write to disk and turns it into\ntokens, costs, sessions and activity you can actually reason about. Nothing is uploaded: on a\nsingle machine it never opens an outbound connection at all, and in Team Mode a machine sends\n<strong>computed metrics only</strong> — never chat.</p>\n<p>It tracks <strong>six harnesses</strong> side by side:</p>\n<table>\n<thead>\n<tr>\n<th>Harness</th>\n<th>Read from</th>\n<th>Tokens &amp; cost</th>\n<th>Agent metrics</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Claude Code</strong></td>\n<td><code>~/.claude/</code></td>\n<td>✅</td>\n<td>✅</td>\n<td>The deepest source — agent invocations, git line counts, workflow runs</td>\n</tr>\n<tr>\n<td><strong>Codex CLI</strong></td>\n<td><code>~/.codex/sessions/</code></td>\n<td>✅</td>\n<td>—</td>\n<td></td>\n</tr>\n<tr>\n<td><strong>Gemini CLI</strong></td>\n<td><code>~/.gemini/tmp/</code></td>\n<td>—</td>\n<td>—</td>\n<td>Local files carry no token data</td>\n</tr>\n<tr>\n<td><strong>GitHub Copilot CLI</strong></td>\n<td><code>~/.copilot/session-state/</code></td>\n<td>✅</td>\n<td>—</td>\n<td>MCP tool calls counted</td>\n</tr>\n<tr>\n<td><strong>Antigravity (<code>agy</code>)</strong></td>\n<td><code>~/.gemini/antigravity-cli/</code></td>\n<td>✅</td>\n<td>—</td>\n<td>Decoded from its own protobuf blobs; edit deltas for lines changed</td>\n</tr>\n<tr>\n<td><strong>Kimi Code</strong></td>\n<td><code>~/.kimi-code/sessions/</code></td>\n<td>✅</td>\n<td>—</td>\n<td>Routes to other providers; priced by the routed model</td>\n</tr>\n</tbody></table>\n<p>A metric a harness genuinely cannot produce renders as <strong>N/A</strong>, never as a confident <code>0</code> —\n<code>HARNESS_CAPABILITIES</code> in <code>@agentistics/core</code> is the single source of truth for which is which.</p>\n<hr>\n<h2 id=\"install\">Install</h2>\n<p><a name=\"install\"></a></p>\n<h3 id=\"linux-wsl-one-line\">Linux / WSL — one line</h3>\n<pre><code class=\"language-bash\">curl -fsSL https://agentop.openvibes.tech/cli | bash\n</code></pre>\n<p>Then just run it:</p>\n<pre><code class=\"language-bash\">agentop\n</code></pre>\n<p>Bare <code>agentop</code> on a terminal opens the <strong>control center</strong> — one full-screen application in the\nterminal&#39;s alternate buffer, so it adds nothing to your scrollback.</p>\n<p align=\"center\">\n  <img src=\"/release/gifs/control-center.gif\" alt=\"The agentop control center\" width=\"100%\" />\n</p><p>Its tabs are <strong>Services</strong> (start / stop / restart this machine, a central, or the Docker machine;\nconnect to or leave a central; enable a boot service), <strong>Setup</strong>, <strong>Logs</strong>, <strong>Commands</strong>, <strong>Help</strong>\nand <strong>Contribute</strong>. Screens change with <code>←</code>/<code>→</code>, panes with <code>tab</code>, and the footer always names the\nkeys that work <em>in the current focus</em>.</p>\n<p>Open the dashboard at <strong><a href=\"http://localhost:47292\" target=\"_blank\" rel=\"noopener\">http://localhost:47292</a></strong> (the API + MCP endpoint stays on <strong>47291</strong>).</p>\n<h3 id=\"windows\">Windows</h3>\n<p>Download the latest <code>.msi</code> or <code>.exe</code> from the <a href=\"https://github.com/blpsoares/agentistics/releases/latest\" target=\"_blank\" rel=\"noopener\">Releases page</a>.\nOn first launch agentistics detects your Claude Code data path automatically (Windows native or WSL).</p>\n<blockquote>\n<p><strong>SmartScreen warning?</strong> &quot;More info → Run anyway&quot;. The binary is not code-signed yet.</p>\n</blockquote>\n<h3 id=\"from-source\">From source</h3>\n<pre><code class=\"language-bash\">git clone https://github.com/blpsoares/agentistics.git\ncd agentistics &amp;&amp; bun install\nbun run dev            # API (47291) + UI dev server (47292)\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Script</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>bun run dev</code></td>\n<td>API + Vite dev server in parallel</td>\n</tr>\n<tr>\n<td><code>bun run watch</code></td>\n<td>OTel daemon only</td>\n</tr>\n<tr>\n<td><code>bun run watch:cli</code></td>\n<td>Terminal TUI</td>\n</tr>\n<tr>\n<td><code>bun test</code></td>\n<td>Unit tests</td>\n</tr>\n<tr>\n<td><code>bun run build:binary</code></td>\n<td>Full build → <code>release/agentop</code></td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"the-terminal-dashboard\">The terminal dashboard</h2>\n<p><code>agentop tui</code> is a live dashboard that needs no browser — Overview, Projects, Sessions, Costs and\nHarnesses, switched with the digits or <code>tab</code>.</p>\n<p align=\"center\">\n  <img src=\"/release/gifs/tui.gif\" alt=\"agentop tui\" width=\"100%\" />\n</p><hr>\n<h2 id=\"the-dashboard\">The dashboard</h2>\n<h3 id=\"pages\">Pages</h3>\n<table>\n<thead>\n<tr>\n<th>Page</th>\n<th>Route</th>\n<th>What it answers</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Home</td>\n<td><code>/</code></td>\n<td>KPIs, activity over time, heatmap, hourly usage, highlights</td>\n</tr>\n<tr>\n<td>Costs</td>\n<td><code>/costs</code></td>\n<td>Spend by model and date, budget and forecast, cache efficiency</td>\n</tr>\n<tr>\n<td>Projects</td>\n<td><code>/projects</code></td>\n<td>Per-project breakdown and comparison</td>\n</tr>\n<tr>\n<td>Repositories</td>\n<td><code>/repositories</code></td>\n<td>Grouped by <strong>git remote</strong>, so one repo unifies across people, paths and machines</td>\n</tr>\n<tr>\n<td>Actions</td>\n<td><code>/repositories/actions</code></td>\n<td>Sessions produced by CI runners</td>\n</tr>\n<tr>\n<td>Tags</td>\n<td><code>/tags</code></td>\n<td>Saved, named groupings — &quot;what did Client X cost this month&quot;</td>\n</tr>\n<tr>\n<td>Tools</td>\n<td><code>/tools</code></td>\n<td>Tool-call ranking and token attribution</td>\n</tr>\n<tr>\n<td>Compare</td>\n<td><code>/compare</code></td>\n<td>Every harness side by side</td>\n</tr>\n<tr>\n<td>Custom</td>\n<td><code>/custom</code></td>\n<td>Drag-and-drop layout builder</td>\n</tr>\n</tbody></table>\n<h3 id=\"comparing-harnesses\">Comparing harnesses</h3>\n<p align=\"center\">\n  <img src=\"/release/gifs/machine-compare.gif\" alt=\"The compare page\" width=\"100%\" />\n</p><h3 id=\"repositories\">Repositories</h3>\n<p>Metrics group by <strong>normalized git remote</strong> (<code>host/org/repo</code>), independent of where the repo is\nchecked out or which machine produced the work. A repo&#39;s detail page carries Overview, Members,\nSessions, an <strong>Actions</strong> tab when it has CI runs, and a <strong>Dynamic Workflows</strong> tab that renders each\nmulti-agent Workflow run as a phase-by-phase timeline.</p>\n<p align=\"center\">\n  <img src=\"/release/gifs/machine-repositories.gif\" alt=\"Repositories and repo detail\" width=\"100%\" />\n</p><h3 id=\"tags\">Tags</h3>\n<p>A tag is a saved grouping of sources — repositories, projects, machines, teams or accounts —\noptionally pinned to a date window. Tag responses are <strong>aggregate-only</strong>: counts and sums, never\nsession rows or transcripts, with any key the viewer cannot see collapsed into an &quot;other&quot; bucket.</p>\n<p align=\"center\">\n  <img src=\"/release/gifs/machine-tags.gif\" alt=\"Tags\" width=\"100%\" />\n</p><h3 id=\"custom-layouts\">Custom layouts</h3>\n<p align=\"center\">\n  <img src=\"/release/gifs/machine-custom.gif\" alt=\"The custom layout builder\" width=\"100%\" />\n</p><h3 id=\"on-a-phone\">On a phone</h3>\n<p>The whole dashboard is responsive and installs as a PWA.</p>\n<p align=\"center\">\n  <img src=\"/release/gifs/machine-mobile.gif\" alt=\"The dashboard on mobile\" width=\"380\" />\n</p><hr>\n<h2 id=\"team-mode\">Team Mode</h2>\n<p><a name=\"team-mode\"></a></p>\n<p>One machine runs as a <strong>central</strong> and aggregates usage from many <strong>members</strong>. Members push\n<strong>computed metrics only</strong> — raw chat is never stored centrally (it is fetched on demand over a\nreverse WebSocket, from the machine that holds it).</p>\n<p>Every machine has a role: <strong>solo</strong> (local only, the default), <strong>central</strong> (the aggregator, a Docker\nservice on port <strong>48080</strong>), or <strong>member</strong>. A machine may be a member of <strong>several centrals at\nonce</strong>, with different sharing rules for each.</p>\n<pre><code class=\"language-bash\"># host a central\nagentop central init &amp;&amp; agentop central up\n\n# join one\nagentop member connect --endpoint http://&lt;central-host&gt;:48080 --token &lt;token&gt;\n</code></pre>\n<h3 id=\"accounts-teams-and-roles\">Accounts, teams and roles</h3>\n<p>A central is not a shared password. First boot prints a one-time <strong>setup token</strong> and asks you to\ncreate the <strong>owner</strong> account; everyone else is invited. Accounts carry argon2id passwords, optional\n<strong>TOTP two-factor</strong>, and a role. A <strong>team</strong> is a scope key, not a label — being in a team <em>is</em>\nseeing it.</p>\n<p align=\"center\">\n  <img src=\"/release/gifs/central-login.gif\" alt=\"Signing in to a central\" width=\"100%\" />\n</p><p><strong>Step-up authentication</strong> gates escalation, not paperwork: editing an account, deleting a team and\nchanging a password ask for a second proof. Enrolling a machine, a token or a repository does not —\na prompt people meet daily is a prompt they clear without reading.</p>\n<p align=\"center\">\n  <img src=\"/release/gifs/central-dashboard.gif\" alt=\"The central dashboard\" width=\"100%\" />\n</p><h3 id=\"what-a-machine-shares-and-what-it-withholds\">What a machine shares — and what it withholds</h3>\n<p>Each connection carries its own rules across two dimensions (repositories and projects) in one of\ntwo modes: <strong>denylist</strong> (share everything except these) or <strong>allowlist</strong> (share only these). The\nrules never travel: the central is told nothing about them beyond a per-dimension <em>count</em>.</p>\n<p>When several of your machines share one account, they tell <strong>each other</strong> what they withhold over a\n<strong>sealed machine-to-machine channel</strong> (X25519 → HKDF → AES-256-GCM, sender and recipient bound into\nthe authenticated header, keys pinned on first sight and every new pin announced). That is what\nmakes two things possible:</p>\n<ul>\n<li>A <strong>warning at the point of decision</strong>: before you start sharing a repository, the picker names\nthe sibling machines that withhold it. It warns, never blocks — and it always says that an absent\nwarning is not proof, because a machine knows only what its siblings announced.</li>\n<li>A <strong>proposal</strong>: a sibling can offer its rules, and applying one may only ever <strong>narrow</strong> what this\nmachine shares. Never replace, never widen.</li>\n</ul>\n<h3 id=\"presence-self-healing-and-ci\">Presence, self-healing and CI</h3>\n<ul>\n<li><strong>Presence</strong> is WebSocket-authoritative — online while the socket is live, offline within ~8s.</li>\n<li><strong>Auto-reconciliation</strong>: if the central&#39;s database is wiped, a token is rotated or an endpoint\nchanges, a member notices and re-pushes its full history. A revoked machine resets itself to solo.</li>\n<li><strong>GitHub Actions</strong>: an ephemeral runner pushes its metrics with <code>agentop ci-push</code>, authenticated\nby <strong>keyless GitHub OIDC</strong> against a registered-repos allowlist. The central stamps the repository\nitself, so a runner cannot mis-report which repo it ran for. See <a href=\"#github-actions\">docs/github-actions.md</a>.</li>\n</ul>\n<p>→ <a href=\"#architecture\">docs/architecture.md</a> · <a href=\"#deploy\">docs/DEPLOY.md</a> · <a href=\"#security\">docs/security.md</a></p>\n<hr>\n<h2 id=\"exposing-a-central-on-the-internet\">Exposing a central on the internet</h2>\n<p>A central can be published behind a tunnel. <code>AGENTISTICS_EXPOSURE</code> selects a profile —\n<code>local</code> | <code>lan</code> | <code>public</code> — and <strong><code>public</code> permanently revokes every route that touches the host</strong>\n(shell, local chat, raw transcripts, MCP admin) and requires a second factor of every owner. The\nprofile is the only thing that decides a capability; no opt-in re-enables host power on <code>public</code>.</p>\n<pre><code class=\"language-bash\">agentop doctor --exposed     # run this BEFORE opening a tunnel\n</code></pre>\n<p>A check that could not be verified reports <code>fail</code>, never a reassuring <code>pass</code>.</p>\n<p>→ <a href=\"#exposure\">docs/exposure.md</a> · <a href=\"https://github.com/blpsoares/agentistics/blob/main/SECURITY.md\" target=\"_blank\" rel=\"noopener\">SECURITY.md</a></p>\n<hr>\n<h2 id=\"the-cli-agentop\">The CLI — `agentop`</h2>\n<p align=\"center\">\n  <img src=\"/release/gifs/control-center-commands.gif\" alt=\"The commands cheat sheet\" width=\"100%\" />\n</p><table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><em>(bare)</em> / <code>start</code></td>\n<td>The control center</td>\n</tr>\n<tr>\n<td><code>setup</code></td>\n<td>First-run wizard (solo / central / member)</td>\n</tr>\n<tr>\n<td><code>server</code></td>\n<td>Web + API + MCP + daemon. <code>--central</code> runs a central natively (no Docker), <code>--bg</code> detaches</td>\n</tr>\n<tr>\n<td><code>restart</code></td>\n<td>Bounce a mode; <code>--rebuild</code> rebuilds first (a <strong>full</strong>, cacheless rebuild — <code>--cache</code> opts out)</td>\n</tr>\n<tr>\n<td><code>status</code></td>\n<td>Services + health</td>\n</tr>\n<tr>\n<td><code>tui</code></td>\n<td>The live terminal dashboard</td>\n</tr>\n<tr>\n<td><code>watch</code></td>\n<td>The OTel daemon only</td>\n</tr>\n<tr>\n<td><code>central</code></td>\n<td><code>up</code> / <code>init</code> / <code>down</code> / <code>logs</code> / <code>status</code> / <code>restart</code> / <code>pull</code></td>\n</tr>\n<tr>\n<td><code>member</code></td>\n<td><code>connect</code> / <code>leave</code> / <code>status</code> / <code>list</code></td>\n</tr>\n<tr>\n<td><code>ci-push</code></td>\n<td>One-shot push of a CI runner&#39;s metrics</td>\n</tr>\n<tr>\n<td><code>autostart</code></td>\n<td>Start a mode with the system (systemd user service)</td>\n</tr>\n<tr>\n<td><code>doctor</code></td>\n<td>Exposure preflight; <code>--exposed</code> checks against the strict public bar</td>\n</tr>\n<tr>\n<td><code>setup-token</code></td>\n<td>Reissue the one-time owner setup token</td>\n</tr>\n<tr>\n<td><code>reset-password</code></td>\n<td>The way back in for a locked-out last owner</td>\n</tr>\n<tr>\n<td><code>upgrade</code> · <code>check-update</code></td>\n<td>Update, or print a notice when one is due</td>\n</tr>\n</tbody></table>\n<p>→ <strong>Full reference:</strong> <a href=\"#cli\">docs/cli.md</a></p>\n<hr>\n<h2 id=\"costs-and-where-the-prices-come-from\">Costs, and where the prices come from</h2>\n<p>Costs are computed per session from a pricing table with <strong>three layered sources</strong>, merged in order\nof trust: the built-in table (compiled in — the floor), the LiteLLM community dataset, and the\nvendors&#39; own pages. A source that fails or returns junk costs freshness, never the ability to price\nanything; community rows that imply a unit change or sit more than tenfold from the built-in figure\nare dropped.</p>\n<p><strong>Settings → Pricing</strong> lists every model <em>this machine has actually used</em>, each with its origin\n(<code>official</code> / <code>community</code> / <code>builtin</code>) — so a model nobody can price is visible rather than silently\nguessed at.</p>\n<hr>\n<h2 id=\"keeping-history-that-the-harnesses-delete\">Keeping history that the harnesses delete</h2>\n<p>Claude Code deletes session transcripts older than <code>cleanupPeriodDays</code> (30 by default) on every\nstartup. agentistics asks once, on first run, what to do about that:</p>\n<table>\n<thead>\n<tr>\n<th>Mode</th>\n<th>What it keeps</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>consolidate</code> <em>(recommended)</em></td>\n<td>One small JSON of computed metrics per session — survives the cleanup, no chat duplicated</td>\n</tr>\n<tr>\n<td><code>full</code></td>\n<td>Additionally mirrors the raw transcripts, chat included. Grows without bound</td>\n</tr>\n<tr>\n<td><code>off</code></td>\n<td>Nothing — <code>~/.claude</code> only</td>\n</tr>\n</tbody></table>\n<p>Revived sessions appear in lists and agent metrics but never inflate aggregate totals.</p>\n<hr>\n<h2 id=\"nay-and-the-mcp-server\">Nay, and the MCP server</h2>\n<p><strong>Nay</strong> is a chat panel that answers questions about your own data by calling the MCP tools.\nIt runs <code>claude --print</code> under the hood, so it spends your Claude subscription quota.</p>\n<p>The <strong>MCP server</strong> exposes the same analytics as structured tools (summary, harnesses, projects,\nsessions, costs, layout building, PDF export) and registers itself at user scope on first start —\nso any Claude Code session can use them, not just Nay.</p>\n<pre><code class=\"language-bash\">claude mcp list   # should show &quot;agentistics&quot;\n</code></pre>\n<p>→ <a href=\"#nay\">docs/nay.md</a> · <a href=\"#mcp\">docs/mcp.md</a></p>\n<hr>\n<h2 id=\"also-in-the-box\">Also in the box</h2>\n<ul>\n<li><strong>OpenTelemetry export</strong> for Grafana, Datadog or any OTLP collector — <a href=\"#opentelemetry\">docs/opentelemetry.md</a></li>\n<li><strong>Live sessions</strong> — which assistants are running right now, detected from host processes. When\ndetection is impossible (not Linux, no <code>/proc</code>, a container that cannot see the host) it says so\ninstead of rendering an honest-looking zero</li>\n<li><strong>PDF export</strong> of any combination of sections, period and theme</li>\n<li><strong>PT-BR + EN</strong>, light and dark, throughout — CLI, TUI and web</li>\n</ul>\n<hr>\n<h2 id=\"documentation\">Documentation</h2>\n<table>\n<thead>\n<tr>\n<th>Doc</th>\n<th>Contents</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><a href=\"#cli\">docs/cli.md</a></td>\n<td>Every <code>agentop</code> command, flag and example</td>\n</tr>\n<tr>\n<td><a href=\"#architecture\">docs/architecture.md</a></td>\n<td>Structure, request lifecycle, build pipeline</td>\n</tr>\n<tr>\n<td><a href=\"#security\">docs/security.md</a></td>\n<td>Threat model, trust boundaries, the limits of each control</td>\n</tr>\n<tr>\n<td><a href=\"#exposure\">docs/exposure.md</a></td>\n<td>Publishing a central safely</td>\n</tr>\n<tr>\n<td><a href=\"#deploy\">docs/DEPLOY.md</a></td>\n<td>Central deployment, <code>central.sh</code>, env vars</td>\n</tr>\n<tr>\n<td><a href=\"#github-actions\">docs/github-actions.md</a></td>\n<td>CI ingest, OIDC, repo-bound tokens</td>\n</tr>\n<tr>\n<td><a href=\"#harness-contract\">docs/harness-contract.md</a></td>\n<td>What each metric must MEAN across harnesses</td>\n</tr>\n<tr>\n<td><a href=\"#data-sources\">docs/data-sources.md</a></td>\n<td>Data sources, JSONL parsing, <code>SessionMeta</code></td>\n</tr>\n<tr>\n<td><a href=\"#metrics\">docs/metrics.md</a></td>\n<td>Pricing table, cost formula, streak, cache</td>\n</tr>\n<tr>\n<td><a href=\"#nay\">docs/nay.md</a> · <a href=\"#mcp\">docs/mcp.md</a></td>\n<td>The chat assistant and the MCP tools</td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"reproducing-the-media\">Reproducing the media</h2>\n<p><a name=\"reproducing-the-media\"></a></p>\n<p>The GIFs above are recorded from a demo fleet, never from a real machine — a published recording\ncannot be taken back, and a real project name or repository in one is a leak.</p>\n<pre><code class=\"language-bash\"># 1. a pseudonymized fleet, derived from this machine&#39;s computed metrics\n#    (never from transcripts, so chat cannot reach it by construction)\nbun run packages/server/scripts/seed-demo.ts --split 3 --force\n\n# 2. run them\nHOME=~/.agentistics-demo-home-1 PORT=47391 ./release/agentop server &amp;\n\n# 3. record\npackages/server/scripts/record-all.sh                    # terminal → .cast + .gif\nbun run packages/server/scripts/record-web.ts            # web → .gif\n</code></pre>\n<p>Terminal recordings are <a href=\"https://asciinema.org\" target=\"_blank\" rel=\"noopener\">asciinema</a> casts (<code>casts/*.cast</code>, embeddable as a\nreal player with selectable text) rendered to GIF with <a href=\"https://github.com/asciinema/agg\" target=\"_blank\" rel=\"noopener\">`agg`</a>.</p>\n<hr>\n<h2 id=\"contributing\">Contributing</h2>\n<p>See <a href=\"https://github.com/blpsoares/agentistics/blob/main/CONTRIBUTING.md\" target=\"_blank\" rel=\"noopener\">CONTRIBUTING.md</a>. Everything in this project is in English — code, comments,\ncommits, docs.</p>\n<hr>\n<h2 id=\"star-history\">Star History</h2>\n<p align=\"center\">\n  <a href=\"https://star-history.com/#blpsoares/agentistics&Date\">\n    <picture>\n      <source media=\"(prefers-color-scheme: dark)\" srcset=\"https://api.star-history.com/svg?repos=blpsoares/agentistics&type=Date&theme=dark\" />\n      <source media=\"(prefers-color-scheme: light)\" srcset=\"https://api.star-history.com/svg?repos=blpsoares/agentistics&type=Date\" />\n      <img alt=\"Star History Chart\" src=\"https://api.star-history.com/svg?repos=blpsoares/agentistics&type=Date\" />\n    </picture>\n  </a>\n</p><hr>\n<p align=\"center\">\n  Made with ♥ for the vibe coding community\n</p>\n",
    "headings": [
      {
        "id": "what-is-agentistics",
        "text": "What is agentistics?",
        "level": 2
      },
      {
        "id": "install",
        "text": "Install",
        "level": 2
      },
      {
        "id": "linux-wsl-one-line",
        "text": "Linux / WSL — one line",
        "level": 3
      },
      {
        "id": "windows",
        "text": "Windows",
        "level": 3
      },
      {
        "id": "from-source",
        "text": "From source",
        "level": 3
      },
      {
        "id": "the-terminal-dashboard",
        "text": "The terminal dashboard",
        "level": 2
      },
      {
        "id": "the-dashboard",
        "text": "The dashboard",
        "level": 2
      },
      {
        "id": "pages",
        "text": "Pages",
        "level": 3
      },
      {
        "id": "comparing-harnesses",
        "text": "Comparing harnesses",
        "level": 3
      },
      {
        "id": "repositories",
        "text": "Repositories",
        "level": 3
      },
      {
        "id": "tags",
        "text": "Tags",
        "level": 3
      },
      {
        "id": "custom-layouts",
        "text": "Custom layouts",
        "level": 3
      },
      {
        "id": "on-a-phone",
        "text": "On a phone",
        "level": 3
      },
      {
        "id": "team-mode",
        "text": "Team Mode",
        "level": 2
      },
      {
        "id": "accounts-teams-and-roles",
        "text": "Accounts, teams and roles",
        "level": 3
      },
      {
        "id": "what-a-machine-shares-and-what-it-withholds",
        "text": "What a machine shares — and what it withholds",
        "level": 3
      },
      {
        "id": "presence-self-healing-and-ci",
        "text": "Presence, self-healing and CI",
        "level": 3
      },
      {
        "id": "exposing-a-central-on-the-internet",
        "text": "Exposing a central on the internet",
        "level": 2
      },
      {
        "id": "the-cli-agentop",
        "text": "The CLI — `agentop`",
        "level": 2
      },
      {
        "id": "costs-and-where-the-prices-come-from",
        "text": "Costs, and where the prices come from",
        "level": 2
      },
      {
        "id": "keeping-history-that-the-harnesses-delete",
        "text": "Keeping history that the harnesses delete",
        "level": 2
      },
      {
        "id": "nay-and-the-mcp-server",
        "text": "Nay, and the MCP server",
        "level": 2
      },
      {
        "id": "also-in-the-box",
        "text": "Also in the box",
        "level": 2
      },
      {
        "id": "documentation",
        "text": "Documentation",
        "level": 2
      },
      {
        "id": "reproducing-the-media",
        "text": "Reproducing the media",
        "level": 2
      },
      {
        "id": "contributing",
        "text": "Contributing",
        "level": 2
      },
      {
        "id": "star-history",
        "text": "Star History",
        "level": 2
      }
    ]
  },
  "cli": {
    "id": "cli",
    "file": "docs/cli.md",
    "title": "CLI reference",
    "group": "Start here",
    "html": "<h1 id=\"agentistics-cli-agentop\">agentistics CLI — `agentop`</h1>\n<p><code>agentop</code> is the single binary for everything agentistics does: run the dashboard,\nthe terminal TUI, the OpenTelemetry daemon, host or join a <strong>Team Mode</strong> central,\nand manage autostart + updates.</p>\n<p>Get the binary from the <a href=\"https://github.com/blpsoares/agentistics/blob/main/../README.md#install\" target=\"_blank\" rel=\"noopener\">install instructions</a> (<code>install.sh</code>,\nthe Windows installer, or <code>bun run build:binary</code> from source). From a checkout you\ncan also run it directly with <code>bun run packages/server/bin/cli.ts &lt;command&gt;</code>.</p>\n<pre><code class=\"language-bash\">agentop --help       # full usage\nagentop --version    # print version (and a notice if an update exists)\n</code></pre>\n<blockquote>\n<p><strong>Ports:</strong> a solo/member instance serves the <strong>web dashboard on 47292</strong> (the URL you open) and\nthe <strong>api + mcp on 47291</strong> (in dev, Vite serves the web on 47292 and the api runs on 47291 — same split).\nA Team Mode <strong>central</strong> runs in Docker on <strong>48080</strong>\nby default. These are intentionally distinct so a member and a central can\ncoexist on the same host.</p>\n</blockquote>\n<hr>\n<h2 id=\"command-overview\">Command overview</h2>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><a href=\"#start\">`start`</a></td>\n<td>Interactive launcher — pick mode + how to run (foreground / bg / Docker / boot)</td>\n</tr>\n<tr>\n<td><a href=\"#setup\">`setup`</a></td>\n<td>Interactive first-run wizard (solo / central / member)</td>\n</tr>\n<tr>\n<td><a href=\"#server\">`server`</a></td>\n<td>Start the web dashboard + api + Nay + background daemon (non-interactive)</td>\n</tr>\n<tr>\n<td><a href=\"#restart\">`restart`</a></td>\n<td>Restart a running mode so it picks up new code / config</td>\n</tr>\n<tr>\n<td><a href=\"#status\">`status`</a></td>\n<td>At-a-glance: mode, services (server/central/machine) + health</td>\n</tr>\n<tr>\n<td><a href=\"#tui\">`tui`</a></td>\n<td>Live terminal dashboard (no browser)</td>\n</tr>\n<tr>\n<td><a href=\"#watch\">`watch`</a></td>\n<td>OpenTelemetry metrics daemon only (headless)</td>\n</tr>\n<tr>\n<td><a href=\"#central\">`central`</a></td>\n<td>Manage the Team Mode central (wraps <code>central.sh</code>)</td>\n</tr>\n<tr>\n<td><a href=\"#member\">`member`</a></td>\n<td>Join / leave / inspect a Team Mode central from this machine</td>\n</tr>\n<tr>\n<td><a href=\"#ci-push\">`ci-push`</a></td>\n<td>One-shot push of a GitHub Actions run&#39;s metrics to a central (per repo)</td>\n</tr>\n<tr>\n<td><a href=\"#autostart\">`autostart`</a></td>\n<td>Start a mode with the system (systemd user service)</td>\n</tr>\n<tr>\n<td><a href=\"#upgrade\">`upgrade`</a></td>\n<td>Upgrade <code>agentop</code> to the latest release</td>\n</tr>\n<tr>\n<td><a href=\"#check-update\">`check-update`</a></td>\n<td>Print an &quot;update available&quot; banner, else stay silent</td>\n</tr>\n<tr>\n<td><a href=\"#doctor\">`doctor`</a></td>\n<td>Run the exposure preflight before publishing a central</td>\n</tr>\n</tbody></table>\n<p>Running <strong>bare <code>agentop</code></strong> on an interactive terminal, on a machine that isn&#39;t\nconfigured yet, launches the <a href=\"#setup\">`setup`</a> wizard. Otherwise it prints help.</p>\n<hr>\n<h2 id=\"start\">`start`</h2>\n<p>The interactive launcher — a <strong>re-runnable control panel</strong> with <strong>arrow-key navigation</strong> (↑/↓ +\nEnter; you only type in text fields like an endpoint or token). <strong>English by default, with a pt-BR\ntoggle.</strong> It prints a banner and a two-part status, then a menu. Run it as often as you like; it\nalways reflects the current state.</p>\n<pre><code class=\"language-bash\">agentop start\nagentop start --lang pt      # force Portuguese for this run\n</code></pre>\n<pre><code>  ▄▀█ █▀▀ █▀▀ █▄░█ ▀█▀ █ █▀ ▀█▀ █ █▀▀ █▀\n  █▀█ █▄█ ██▄ █░▀█ ░█░ █ ▄█ ░█░ █ █▄▄ ▄█\n  AI coding-assistant analytics · agentop\n  ──────────────────────────────────────\n  config   member — sends metrics to a central at http://host:48080\n  running  ● agentistics    (this machine)   http://localhost:47292\n           ● agentistics central    (docker)\n  ──────────────────────────────────────\n\n  What would you like to start?\n  ❯ agentistics            this machine\n    agentistics central    team aggregator · :48080\n    Disconnect from the central    back to solo\n    Stop a running service…\n    Switch to Português\n    Quit\n</code></pre>\n<p><strong>Naming:</strong> <code>agentistics</code> is the per-machine app; <code>agentistics central</code> is the team aggregator.\nBoth serve a web dashboard, so neither is labelled &quot;the dashboard&quot;.</p>\n<p>The status is split in two, because they&#39;re independent:</p>\n<ul>\n<li><strong>config</strong> — what your preferences say: <code>solo</code> (nothing leaves), <code>member — sends metrics to a central at &lt;url&gt;</code>, or <code>central</code> (this machine hosts one).</li>\n<li><strong>running</strong> — what&#39;s actually up right now, detected live: <code>agentistics</code> (native local server),\nan <code>agentistics central</code> container, and/or an <code>agentistics</code> machine container. A machine can be\nseveral at once.</li>\n</ul>\n<p>Menu:</p>\n<ul>\n<li><strong>agentistics</strong> → choose how to run: <strong>Foreground</strong> / <strong>Background</strong> / <strong>Docker</strong>. Starting while\none is already up warns you and offers to <strong>kill it and start fresh</strong>. Background/central starts\nthen offer to also <strong>start on boot</strong> (systemd).</li>\n<li><strong>agentistics central</strong> → builds &amp; starts the aggregator (Docker, bundles MongoDB, <code>:48080</code>).</li>\n<li><strong>Connect to a central</strong> (or <strong>Disconnect</strong> when already a member) — become/stop being a member;\nyou can also do this from the web UI (<strong>Settings → Team</strong>).</li>\n<li><strong>Stop a running service…</strong> (shown only when something is up) lists exactly what&#39;s running and\nlets you take down one — or <strong>Everything</strong>.</li>\n<li><strong>Switch to Português / English</strong> — flips the language and persists it (shared with the web\n<code>lang</code> preference).</li>\n<li>The <strong>Docker</strong> option mounts the host&#39;s harness dirs read-only — run the machine in Docker <strong>or</strong>\nnatively, not both. See <a href=\"https://github.com/blpsoares/agentistics/blob/main/DEPLOY.md#machine-in-docker\" target=\"_blank\" rel=\"noopener\">Machine in Docker</a>.</li>\n<li><strong>Non-interactive stdin</strong> (a pipe or a systemd unit) skips the panel and behaves exactly like\n<a href=\"#server\">`server`</a>.</li>\n</ul>\n<p>Ctrl-C is non-destructive — it aborts without starting anything.</p>\n<hr>\n<h2 id=\"restart\">`restart`</h2>\n<p>Restart a running mode so it picks up new code (after an <code>upgrade</code> / <code>git pull</code>) or a changed\nconfig. Defaults to <code>server</code>.</p>\n<pre><code class=\"language-bash\">agentop restart            # = restart server\nagentop restart server     # bounce the systemd user service (agentop-server)\nagentop restart watch      # bounce the watch service\nagentop restart central    # rebuild + restart the central&#39;s Docker container\n</code></pre>\n<p><code>server</code>/<code>watch</code> bounce the installed <a href=\"#autostart\">systemd user service</a> — if none is installed it\ntells you to run it in the foreground or enable autostart first. <code>central</code> delegates to\n<code>central.sh restart</code>; to also pick up <strong>code</strong> changes on a central, use <code>agentop central up</code>\n(rebuilds the image) instead.</p>\n<hr>\n<h2 id=\"status\">`status`</h2>\n<p>Non-interactive, at-a-glance report of this machine. Prints three blocks:</p>\n<ul>\n<li><strong>CONFIG</strong> — the team mode (<code>solo</code> / <code>central</code> / <code>member</code>) and, for a member, the\ncentral endpoint (from preferences).</li>\n<li><strong>SERVICES</strong> — detected live: the local server (<code>http://localhost:47291/api/health</code>;\nshows the dashboard URL when up), the central container, and the machine container.</li>\n<li><strong>HEALTH</strong> — a one-line summary from <code>/api/health</code> when the server is up, else <code>n/a</code>.</li>\n</ul>\n<pre><code class=\"language-bash\">agentop status\n</code></pre>\n<p>Handy for a quick &quot;is everything up and healthy?&quot; without opening the dashboard.</p>\n<hr>\n<h2 id=\"setup\">`setup`</h2>\n<p>Interactive first-run wizard. Walks you through picking a mode and wires up the\nrest for you: <strong>solo</strong> (local only, nothing leaves the machine), <strong>central</strong> (host\nthe aggregator on this machine via <code>central.sh init</code>), or <strong>member</strong> (push this\nmachine&#39;s computed metrics to a central via <code>member connect</code>). It then offers to\nenable <a href=\"#autostart\">autostart</a>.</p>\n<pre><code class=\"language-bash\">agentop setup\n</code></pre>\n<p>Needs a TTY. Ctrl-C is non-destructive — it aborts without touching your\npreferences. For non-interactive/scripted member onboarding, use\n<a href=\"#member\">`agentop member connect`</a> directly.</p>\n<hr>\n<h2 id=\"server\">`server`</h2>\n<p>Starts the web dashboard, api, Nay chat, MCP registration, and the OTel daemon —\neverything in one process. Binds two ports: the <strong>web dashboard on 47292</strong> (open this) and the\n<strong>api + mcp on 47291</strong>. They share one request handler, so the dashboard&#39;s <code>/api/*</code> calls just work.</p>\n<pre><code class=\"language-bash\">agentop server              # web: http://localhost:47292 · api/mcp: http://localhost:47291\nagentop server --port 4000  # api on 4000, web on 4001\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>--port &lt;n&gt;</code></td>\n<td><code>47291</code></td>\n<td>The <strong>api + mcp</strong> port; the web dashboard is served on this + 1 (default 47292)</td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"tui\">`tui`</h2>\n<p>A live multi-screen dashboard in the terminal — no browser needed. Built with\n<a href=\"https://term.ink\" target=\"_blank\" rel=\"noopener\">Ink</a>, it updates the instant the web dashboard does, over the same\n<code>/api/events</code> SSE stream.</p>\n<pre><code class=\"language-bash\">agentop tui\nagentop tui --lang pt         # force Portuguese for this run\n</code></pre>\n<pre><code> agentistics  ·  live coding-assistant analytics                          ● live\n\n 1 Overview  2 Projects  3 Sessions  4 Costs  5 Harnesses\n\n USD 8,364.44        13.3B         262           113.4K        2d\n cost                tokens        sessions      messages      streak\n\n activity · last 30 days\n ▅▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█▅▁▁▁█▃\n\n Harnesses\n Claude       ██████████████████████████████ USD 8,363.48  13.3B tok\n Codex        ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ USD 0.01  27.6K tok\n\n 1-5/tab switch screen  ·  f filter  ·  ? help  ·  q quit\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Key</th>\n<th>Does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>1</code>–<code>5</code>, <code>tab</code></td>\n<td>Switch screen — Overview, Projects, Sessions, Costs, Harnesses</td>\n</tr>\n<tr>\n<td><code>f</code></td>\n<td>Filter by harness</td>\n</tr>\n<tr>\n<td><code>r</code></td>\n<td>Force a refresh</td>\n</tr>\n<tr>\n<td><code>?</code></td>\n<td>Keyboard help</td>\n</tr>\n<tr>\n<td><code>q</code>, <code>ctrl+c</code></td>\n<td>Quit</td>\n</tr>\n</tbody></table>\n<p>There is no configuration wizard — it opens straight into Overview and filtering happens in-app.\nIf no server is listening it starts one itself and stops it again on exit; a server that was\nalready running is left alone.</p>\n<p>Metrics match the web dashboard exactly: both price through <code>calcCost()</code>, Claude totals come from\n<code>stats-cache.json</code> and every other harness from per-session sums. A metric a harness cannot\nproduce shows <code>N/A</code> rather than a misleading <code>0</code>.</p>\n<p>Needs an interactive terminal — piping it exits with a message instead of a crash.</p>\n<hr>\n<h2 id=\"watch\">`watch`</h2>\n<p>Runs only the OpenTelemetry file watcher + OTLP metrics exporter (the same daemon\n<code>server</code> runs in the background). Use this on a headless box that only needs to\nfeed Grafana/Datadog. See <a href=\"https://github.com/blpsoares/agentistics/blob/main/opentelemetry.md\" target=\"_blank\" rel=\"noopener\">docs/opentelemetry.md</a>.</p>\n<pre><code class=\"language-bash\">agentop watch\n</code></pre>\n<hr>\n<h2 id=\"central\">`central`</h2>\n<p>Manage the <strong>Team Mode central</strong> — the Docker service that aggregates metrics from\nmany members. This is a thin wrapper over the repo&#39;s <code>central.sh</code>, so it needs to\nrun from an agentistics checkout (the compiled binary alone doesn&#39;t ship the\nCompose stack). Full deployment details live in <a href=\"https://github.com/blpsoares/agentistics/blob/main/DEPLOY.md\" target=\"_blank\" rel=\"noopener\">docs/DEPLOY.md</a>.</p>\n<pre><code class=\"language-bash\">agentop central &lt;up|init|down|logs|status|restart|pull&gt;\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Action</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>init</code></td>\n<td>(Re)generate <code>central.env</code> interactively — auto-generates the secrets with <code>openssl</code>, detects your Tailscale IP for the bind, writes the file <code>chmod 600</code></td>\n</tr>\n<tr>\n<td><code>up</code></td>\n<td>Build the image and (re)create the containers (<code>--build --force-recreate</code>); offers <code>init</code> first if <code>central.env</code> is missing</td>\n</tr>\n<tr>\n<td><code>restart</code></td>\n<td>Restart the <code>app</code> container without rebuilding</td>\n</tr>\n<tr>\n<td><code>logs</code></td>\n<td>Follow the <code>app</code> container logs</td>\n</tr>\n<tr>\n<td><code>status</code></td>\n<td>Show container + health status</td>\n</tr>\n<tr>\n<td><code>down</code></td>\n<td>Stop and remove the containers — <strong>keeps</strong> the Mongo data volume</td>\n</tr>\n<tr>\n<td><code>pull</code></td>\n<td>Rebuild from a fresh base image (run <code>git pull</code> first)</td>\n</tr>\n</tbody></table>\n<pre><code class=\"language-bash\">agentop central init        # generate central.env (interactive)\nagentop central up          # build + (re)create — most common\nagentop central logs        # tail the app logs\nagentop central down        # stop, keep the data volume\n</code></pre>\n<p>From a checkout you can equivalently use the package scripts\n<code>bun run init:central</code> and <code>bun run up:central</code>, or call <code>./central.sh</code> directly.</p>\n<hr>\n<h2 id=\"member\">`member`</h2>\n<p>Configure this machine as a <strong>member</strong> that pushes computed metrics to a central.\nOnly aggregated metrics are sent — <strong>never</strong> chat content or raw transcripts. The\nmachine&#39;s display name is assigned by the central (baked into the minted token)\nand resolved via <code>/api/team/whoami</code>; there is no name field on the machine.</p>\n<pre><code class=\"language-bash\">agentop member connect --endpoint &lt;url&gt; --token &lt;token&gt; [--org &lt;org&gt;]\nagentop member leave\nagentop member status\n</code></pre>\n<h3 id=\"member-connect\">`member connect`</h3>\n<p>Verifies the token against the central&#39;s <code>whoami</code> endpoint, then saves the member\nconfig. On a bad token it prints an actionable error and writes <strong>nothing</strong> (no\nhalf-configured state).</p>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Required</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>--endpoint &lt;url&gt;</code></td>\n<td>yes</td>\n<td>Central base url, e.g. <code>http://host:48080</code></td>\n</tr>\n<tr>\n<td><code>--token &lt;token&gt;</code></td>\n<td>yes</td>\n<td>Token minted for this machine in the central&#39;s Team Manager</td>\n</tr>\n<tr>\n<td><code>--org &lt;org&gt;</code></td>\n<td>no</td>\n<td>Org override; defaults to the org on the token</td>\n</tr>\n</tbody></table>\n<pre><code class=\"language-bash\">agentop member connect --endpoint http://100.64.0.2:48080 --token abc123\n</code></pre>\n<h3 id=\"member-leave\">`member leave`</h3>\n<p>Best-effort notifies the central (so it drops this member&#39;s data) and resets this\nmachine back to solo. Succeeds locally even if the central is unreachable.</p>\n<h3 id=\"member-status\">`member status`</h3>\n<p>Prints the current mode / endpoint / org / user plus the live uploader state\n(<code>last sync</code> timestamp and whether the token/endpoint are healthy).</p>\n<pre><code>mode:      member\nendpoint:  http://100.64.0.2:48080\norg:       default\nuser:      alice-laptop\nlast sync: 2026-07-01T12:34:56.000Z\nstate:     ok\n</code></pre>\n<hr>\n<h2 id=\"ci-push\">`ci-push`</h2>\n<p>One-shot push of a <strong>GitHub Actions</strong> run&#39;s computed metrics to a central, attributed to\nthe repository it ran in. Meant to be the <strong>last step</strong> of a workflow that already runs Claude\nCode (it does <strong>not</strong> run Claude) — the ephemeral runner populates <code>~/.claude</code>, then <code>ci-push</code>\nsends that run&#39;s session/token/cost aggregates before the runner is torn down. It reads\neverything from the environment and takes no flags.</p>\n<table>\n<thead>\n<tr>\n<th>Env var</th>\n<th>Required</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>AGENTISTICS_CENTRAL_URL</code></td>\n<td>yes</td>\n<td>Central base URL, e.g. <code>https://central.example.com</code></td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_CI_TOKEN</code></td>\n<td>no</td>\n<td>Repo-bound static token (fallback when OIDC is unavailable)</td>\n</tr>\n</tbody></table>\n<p>Auth is <strong>keyless GitHub OIDC</strong> by default: with <code>permissions: id-token: write</code> on the job,\n<code>ci-push</code> fetches a short-lived GitHub-signed token itself (audience = <code>AGENTISTICS_CENTRAL_URL</code>)\nand the central verifies it against GitHub&#39;s JWKS. No secret needed. If no OIDC token can be\nfetched it falls back to <code>AGENTISTICS_CI_TOKEN</code>. The repo must first be <strong>registered</strong> on the\ncentral (Settings → Team → Repositories, which allowlists the remote and, for the fallback,\nmints the token).</p>\n<pre><code class=\"language-bash\"># inside a GitHub Actions job, after the Claude Code Action step:\ncurl -fsSL &quot;https://github.com/blpsoares/agentistics/releases/latest/download/agentop&quot; -o agentop\nchmod +x agentop\n./agentop ci-push\n</code></pre>\n<p><code>ci-push</code> <strong>never fails your job</strong> — a push error (central down, unverifiable token) logs and\nexits 0. It pushes <strong>computed metrics only</strong>, never chat. See\n<a href=\"https://github.com/blpsoares/agentistics/blob/main/github-actions.md\" target=\"_blank\" rel=\"noopener\">`docs/github-actions.md`</a> for the full workflow + registration walkthrough.</p>\n<hr>\n<h2 id=\"autostart\">`autostart`</h2>\n<p>Register a mode to start with the system. On <strong>Linux/WSL</strong> this installs a systemd\n<strong>user</strong> service at <code>~/.config/systemd/user/agentop-&lt;mode&gt;.service</code>, enables it\nwith <code>systemctl --user enable --now</code>, and runs <code>loginctl enable-linger</code> so it also\nstarts at boot without an active login. <code>enable</code> additionally installs a\n<code>~/.bashrc</code> hook that runs <a href=\"#check-update\">`agentop check-update`</a> on every\nterminal open. macOS and Windows print the manual step instead.</p>\n<pre><code class=\"language-bash\">agentop autostart &lt;mode&gt; &lt;enable|disable|status&gt;\n</code></pre>\n<ul>\n<li><code>mode</code> ∈ <code>server</code> · <code>central</code> · <code>watch</code></li>\n<li><code>enable</code> — register + start the service (and add the terminal update hook)</li>\n<li><code>disable</code> — stop and remove the service</li>\n<li><code>status</code> — show enabled/active state; <strong>omit the mode</strong> to list all services</li>\n</ul>\n<pre><code class=\"language-bash\">agentop autostart server enable    # start the dashboard at boot\nagentop autostart status           # list every autostart service\nagentop autostart watch disable    # stop the otel daemon service\n</code></pre>\n<hr>\n<h2 id=\"upgrade\">`upgrade`</h2>\n<p>Download and install the latest <code>agentop</code> release in place. (<code>update</code> is an alias.)</p>\n<pre><code class=\"language-bash\">agentop upgrade\n</code></pre>\n<p>On a <strong>central</strong> you upgrade the Docker stack instead — pull the repo and rebuild:</p>\n<pre><code class=\"language-bash\">git pull &amp;&amp; bun run up:central    # or: agentop central pull\n</code></pre>\n<p>On a <strong>member</strong> running as a systemd service, restart it after upgrading:</p>\n<pre><code class=\"language-bash\">agentop upgrade &amp;&amp; systemctl --user restart agentop-server\n</code></pre>\n<hr>\n<h2 id=\"check-update\">`check-update`</h2>\n<p>Prints the &quot;new version available&quot; banner <strong>only</strong> when a newer release exists,\nand stays completely silent otherwise — so it&#39;s safe to run on every shell start.\nThis is exactly what the <code>~/.bashrc</code> hook installed by <code>agentop autostart … enable</code> runs.</p>\n<pre><code class=\"language-bash\">agentop check-update\n</code></pre>\n<hr>\n<h2 id=\"update-detection\">Update detection</h2>\n<p>agentistics surfaces available updates in three places, all sourced from the same\nversion check:</p>\n<ul>\n<li><strong>On command run</strong> — most <code>agentop</code> commands print the update banner in parallel\nwith startup (non-blocking); <code>--version</code> appends the notice too.</li>\n<li><strong>On terminal / boot</strong> — the <code>~/.bashrc</code> hook added by <code>autostart … enable</code> runs\n<code>agentop check-update</code> when you open a shell (silent when you&#39;re current).</li>\n<li><strong>On the dashboard</strong> — a bell notification plus a <strong>mode-aware</strong> upgrade modal\nshowing the exact command for your role (a central shows <code>bun run up:central</code>; a\nmember shows <code>agentop upgrade</code> then <code>systemctl --user restart agentop-server</code>). A\nperiodic (~6h) server re-check pushes the notification live over SSE.</li>\n</ul>\n<hr>\n<h2 id=\"doctor\">`doctor`</h2>\n<p>Runs the exposure preflight: nine checks that decide whether this instance is safe to publish.\nExits non-zero on any failure, so going live can be gated on one command instead of on\nremembering nine environment variables.</p>\n<pre><code class=\"language-bash\">agentop doctor              # check against the current profile\nagentop doctor --exposed    # check against the strict public bar\n\n./central.sh doctor --exposed        # from a repo checkout, INSIDE the container\nagentop central doctor --exposed     # same, from the standalone binary\n</code></pre>\n<p><strong>On a Docker central, prefer the <code>central.sh</code> / <code>agentop central</code> form.</strong> The command\nevaluates the configuration the deployment will actually run with — it reads <code>central.env</code> when\npresent and prints which file it used — but the owner-MFA and machine-token checks also need\nMongoDB, which is reachable only from inside the compose network. Run on the host, those two\nreport as unverified, which counts as a failure by design.</p>\n<p><code>--exposed</code> evaluates as if <code>AGENTISTICS_EXPOSURE=public</code> were already set, which is how you\nverify readiness <strong>before</strong> flipping it.</p>\n<p>What it checks:</p>\n<table>\n<thead>\n<tr>\n<th>Check</th>\n<th>Fails when</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>local-shell</code></td>\n<td><code>/api/exec</code>, <code>/api/chat-tty</code>, the host transcript readers or <code>/api/mcp-action</code> are still reachable</td>\n</tr>\n<tr>\n<td><code>session-secret</code></td>\n<td>The secret is missing, shorter than 32 chars, or equal to the dashboard password</td>\n</tr>\n<tr>\n<td><code>tls</code></td>\n<td><code>AGENTISTICS_TEAM_TLS</code> is unset on a public profile</td>\n</tr>\n<tr>\n<td><code>bind-ip</code></td>\n<td><code>BIND_IP</code> is not loopback on a public profile — the tunnel connects locally, so anything wider is a way in that bypasses it</td>\n</tr>\n<tr>\n<td><code>trust-proxy</code></td>\n<td><em>(warn)</em> forwarded-IP trust does not match the deployment, so per-IP limits apply to everyone at once</td>\n</tr>\n<tr>\n<td><code>owner-mfa</code></td>\n<td>Any owner account has no TOTP enrolled</td>\n</tr>\n<tr>\n<td><code>cors</code></td>\n<td>A plaintext origin sits in <code>AGENTISTICS_ALLOWED_ORIGINS</code></td>\n</tr>\n<tr>\n<td><code>mongo-auth</code></td>\n<td><em>(warn)</em> the database has no credentials — acceptable only while its port stays unpublished</td>\n</tr>\n<tr>\n<td><code>machine-tokens</code></td>\n<td><em>(warn)</em> no machine tokens have been minted yet</td>\n</tr>\n</tbody></table>\n<p>A check that could not be verified — the database was unreachable, say — reports a <strong>failure</strong>,\nnot a reassuring pass. See <a href=\"https://github.com/blpsoares/agentistics/blob/main/exposure.md\" target=\"_blank\" rel=\"noopener\">exposure.md</a> for the full deployment runbook.</p>\n",
    "headings": [
      {
        "id": "agentistics-cli-agentop",
        "text": "agentistics CLI — `agentop`",
        "level": 1
      },
      {
        "id": "command-overview",
        "text": "Command overview",
        "level": 2
      },
      {
        "id": "start",
        "text": "`start`",
        "level": 2
      },
      {
        "id": "restart",
        "text": "`restart`",
        "level": 2
      },
      {
        "id": "status",
        "text": "`status`",
        "level": 2
      },
      {
        "id": "setup",
        "text": "`setup`",
        "level": 2
      },
      {
        "id": "server",
        "text": "`server`",
        "level": 2
      },
      {
        "id": "tui",
        "text": "`tui`",
        "level": 2
      },
      {
        "id": "watch",
        "text": "`watch`",
        "level": 2
      },
      {
        "id": "central",
        "text": "`central`",
        "level": 2
      },
      {
        "id": "member",
        "text": "`member`",
        "level": 2
      },
      {
        "id": "member-connect",
        "text": "`member connect`",
        "level": 3
      },
      {
        "id": "member-leave",
        "text": "`member leave`",
        "level": 3
      },
      {
        "id": "member-status",
        "text": "`member status`",
        "level": 3
      },
      {
        "id": "ci-push",
        "text": "`ci-push`",
        "level": 2
      },
      {
        "id": "autostart",
        "text": "`autostart`",
        "level": 2
      },
      {
        "id": "upgrade",
        "text": "`upgrade`",
        "level": 2
      },
      {
        "id": "check-update",
        "text": "`check-update`",
        "level": 2
      },
      {
        "id": "update-detection",
        "text": "Update detection",
        "level": 2
      },
      {
        "id": "doctor",
        "text": "`doctor`",
        "level": 2
      }
    ]
  },
  "metrics": {
    "id": "metrics",
    "file": "docs/metrics.md",
    "title": "Metrics & pricing",
    "group": "Start here",
    "html": "<h1 id=\"metrics-and-calculations\">Metrics and Calculations</h1>\n<p>All cost and token calculations use a single source of truth: <code>packages/core/src/types.ts</code> (imported as <code>@agentistics/core</code>). No layer duplicates the pricing logic.</p>\n<h2 id=\"pricing-table\">Pricing table</h2>\n<p>All prices are in USD per <strong>1 million tokens</strong>:</p>\n<table>\n<thead>\n<tr>\n<th>Model</th>\n<th>Input</th>\n<th>Output</th>\n<th>Cache Read</th>\n<th>Cache Write</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Claude Opus 4.6 / 4.5</td>\n<td>$5.00</td>\n<td>$25.00</td>\n<td>$0.50</td>\n<td>$6.25</td>\n</tr>\n<tr>\n<td>Claude Opus 4.1 / 4.0</td>\n<td>$15.00</td>\n<td>$75.00</td>\n<td>$1.50</td>\n<td>$18.75</td>\n</tr>\n<tr>\n<td>Claude Sonnet 4.6 / 4.5 / 4.0</td>\n<td>$3.00</td>\n<td>$15.00</td>\n<td>$0.30</td>\n<td>$3.75</td>\n</tr>\n<tr>\n<td>Claude Haiku 4.5</td>\n<td>$1.00</td>\n<td>$5.00</td>\n<td>$0.10</td>\n<td>$1.25</td>\n</tr>\n<tr>\n<td>Claude Haiku 3.5</td>\n<td>$0.80</td>\n<td>$4.00</td>\n<td>$0.08</td>\n<td>$1.00</td>\n</tr>\n<tr>\n<td>Claude Haiku 3.0</td>\n<td>$0.25</td>\n<td>$1.25</td>\n<td>$0.03</td>\n<td>$0.30</td>\n</tr>\n</tbody></table>\n<blockquote>\n<p>Prices are updated in <code>packages/core/src/types.ts → MODEL_PRICING</code>. If Anthropic changes pricing, update only that table — all layers pick it up automatically.</p>\n</blockquote>\n<p>The <code>/api/rates</code> endpoint also fetches the current Anthropic pricing page and caches it. The dashboard shows a &quot;pricing source&quot; indicator when live prices differ from the local table.</p>\n<h2 id=\"cost-formula\">Cost formula</h2>\n<pre><code>Total Cost = Σ per model [\n  (inputTokens     / 1,000,000 × input_price)       +\n  (outputTokens    / 1,000,000 × output_price)      +\n  (cacheReadTokens / 1,000,000 × cache_read_price)  +\n  (cacheWriteTokens/ 1,000,000 × cache_write_price)\n]\n</code></pre>\n<p>Implemented in <code>calcCost(usage, modelId)</code> at <code>packages/core/src/types.ts</code>.</p>\n<h2 id=\"blended-rate-per-session-cost-estimate\">Blended rate (per-session cost estimate)</h2>\n<p>Individual sessions may not store which model was used — that data is only available in aggregate via <code>stats-cache.json → modelUsage</code>. When a per-session cost estimate is needed (project filter active, or per-session cost column in PDF export), a weighted average rate is computed:</p>\n<pre><code>avg_input_rate  = Σ(model_input_tokens  × model_input_price)  / Σ input_tokens\navg_output_rate = Σ(model_output_tokens × model_output_price) / Σ output_tokens\n(same for cache read and write)\n\nEstimated Session Cost = session_input_tokens  × avg_input_rate\n                       + session_output_tokens × avg_output_rate\n                       + ...\n</code></pre>\n<h2 id=\"token-types\">Token types</h2>\n<table>\n<thead>\n<tr>\n<th>Type</th>\n<th>Description</th>\n<th>Relative cost</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Input</strong></td>\n<td>Context + prompt sent to the model</td>\n<td>Base</td>\n</tr>\n<tr>\n<td><strong>Output</strong></td>\n<td>Tokens generated by the model</td>\n<td>~5× more expensive than input</td>\n</tr>\n<tr>\n<td><strong>Cache Read</strong></td>\n<td>Served from prompt cache</td>\n<td>~10× cheaper than input</td>\n</tr>\n<tr>\n<td><strong>Cache Write</strong></td>\n<td>Creating/updating the prompt cache</td>\n<td>~1.25× more expensive than input</td>\n</tr>\n</tbody></table>\n<h3 id=\"cache-efficiency\">Cache efficiency</h3>\n<p>Cache hit rate = <code>cacheRead / (cacheRead + input)</code></p>\n<p>Color coding: red &lt; 30% · yellow 30–60% · green ≥ 60%</p>\n<p>Net savings = gross saved − write overhead:</p>\n<pre><code>gross_saved   = cacheReadTokens × (input_price − cache_read_price)\nwrite_overhead = cacheWriteTokens × (cache_write_price − input_price)\nnet_saved     = gross_saved − write_overhead\n</code></pre>\n<h2 id=\"streak-consecutive-active-days\">Streak (consecutive active days)</h2>\n<p>The streak is calculated globally — date and project filters do not affect it.</p>\n<pre><code>streak = 0\nfor i = 0, 1, 2, ..., 365:\n    date = today − i days\n    if date has activity:\n        streak++\n    else if i &gt; 0:   # today with no activity yet does NOT break the streak\n        break\n</code></pre>\n<p>This means: if you haven&#39;t worked yet today, the streak still counts from yesterday. You are not penalized for opening the dashboard before your first commit of the day.</p>\n<h2 id=\"session-duration\">Session duration</h2>\n<pre><code>duration_minutes = (last_message_timestamp − first_message_timestamp) / 60\n</code></pre>\n<p>Minimum duration is 0 (single-turn sessions).</p>\n<h2 id=\"git-commits-and-pushes\">Git commits and pushes</h2>\n<p>Detected by analyzing <code>Bash</code> tool inputs at JSONL parse time. Each command is split on <code>&amp;&amp;</code>, <code>||</code>, <code>;</code>, and newlines before matching:</p>\n<pre><code>/^(cd\\s+\\S+\\s+&amp;&amp;\\s+)?git\\s+commit\\b/  → gitCommits++\n/^(cd\\s+\\S+\\s+&amp;&amp;\\s+)?git\\s+push\\b/    → gitPushes++\n</code></pre>\n<p>Lines changed and files modified are retrieved from git:</p>\n<pre><code class=\"language-bash\">git -C &lt;project_path&gt; log --numstat --after=&quot;&lt;start&gt;&quot; --before=&quot;&lt;end&gt;&quot;\n</code></pre>\n<p><code>files_modified</code> is the higher of git-tracked files and files Claude directly edited (Edit/Write/MultiEdit tool calls). See <a href=\"https://github.com/blpsoares/agentistics/blob/main/data-sources.md#files_modified-counting\" target=\"_blank\" rel=\"noopener\">data-sources.md</a> for details.</p>\n<h2 id=\"tool-token-attribution\">Tool token attribution</h2>\n<p>Output tokens are attributed to tools using a fair-split algorithm:</p>\n<pre><code>For each assistant message with N tool_use blocks:\n  tokens_per_tool = output_tokens ÷ N\n  tool_output_tokens[tool_name] += tokens_per_tool\n</code></pre>\n<p>Tools consuming more than 40% of total output tokens are flagged as token &quot;villains&quot; in the Tool Metrics panel.</p>\n<h2 id=\"brl-conversion\">BRL conversion</h2>\n<p>The Brazilian Real exchange rate is fetched from a public API by <code>/api/rates</code> and cached for 1 hour. If the fetch fails, a hardcoded fallback rate is used. The dashboard always shows the source and timestamp of the rate in the currency toggle tooltip.</p>\n",
    "headings": [
      {
        "id": "metrics-and-calculations",
        "text": "Metrics and Calculations",
        "level": 1
      },
      {
        "id": "pricing-table",
        "text": "Pricing table",
        "level": 2
      },
      {
        "id": "cost-formula",
        "text": "Cost formula",
        "level": 2
      },
      {
        "id": "blended-rate-per-session-cost-estimate",
        "text": "Blended rate (per-session cost estimate)",
        "level": 2
      },
      {
        "id": "token-types",
        "text": "Token types",
        "level": 2
      },
      {
        "id": "cache-efficiency",
        "text": "Cache efficiency",
        "level": 3
      },
      {
        "id": "streak-consecutive-active-days",
        "text": "Streak (consecutive active days)",
        "level": 2
      },
      {
        "id": "session-duration",
        "text": "Session duration",
        "level": 2
      },
      {
        "id": "git-commits-and-pushes",
        "text": "Git commits and pushes",
        "level": 2
      },
      {
        "id": "tool-token-attribution",
        "text": "Tool token attribution",
        "level": 2
      },
      {
        "id": "brl-conversion",
        "text": "BRL conversion",
        "level": 2
      }
    ]
  },
  "data-sources": {
    "id": "data-sources",
    "file": "docs/data-sources.md",
    "title": "Data sources",
    "group": "Start here",
    "html": "<h1 id=\"data-sources\">Data Sources</h1>\n<p>agentistics reads data exclusively from local files written by Claude Code. No data ever leaves your machine.</p>\n<h2 id=\"file-paths\">File paths</h2>\n<table>\n<thead>\n<tr>\n<th>Source</th>\n<th>Path</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Stats Cache</strong></td>\n<td><code>~/.claude/stats-cache.json</code></td>\n<td>Pre-computed aggregates: daily activity, tokens per model, streak</td>\n</tr>\n<tr>\n<td><strong>Session Meta</strong></td>\n<td><code>~/.claude/usage-data/session-meta/*.json</code></td>\n<td>Detailed per-session metadata — preferred source</td>\n</tr>\n<tr>\n<td><strong>Raw JSONL</strong></td>\n<td><code>~/.claude/projects/**/*.jsonl</code></td>\n<td>Raw conversation logs — used as fallback and for agent metrics</td>\n</tr>\n<tr>\n<td><strong>Local Git</strong></td>\n<td><code>git log --numstat</code></td>\n<td>Commits, files, and line counts within each session&#39;s time window</td>\n</tr>\n</tbody></table>\n<h2 id=\"data-flow\">Data flow</h2>\n<pre><code>~/.claude/\n  ├── stats-cache.json\n  ├── usage-data/session-meta/\n  └── projects/**/*.jsonl\n         ↓\n    server/data.ts  (buildApiResponse — main orchestrator)\n    server/agent-metrics.ts  (extractAgentMetrics — parses Agent tool_use)\n         ↓\n    /api/data  →  useData()  →  useDerivedStats()  →  React components\n</code></pre>\n<h2 id=\"session-sources\">Session sources</h2>\n<p>Sessions can come from three sources, indicated by the <code>_source</code> field:</p>\n<table>\n<thead>\n<tr>\n<th>Source</th>\n<th><code>_source</code></th>\n<th>Data quality</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Session-meta JSON</td>\n<td><code>&#39;meta&#39;</code></td>\n<td>Most complete — has all fields including cache tokens, tool breakdown, and agent metrics</td>\n</tr>\n<tr>\n<td>Direct JSONL parse</td>\n<td><code>&#39;jsonl&#39;</code></td>\n<td>Good — parsed on the fly from raw conversation files</td>\n</tr>\n<tr>\n<td>Subdirectory scan</td>\n<td><code>&#39;subdir&#39;</code></td>\n<td>Partial — limited fields, no git line counts or cache tokens</td>\n</tr>\n</tbody></table>\n<p><code>&#39;meta&#39;</code> sessions are always preferred. <code>&#39;jsonl&#39;</code> and <code>&#39;subdir&#39;</code> fill in sessions where metadata is not available.</p>\n<h2 id=\"jsonl-parsing-pipeline\">JSONL parsing pipeline</h2>\n<p>When session-meta is absent, <code>server/jsonl.ts</code> parses each <code>.jsonl</code> file line by line:</p>\n<pre><code>.jsonl file\n  ├── Extracts start_time and duration (first + last message timestamps)\n  ├── Counts user messages (excluding tool_result)\n  ├── Counts assistant messages (type: &#39;assistant&#39;)\n  ├── Maps tool_use → tool_counts { Bash: N, Read: N, Edit: N, ... }\n  ├── Attributes output tokens per tool (tool_output_tokens)\n  ├── Detects agent instruction file reads (CLAUDE.md, AGENTS.md, etc.)\n  ├── Extracts tokens from usage field (input, output, cacheRead, cacheWrite)\n  ├── Detects commits: regex /^git commit\\b/ in Bash inputs\n  ├── Detects pushes: regex /^git push\\b/ in Bash inputs\n  ├── Detects languages by file extension (Read, Edit, Write tool paths)\n  ├── Counts files Claude directly modified (unique paths from Edit/Write/MultiEdit calls)\n  ├── Counts tool errors (tool_result.is_error = true)\n  ├── Captures first prompt (first 200 chars)\n  ├── Records message hours (array 0–23)\n  └── Returns SessionMeta object (files_modified = max(git-based, claude-direct-edits))\n</code></pre>\n<h2 id=\"sessionmeta-structure\">SessionMeta structure</h2>\n<pre><code class=\"language-typescript\">interface SessionMeta {\n  session_id: string               // Session UUID\n  project_path: string             // Absolute project directory path\n  start_time: string               // ISO 8601\n  duration_minutes: number         // Total duration\n  user_message_count: number       // Actual user messages\n  assistant_message_count: number  // Model responses\n  tool_counts: Record&lt;string, number&gt;         // e.g.: { Bash: 12, Read: 8 }\n  tool_output_tokens: Record&lt;string, number&gt;  // Output tokens attributed per tool\n  agent_file_reads: Record&lt;string, number&gt;    // Agent instruction file reads\n  languages: string[]              // Detected coding languages\n  git_commits: number              // Commits via AI assistant\n  git_pushes: number               // Pushes via AI assistant\n  input_tokens: number             // Tokens sent to the model\n  output_tokens: number            // Tokens generated\n  cache_read_tokens: number        // Tokens served from prompt cache\n  cache_write_tokens: number       // Tokens written to prompt cache\n  lines_added: number              // Lines added (git)\n  lines_removed: number            // Lines removed (git)\n  files_modified: number           // Unique files modified\n  message_hours: number[]          // Message turn hours (0–23)\n  first_prompt: string             // First 200 chars of the first user message\n  tool_errors: number              // Total tool errors\n  uses_task_agent: boolean         // Used Task/Agent sub-agent tool\n  uses_mcp: boolean                // Used MCP tools\n  model?: string                   // Model ID (if available)\n  _source: &#39;meta&#39; | &#39;jsonl&#39; | &#39;subdir&#39;\n}\n</code></pre>\n<h2 id=\"stats-cache-limitations\">Stats cache limitations</h2>\n<p><code>~/.claude/stats-cache.json</code> is written by Claude Code and has <strong>no project-level granularity</strong>. Project breakdowns are computed by agentistics by summing individual sessions grouped by <code>project_path</code>.</p>\n<p><code>dailyModelTokens</code> in the stats cache stores per-day/per-model totals but not the input/output split per day. When filtering by date, the global model usage proportions (input% / output%) are used as an approximation for the split.</p>\n<h2 id=\"agent-metrics\">Agent metrics</h2>\n<p>Agent metrics are extracted from raw JSONL by <code>server/agent-metrics.ts</code>. They are only available for sessions whose JSONL files are accessible (<code>_source: &#39;meta&#39;</code>-only sessions may have incomplete agent data if the underlying JSONL was removed).</p>\n<h3 id=\"available-per-agent-invocation\">Available per Agent invocation</h3>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Source in JSONL</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>agentType</code></td>\n<td><code>toolUseResult.agentType</code></td>\n</tr>\n<tr>\n<td><code>description</code></td>\n<td><code>tool_use.input.description</code></td>\n</tr>\n<tr>\n<td><code>totalTokens</code></td>\n<td><code>toolUseResult.totalTokens</code></td>\n</tr>\n<tr>\n<td><code>totalDurationMs</code></td>\n<td><code>toolUseResult.totalDurationMs</code></td>\n</tr>\n<tr>\n<td><code>totalToolUseCount</code></td>\n<td><code>toolUseResult.totalToolUseCount</code></td>\n</tr>\n<tr>\n<td><code>inputTokens / outputTokens / cacheReadTokens / cacheWriteTokens</code></td>\n<td><code>toolUseResult.usage.*</code></td>\n</tr>\n<tr>\n<td><code>toolStats</code></td>\n<td><code>toolUseResult.toolStats</code></td>\n</tr>\n<tr>\n<td><code>costUSD</code></td>\n<td>Calculated via <code>calcCost()</code></td>\n</tr>\n<tr>\n<td><code>status</code></td>\n<td><code>toolUseResult.status</code></td>\n</tr>\n</tbody></table>\n<h3 id=\"what-is-not-tracked-for-skills-and-tasks\">What is NOT tracked for Skills and Tasks</h3>\n<ul>\n<li><strong>Skills</strong> (<code>/commit</code>, <code>/review-pr</code>, etc.) do not produce individual <code>tool_use</code> events in JSONL — only a <code>skill_listing</code> attachment appears. Skill invocations can only be inferred indirectly from subsequent tool calls.</li>\n<li><strong>Tasks</strong> (<code>TaskCreate</code> / <code>TaskUpdate</code>) have subject/description/status but no token or duration breakdown.</li>\n</ul>\n<h2 id=\"git-data\">Git data</h2>\n<p>Git stats are computed by running:</p>\n<pre><code class=\"language-bash\">git -C &lt;project_path&gt; log --numstat --after=&quot;&lt;session_start&gt;&quot; --before=&quot;&lt;session_end&gt;&quot;\n</code></pre>\n<p>This produces: commits count, files modified, lines added, lines removed.</p>\n<p>Git stats are only available for sessions whose <code>project_path</code> is an accessible git repository at query time.</p>\n<h3 id=\"workspace-folder-fallback\">Workspace folder fallback</h3>\n<p><code>getProjectGitStats</code> in <code>server/git.ts</code> first tries the project path as a single git repo. If that fails (the path is not itself a git repo), it falls back to scanning one level of subdirectories and aggregating stats across all git repos found there. This handles workspace folders (e.g. <code>~/zuke</code>) that contain multiple sub-repos.</p>\n<h3 id=\"files_modified-counting\">files_modified counting</h3>\n<p><code>server/jsonl.ts</code> tracks a <code>claudeFilesModified</code> Set of unique file paths from Edit, Write, and MultiEdit tool calls. The final <code>files_modified</code> value is <code>Math.max(gitFileStats.filesModified, claudeFilesModified.size)</code>, whichever is higher. This ensures files Claude edited in non-git directories are still counted.</p>\n<p>The FILES KPI in <code>useData.ts</code> always prefers session-level <code>files_modified</code> (which captures direct Claude edits). It only falls back to project-level <code>git_stats.files_modified</code> when sessions show 0.</p>\n",
    "headings": [
      {
        "id": "data-sources",
        "text": "Data Sources",
        "level": 1
      },
      {
        "id": "file-paths",
        "text": "File paths",
        "level": 2
      },
      {
        "id": "data-flow",
        "text": "Data flow",
        "level": 2
      },
      {
        "id": "session-sources",
        "text": "Session sources",
        "level": 2
      },
      {
        "id": "jsonl-parsing-pipeline",
        "text": "JSONL parsing pipeline",
        "level": 2
      },
      {
        "id": "sessionmeta-structure",
        "text": "SessionMeta structure",
        "level": 2
      },
      {
        "id": "stats-cache-limitations",
        "text": "Stats cache limitations",
        "level": 2
      },
      {
        "id": "agent-metrics",
        "text": "Agent metrics",
        "level": 2
      },
      {
        "id": "available-per-agent-invocation",
        "text": "Available per Agent invocation",
        "level": 3
      },
      {
        "id": "what-is-not-tracked-for-skills-and-tasks",
        "text": "What is NOT tracked for Skills and Tasks",
        "level": 3
      },
      {
        "id": "git-data",
        "text": "Git data",
        "level": 2
      },
      {
        "id": "workspace-folder-fallback",
        "text": "Workspace folder fallback",
        "level": 3
      },
      {
        "id": "files_modified-counting",
        "text": "files_modified counting",
        "level": 3
      }
    ]
  },
  "harness-contract": {
    "id": "harness-contract",
    "file": "docs/harness-contract.md",
    "title": "Harness contract",
    "group": "Harnesses",
    "html": "<h1 id=\"harness-contract\">Harness contract</h1>\n<p>Every harness Agentistics tracks must satisfy this contract. It is the <strong>project-wide definition of\neach metric</strong> — not a per-adapter choice. Two adapters computing &quot;duration&quot; differently is not a\ndifference in the harnesses, it is a bug in one of them.</p>\n<p><code>CLAUDE.md</code> (&quot;Adding a harness — the complete checklist&quot;) lists the <em>mechanical</em> steps: the\n<code>HarnessId</code>, the capability record, the sort record, the adapter pair, live sessions, the frontend\nlabels, the docs. <strong>This file defines what the numbers must MEAN.</strong> Do both.</p>\n<p>The governing principle, stated once:</p>\n<blockquote>\n<p><strong>Report what the harness recorded. When it recorded nothing, report nothing.</strong></p>\n<p>A confident wrong number is worse than a visible gap. <code>N/A</code> and <code>—</code> are correct answers. A\nplausible-looking default, an inferred threshold, or a fallback rate applied silently is not.</p>\n</blockquote>\n<hr>\n<h2 id=\"1-time-duration_minutes-vs-active_minutes\">1. Time — `duration_minutes` vs `active_minutes`</h2>\n<p>Two fields, and they answer different questions. Both are required.</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Meaning</th>\n<th>Rule</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>duration_minutes</code></td>\n<td><strong>Wall clock.</strong> Last event − first event.</td>\n<td>Always populated.</td>\n</tr>\n<tr>\n<td><code>active_minutes</code></td>\n<td><strong>Time actually worked.</strong> Σ per-turn duration.</td>\n<td>Populated whenever the transcript carries usable timing; <code>undefined</code> otherwise.</td>\n</tr>\n</tbody></table>\n<h3 id=\"why-both\">Why both</h3>\n<p>A session reopened across three weeks has a wall clock of ~500h. That is a true statement about\nwhen the session was open and a useless statement about how long it was worked on. Ranking\n&quot;longest session&quot; by it crowns whichever session merely stayed open the longest.</p>\n<p>Active time alone would be equally misleading in the other direction — it hides that the work was\nspread across weeks. <strong>Every surface shows both</strong>, active as the headline and elapsed as its\nqualifier: <code>3h 12m ativo · 958h decorrido</code>.</p>\n<h3 id=\"the-rule-one-implementation-computeactivetime\">The rule — one implementation, `computeActiveTime()`</h3>\n<p><code>packages/core/src/activeTime.ts</code>. <strong>Adapters must not compute time themselves</strong>; they emit\n<code>TurnEvent[]</code> and call <code>activeMinutesOf()</code>. A turn runs from a human prompt until the harness stops\nworking on it.</p>\n<ol>\n<li><strong>If the harness measured the turn itself, that number wins.</strong> Set <code>measuredMs</code>.</li>\n<li><strong>Otherwise reconstruct from event timestamps.</strong> This is the same quantity, not an approximation\nof a different one — validated against Claude Code&#39;s own <code>turn_duration</code> over 1326 real turns:\nmedian difference <strong>0.0s</strong>, within 5s on 63%.</li>\n<li><strong>If the harness writes an explicit end-of-turn</strong> (abort, shutdown), set <code>turnEnd</code>. Without it an\naborted turn stays open to the last line of the file — a real Copilot session aborted at 20:13\nwhose file ends at 23:34 reported 3.4h of &quot;active&quot; work.</li>\n<li><strong>If there is no usable timing at all, the result is <code>undefined</code>.</strong> The UI shows <code>—</code>.</li>\n</ol>\n<h3 id=\"what-this-deliberately-does-not-do\">What this deliberately does NOT do</h3>\n<p>It does not subtract &quot;the user walked away mid-turn.&quot; A turn blocked on a permission prompt\novernight is measured as ~8h <strong>by the harness itself</strong>. Cutting it would need an arbitrary idle\ncutoff — a made-up number inside the metric that exists to stop reporting made-up numbers.</p>\n<p>The gap that IS excluded — harness finishes → human&#39;s next prompt — is excluded because it is a\nreal, observable boundary, not a threshold.</p>\n<p><strong>Do not add an idle-gap heuristic.</strong> If a future harness genuinely records user presence (an\nexplicit AFK / idle event), that is a measurement and may close a turn via <code>turnEnd</code>. An inferred\none may not.</p>\n<h3 id=\"where-each-harnesss-turn-time-comes-from\">Where each harness's turn time comes from</h3>\n<table>\n<thead>\n<tr>\n<th>Harness</th>\n<th>Source</th>\n<th>Measured?</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>claude</td>\n<td><code>{&quot;type&quot;:&quot;system&quot;,&quot;subtype&quot;:&quot;turn_duration&quot;,&quot;durationMs&quot;:N}</code></td>\n<td>yes, when present; timestamps otherwise</td>\n</tr>\n<tr>\n<td>codex</td>\n<td><code>task_complete.duration_ms</code></td>\n<td>yes</td>\n</tr>\n<tr>\n<td>copilot</td>\n<td><code>assistant.turn_start</code> → <code>assistant.turn_end</code>; <code>abort</code> / <code>session.shutdown</code> close an open turn</td>\n<td>yes (bracket)</td>\n</tr>\n<tr>\n<td>gemini</td>\n<td>message timestamps</td>\n<td>reconstructed</td>\n</tr>\n<tr>\n<td>antigravity</td>\n<td>step <code>created_at</code> timestamps</td>\n<td>reconstructed</td>\n</tr>\n<tr>\n<td>kimi</td>\n<td>wire <code>time</code> field; subagent wires merged and <strong>sorted</strong> before use</td>\n<td>reconstructed</td>\n</tr>\n</tbody></table>\n<p><strong>Sub-agents never add time.</strong> A subagent runs <em>inside</em> the parent turn that dispatched it — its\nspan is already inside the parent&#39;s. Antigravity&#39;s <code>mergeAntigravityChild</code> keeps the parent&#39;s\n<code>active_minutes</code>; Kimi sorts every agent&#39;s events into one chronological stream. Summing them\ndouble-counts the same wall clock.</p>\n<hr>\n<h2 id=\"2-cost-and-pricing\">2. Cost and pricing</h2>\n<p>Covered in depth in <code>CLAUDE.md</code> (§ &quot;Pricing — three layered sources&quot;). The contract:</p>\n<ul>\n<li><strong>Report the bare model id.</strong> Strip any <code>provider/</code> prefix (<code>kimi-parse.ts</code> does this). The\nshared table prices it; you almost never add pricing code.</li>\n<li><strong>Cost is <code>calcCost()</code>, never an inline calculation.</strong> One implementation, <code>@agentistics/core</code>.</li>\n<li><strong>Never guess a rate.</strong> A wrong price is invisible; a missing one is visible — Settings → Pricing\nlists any model this machine used that no source can price. Add a rate only with a <strong>verified,\ndated source comment</strong>.</li>\n<li>A harness that routes to other vendors (Kimi, Antigravity) reports <strong>that vendor&#39;s</strong> model.\nA provider is a billing entity; a harness is not.</li>\n</ul>\n<h2 id=\"3-tokens\">3. Tokens</h2>\n<ul>\n<li>Find the <strong>one</strong> place the harness records usage and count only that. Both Kimi and Codex\npublish the same usage twice in different envelopes — summing both doubles every figure.</li>\n<li>Know whether records are <strong>cumulative</strong> (Codex: last one wins) or <strong>per-turn increments</strong>\n(Kimi: sum). Getting this backwards is silent.</li>\n<li>Split cached from fresh input. Codex&#39;s <code>input_tokens</code> <strong>includes</strong> the cached portion; store\n<code>total − cached</code> in <code>input_tokens</code> and the cached part in <code>cache_read_input_tokens</code>.</li>\n<li>No cache-write counter → leave <code>cache_creation_input_tokens</code> at 0, and say so in the capability\ncomment.</li>\n</ul>\n<h2 id=\"4-capabilities-na-vs-a-real-0\">4. Capabilities — `N/A` vs a real `0`</h2>\n<p><code>HARNESS_CAPABILITIES</code> (<code>packages/core/src/types.ts</code>) is a <code>Record&lt;HarnessId, …&gt;</code>, so the build\nfails until a new harness declares every flag. <strong>Be honest.</strong> A flag set <code>true</code> for something the\nharness cannot produce renders a confident <code>0</code>, which is exactly the failure this mechanism exists\nto prevent. <code>activeTime: false</code> means the UI shows only wall-clock elapsed.</p>\n<h2 id=\"5-filters-and-aggregation\">5. Filters and aggregation</h2>\n<ul>\n<li><strong><code>stats-cache.json</code> is Claude-only.</strong> Never aggregate another harness from it. Non-Claude\ntotals come from per-session sums, everywhere — dashboard, Compare page, team central.</li>\n<li><strong>Never hardcode a harness list.</strong> <code>HARNESS_ORDER</code> derives from a <code>Record</code>, because TypeScript\naccepts an array literal with a member missing — that silently dropped a harness from the Compare\npage, the filter bar, the data-source list and the consolidate store while the build stayed green.</li>\n<li><strong>A filter must not silently fall back to a fraction of its own scope.</strong> A member&#39;s deep history\nexists only in the stats caches; <code>resolveMachineCacheScope()</code> returns <code>null</code> — &quot;fall back to the\nper-session sum&quot; — only when the caches cannot serve the scope <em>exactly</em>.</li>\n<li>Every session-level field you add must survive the whole pipeline: parser → consolidate store →\nteam uploader → Mongo → <code>loadSessionMetas</code>. <code>loadSessionMetas</code> builds <code>SessionMeta</code> <strong>field by\nfield</strong>; a field not listed there is dropped on the way back in, silently.</li>\n</ul>\n<h2 id=\"6-timestamps\">6. Timestamps</h2>\n<p>Bucket activity hours on the <strong>local</strong> clock (<code>getHours()</code>), like every existing adapter. Reading a\nUTC timestamp as local put the peak-usage chart off by hours for four harnesses at once.</p>\n<h2 id=\"7-purity-and-failure\">7. Purity and failure</h2>\n<ul>\n<li>Split each adapter in two: <code>&lt;id&gt;.ts</code> does I/O, <code>&lt;id&gt;-parse.ts</code> is <strong>pure</strong> and takes strings.\nOnly the pure half is unit-testable, and only it is easy to reason about.</li>\n<li><strong>A malformed, locked or missing input yields empty data, never a throw.</strong> One corrupt file must\nnot take down the whole scan. Antigravity&#39;s protobuf reader returns <code>null</code> on junk; a locked\nSQLite DB degrades to zero tokens.</li>\n<li>Drop bootstrap/stub sessions with no genuine content (Gemini writes many). A harness appears in\nthe selector only when it contributes a real session.</li>\n</ul>\n<h2 id=\"8-verification-before-claiming-it-works\">8. Verification before claiming it works</h2>\n<p>Run the parser over <strong>real files on disk</strong> and eyeball the output — every rule above was written\nafter real data disproved something that looked right in code:</p>\n<pre><code class=\"language-bash\">bun test                    # pure-function unit tests\nbun tsc --noEmit\n</code></pre>\n<p>Then compare against the harness&#39;s own numbers where it publishes any. For active time, the check\nthat matters is: <em>does the reconstruction agree with the harness&#39;s own measurement where both\nexist?</em> If a future harness publishes durations, run that comparison before trusting the\nreconstruction for the turns where it doesn&#39;t.</p>\n",
    "headings": [
      {
        "id": "harness-contract",
        "text": "Harness contract",
        "level": 1
      },
      {
        "id": "1-time-duration_minutes-vs-active_minutes",
        "text": "1. Time — `duration_minutes` vs `active_minutes`",
        "level": 2
      },
      {
        "id": "why-both",
        "text": "Why both",
        "level": 3
      },
      {
        "id": "the-rule-one-implementation-computeactivetime",
        "text": "The rule — one implementation, `computeActiveTime()`",
        "level": 3
      },
      {
        "id": "what-this-deliberately-does-not-do",
        "text": "What this deliberately does NOT do",
        "level": 3
      },
      {
        "id": "where-each-harnesss-turn-time-comes-from",
        "text": "Where each harness's turn time comes from",
        "level": 3
      },
      {
        "id": "2-cost-and-pricing",
        "text": "2. Cost and pricing",
        "level": 2
      },
      {
        "id": "3-tokens",
        "text": "3. Tokens",
        "level": 2
      },
      {
        "id": "4-capabilities-na-vs-a-real-0",
        "text": "4. Capabilities — `N/A` vs a real `0`",
        "level": 2
      },
      {
        "id": "5-filters-and-aggregation",
        "text": "5. Filters and aggregation",
        "level": 2
      },
      {
        "id": "6-timestamps",
        "text": "6. Timestamps",
        "level": 2
      },
      {
        "id": "7-purity-and-failure",
        "text": "7. Purity and failure",
        "level": 2
      },
      {
        "id": "8-verification-before-claiming-it-works",
        "text": "8. Verification before claiming it works",
        "level": 2
      }
    ]
  },
  "architecture": {
    "id": "architecture",
    "file": "docs/architecture.md",
    "title": "Architecture",
    "group": "Team Mode",
    "html": "<h1 id=\"architecture\">Architecture</h1>\n<h2 id=\"monorepo-layout\">Monorepo layout</h2>\n<pre><code>agentistics/\n├── packages/\n│   ├── core/                         # @agentistics/core — shared, publishable\n│   │   └── src/\n│   │       ├── types.ts              # All shared types + MODEL_PRICING + calcCost()\n│   │       ├── format.ts             # fmt(), fmtCost(), fmtDuration(), fmtFull()\n│   │       ├── chatUtils.ts          # formatToolName, TOOL_LABELS, extractNavLinks\n│   │       ├── i18n.ts               # PT/EN translations, t()\n│   │       ├── otel.ts               # OpenTelemetry metric definitions\n│   │       └── index.ts              # Barrel re-export\n│   │\n│   ├── server/                       # @agentistics/server — CLI + HTTP server\n│   │   ├── bin/\n│   │   │   └── cli.ts                # Binary entry: agentop setup|server|tui|watch|central|member|autostart|upgrade|check-update\n│   │   ├── server/\n│   │   │   ├── index.ts              # Bun HTTP server — thin entry, delegates to modules\n│   │   │   ├── config.ts             # Path constants + PORT + team env vars (TEAM_*, CENTRAL_USER)\n│   │   │   ├── env-config.ts         # .env.config read/write/backup/restore\n│   │   │   ├── utils.ts              # Shared FS helpers (createLimiter, safeRead*)\n│   │   │   ├── git.ts                # Git stats via git log --numstat; workspace fallback scans subdirectories\n│   │   │   ├── jsonl.ts              # JSONL session parser\n│   │   │   ├── health.ts             # Health checks + warnings\n│   │   │   ├── rates.ts              # Pricing scraper + BRL rate cache\n│   │   │   ├── sse.ts                # SSE clients, chokidar watcher, serveStatic, broadcastNotification, triggerSseNotification\n│   │   │   ├── data.ts               # Main orchestrator (buildApiResponse)\n│   │   │   ├── agent-metrics.ts      # Agent tool_use metrics parser\n│   │   │   ├── chat-tty.ts           # Nay chat: ensureNayChat, streamViaClaude\n│   │   │   ├── otel-watcher.ts       # Chokidar + OTLP metrics export daemon\n│   │   │   ├── preferences.ts        # ~/.agentistics preferences incl. team config (mode/endpoint/token/user)\n│   │   │   ├── version.ts            # getVersionInfo() — current vs latest, drives update banners/notifications\n│   │   │   ├── autostart.ts          # systemd user service + loginctl linger + ~/.bashrc update-check hook\n│   │   │   ├── cli-setup.ts          # `agentop setup` wizard (solo/central/member + autostart offer)\n│   │   │   ├── cli-central.ts        # `agentop central …` — thin wrapper over central.sh\n│   │   │   ├── cli-member.ts         # `agentop member connect|leave|status` (whoami-verified, no browser)\n│   │   │   └── (team mode, see below)\n│   │   │       ├── team-tokens.ts        # mint / rotate / revoke / validate tokens (sha256 hashes only)\n│   │   │       ├── team-store.ts         # Mongo team-session doc shape (org:memberId:harness:sessionId)\n│   │   │       ├── team-stats.ts         # per-member statsCache store (exact Claude totals)\n│   │   │       ├── team-ingest.ts        # POST /api/team/ingest → upsert + SSE-on-ingest (real-time central)\n│   │   │       ├── team-source.ts        # central-side read of team sessions for buildApiResponse\n│   │   │       ├── team-admin.ts         # members-panel admin routes (list/rename/rotate/revoke/policy)\n│   │   │       ├── team-uploader.ts      # member→central push: sent-state, sync-signature reconcile, push-on-change, auto-reset on revoke\n│   │   │       ├── team-watch.ts         # central: watch the team collection → SSE refresh\n│   │   │       ├── team-agent.ts         # central WebSocket registry: presence signals, ping/pong latency, on-demand chat fetch\n│   │   │       ├── team-agent-client.ts  # member side of the reverse-channel WebSocket\n│   │   │       ├── team-presence.ts      # computePresence() — WS-authoritative online/offline + latency\n│   │   │       └── central-config.ts     # Mongo central config: instanceId, pushIntervalSec, includeOfflineData\n│   │   └── scripts/\n│   │       ├── embed-dist.ts         # Embeds packages/web/dist/ → embedded-dist.generated.ts\n│   │       └── ensure-type-stub.ts   # Creates type stub for CI (before full build)\n│   │\n│   ├── web/                          # @agentistics/web — React + Vite frontend\n│   │   ├── src/\n│   │   │   ├── App.tsx               # Router, global state, header\n│   │   │   ├── pages/\n│   │   │   │   ├── HomePage.tsx      # Main dashboard (KPIs, charts, sessions)\n│   │   │   │   ├── CustomPage.tsx    # Custom layout builder (/custom)\n│   │   │   │   ├── CostsPage.tsx     # Cost deep-dive\n│   │   │   │   ├── ProjectsPage.tsx  # Projects overview\n│   │   │   │   └── ToolsPage.tsx     # Tool metrics breakdown\n│   │   │   ├── hooks/\n│   │   │   │   ├── useData.ts        # Fetches /api/data + SSE + useDerivedStats()\n│   │   │   │   └── useCustomLayout.ts # Layout state + persistence\n│   │   │   ├── components/           # UI components (charts, cards, modals)\n│   │   │   │   ├── TtyChat.tsx       # Nay chat panel (FAB + floating panel)\n│   │   │   │   ├── PreferencesModal.tsx # Unified Settings modal (Preferences/Live/Install tabs)\n│   │   │   │   ├── TeamLogin.tsx     # Central dashboard password login\n│   │   │   │   ├── TeamMembers.tsx   # Members panel: mint/rotate/revoke/rename + presence column\n│   │   │   │   ├── TeamSettings.tsx  # Central Settings → Team (interval/express, offline-data policy)\n│   │   │   │   ├── DeployCentral.tsx # In-app central deploy/help panel\n│   │   │   │   ├── PresenceFilter.tsx        # Central filter: online/offline members\n│   │   │   │   ├── MemberConnectionStatus.tsx # Member-side connected/reconnecting pill\n│   │   │   │   ├── NotificationToasts.tsx    # Auto-dismiss animated toasts\n│   │   │   │   ├── NotificationBell.tsx      # Header bell: history + unread badge\n│   │   │   │   ├── UpdateModal.tsx   # Mode-aware upgrade instructions modal\n│   │   │   │   └── ...\n│   │   │   ├── lib/\n│   │   │   │   ├── app-context.ts    # AppContext interface (React context shape)\n│   │   │   │   ├── componentCatalog.tsx # Catalog of custom layout components\n│   │   │   │   ├── chatModels.ts     # CHAT_MODELS, DEFAULT_CHAT_MODEL\n│   │   │   │   ├── chatSounds.ts     # CHAT_SOUNDS — 5 Web Audio API notification sounds\n│   │   │   │   └── notifications.ts  # Notification store + render-time pt/en i18n (NOTIFICATION_TEXT by code)\n│   │   │   └── tui/\n│   │   │       └── index.ts          # Terminal TUI (standalone, no browser)\n│   │   ├── public/\n│   │   │   ├── icons/                # PWA icons (icon-192.png, icon-512.png)\n│   │   │   └── ...                   # logo, favicon, etc.\n│   │   ├── index.html\n│   │   └── vite.config.ts            # Vite config with vite-plugin-pwa (devOptions.enabled: true)\n│   │\n│   ├── mcp/                          # @agentistics/mcp — MCP server, publishable to npm\n│   │   └── agentistics-mcp.ts        # stdio transport, 12 tools, imports @agentistics/core\n│   │\n│   └── desktop/                      # Tauri v2 Windows installer\n│       ├── src/main.rs               # Spawns agentop.exe sidecar, polls health, onboarding\n│       ├── ui/index.html             # Loading screen + first-run onboarding UI\n│       ├── capabilities/default.json # Tauri v2 permission declarations\n│       ├── tauri.conf.json           # Window config, CSP, sidecar declaration\n│       └── Cargo.toml\n│\n├── docs/                             # Extended documentation\n├── grafana/                          # Pre-built Grafana dashboard JSON\n├── central.sh                        # Team central lifecycle (docker compose): up/init/down/logs/status/restart/pull\n├── docker-compose.yml                # Central service (app + Mongo, Mongo NOT published to the host)\n├── central.env                       # Generated by `central.sh init` (gitignored — secrets, chmod 600)\n├── .env.config                       # Committed port defaults (PORT, VITE_PORT)\n├── package.json                      # Root: workspaces + orchestration scripts\n└── tsconfig.json                     # Root: paths alias for @agentistics/core\n</code></pre>\n<h2 id=\"request-lifecycle\">Request lifecycle</h2>\n<pre><code>Browser → GET /api/data\n  → packages/server/server/index.ts (Bun.serve)\n    → server/data.ts (buildApiResponse)\n      ├── server/jsonl.ts       (parse raw JSONL sessions)\n      ├── server/agent-metrics.ts (extract agent invocations)\n      ├── server/git.ts         (git stats per project)\n      └── server/health.ts      (warnings)\n    → JSON response\n\nBrowser → GET /api/events  (SSE)\n  → server/sse.ts (sseClients stream)\n    chokidar watches ~/.claude/ → pushes &quot;update&quot; event on change\n  → browser calls /api/data again\n\nBrowser → POST /api/chat-tty  (Nay)\n  → server/chat-tty.ts (streamViaClaude)\n    → Bun.spawn([&#39;claude&#39;, &#39;--print&#39;, &#39;--output-format&#39;, &#39;stream-json&#39;])\n      → claude reads NAY_CHAT_DIR/CLAUDE.md + .claude/settings.json\n      → MCP tools → GET http://localhost:47291/api/data\n    → stream-json chunks → SSE stream → TtyChat.tsx\n</code></pre>\n<h2 id=\"team-mode\">Team Mode</h2>\n<p>Team Mode lets one machine (&quot;central&quot;) aggregate coding-assistant usage metrics from many machines (&quot;members&quot;). Members push <strong>computed metrics only</strong> — session/agent/token/cost aggregates plus their statsCache — <strong>never chat transcripts</strong> (raw chat is fetched on demand over a reverse WebSocket, not stored centrally). Data lives in Mongo, which is <strong>not published to the host</strong> (reachable only inside the compose network).</p>\n<h3 id=\"roles\">Roles</h3>\n<p>Every machine picks one role, persisted at <code>preferences.team.mode</code>:</p>\n<ul>\n<li><strong>solo</strong> — local only, nothing leaves the machine (the default).</li>\n<li><strong>central</strong> — the aggregator. Runs as a Docker service via <code>central.sh</code> (default port <code>48080</code>, distinct from a solo/dev server&#39;s <code>47291</code>). Serves the team dashboard behind a password.</li>\n<li><strong>member</strong> — pushes its computed metrics to a central&#39;s <code>/api/team/ingest</code>.</li>\n</ul>\n<h3 id=\"centralsh-agentop-central\">central.sh + `agentop central`</h3>\n<p><code>central.sh</code> (repo root) wraps <code>docker compose</code> with the project name and env file pre-set; <code>agentop central &lt;up|init|down|logs|status|restart|pull&gt;</code> shells out to it (<code>server/cli-central.ts</code>, stdio inherited so interactive prompts and log streaming work). Key subcommands:</p>\n<ul>\n<li><strong><code>init</code></strong> — interactive: prompts each value, auto-generates the secrets with <code>openssl</code>, detects the Tailscale IP as a suggestion, writes <code>central.env</code> (<code>chmod 600</code>).</li>\n<li><strong><code>up</code></strong> — ensures <code>central.env</code> exists (offers <code>init</code>), then builds and <code>--force-recreate</code>s the containers.</li>\n<li><strong><code>down</code></strong> — stops the containers but <strong>keeps the data volume</strong> (only <code>down -v</code> wipes it, which mints a new <code>instanceId</code> — see reconciliation below).</li>\n</ul>\n<p><code>central.env</code> variables: <code>APP_PORT</code> (default <code>48080</code>), <code>BIND_IP</code> (default <code>127.0.0.1</code> — this host only, which is all a tunnel or reverse proxy needs; set <code>0.0.0.0</code> for the LAN or a Tailscale IP for a private tailnet), <code>AGENTISTICS_TEAM_SESSION_SECRET</code> (HMAC cookie key — <strong>never</strong> derived from the password; leave empty and a random one is generated and persisted), <code>AGENTISTICS_EXPOSURE</code> (<code>local</code>/<code>lan</code>/<code>public</code> — see <a href=\"https://github.com/blpsoares/agentistics/blob/main/exposure.md\" target=\"_blank\" rel=\"noopener\">exposure.md</a>), <code>AGENTISTICS_TRUST_PROXY</code>, <code>AGENTISTICS_ALLOWED_ORIGINS</code>. The full security model — threat model, trust boundaries, request pipeline and the limits of each control — is in <a href=\"https://github.com/blpsoares/agentistics/blob/main/security.md\" target=\"_blank\" rel=\"noopener\">security.md</a>, <code>AGENTISTICS_TEAM_ORG</code>, <code>AGENTISTICS_TEAM_INGEST_TOKEN</code> (optional shared secret), <code>AGENTISTICS_CENTRAL_USER</code> (set when the central also contributes its own machine&#39;s data).</p>\n<h3 id=\"restart-policy-both-services-survive-a-host-reboot\">Restart policy — both services survive a host reboot</h3>\n<p>Both the <code>app</code> <strong>and</strong> the <code>mongo</code> services carry <code>restart: unless-stopped</code> (in <code>docker-compose.yml</code> and the standalone compose materialized by <code>cli-central.ts</code>). Without it on <code>mongo</code>, a host reboot that crashes Mongo (e.g. an OOM kill under WSL) leaves the <code>app</code> back up but pointed at a dead database — the dashboard then shows <strong>zero members</strong> even though the data volume (<code>mongo_data</code>) is intact.</p>\n<p><strong>Migrating an already-running central</strong> (the policy only takes effect once the container is recreated):</p>\n<ul>\n<li>After upgrading, run <code>agentop central up</code> once — it re-materializes the compose and <code>--force-recreate</code>s Mongo with the policy baked in.</li>\n<li>Or apply it live, without any recreate or downtime: <code>docker update --restart unless-stopped team-mode-mongo-1</code>.</li>\n</ul>\n<h3 id=\"member-identity\">Member identity</h3>\n<p>A member does <strong>not</strong> name itself — the display name is set by the central when it mints the token, and the member resolves it via <code>GET /api/team/whoami</code> (<code>server/cli-member.ts</code>, <code>memberConnect</code>). Sessions are keyed centrally by a stable <code>memberId</code> (the token&#39;s sha256 hash), so renaming a member keeps history. <code>agentop member connect</code> never writes a half-config: it only persists <code>preferences.team</code> after whoami accepts the token.</p>\n<h3 id=\"push-model-central-owned-interval-push-on-change\">Push model — central-owned interval + push-on-change</h3>\n<p>The <strong>central owns the cadence</strong> (<code>server/central-config.ts</code>, <code>pushIntervalSec</code>; normal floor 15s, default 30s, express down to 5s via <code>EXPRESS_MIN_SEC</code>). Members fetch it from <code>GET /api/team/policy</code> each cycle and can only follow it — there is no member-side override that goes faster. On top of the periodic timer, <code>server/team-uploader.ts</code> also does <strong>push-on-change</strong>: the file watcher calls <code>notifyDataChanged()</code>, which schedules a debounced push (coalesces bursts, never sooner than the central&#39;s interval since the last success). Members push their <strong>supplemented</strong> statsCache (the one the local dashboard shows, gap-filled past the stale <code>lastComputedDate</code>), not the raw <code>~/.claude/stats-cache.json</code>, so central totals match the member&#39;s own dashboard exactly.</p>\n<h3 id=\"real-time-central\">Real-time central</h3>\n<p>A member push lands in <code>server/team-ingest.ts</code>, which upserts the sessions/stats and then calls <code>triggerSseNotification()</code> — the central&#39;s dashboards refresh live over SSE without polling. This is why the &quot;Live&quot; toggle is <strong>hidden on a central</strong>. <code>server/team-watch.ts</code> also watches the team collection as a fallback SSE source.</p>\n<h3 id=\"presence-websocket-authoritative\">Presence — WebSocket-authoritative</h3>\n<p>Presence is computed by <code>server/team-presence.ts</code> from the reverse-channel WebSocket registry in <code>server/team-agent.ts</code>:</p>\n<ul>\n<li>A member is <strong>online</strong> while its WebSocket is live (source of truth). Killing the app drops the socket → <strong>offline within ~8s</strong> (<code>SOCKET_GRACE_MS</code>, absorbs brief reconnects).</li>\n<li>Once a member has <em>ever</em> held a socket this run, the socket signal is trusted; a <strong>heartbeat window</strong> (<code>server/team-presence.ts</code>) is only the fallback for pure-HTTP members that never opened a socket.</li>\n<li><strong>Latency</strong> comes from WebSocket ping/pong RTT (<code>PING_INTERVAL_MS</code>; a socket missing <code>MAX_MISSED_PONGS</code> pings is force-closed so a hard-killed machine still flips offline).</li>\n<li>The central admin gets a &quot;machine connected&quot; notification (throttled per member).</li>\n</ul>\n<p>The members panel (<code>TeamMembers.tsx</code>, central Settings → Team) can <strong>mint</strong>, <strong>rotate</strong> (new credential that migrates the member&#39;s sessions+stats to the new identity, preserving history), <strong>revoke</strong> (confirmation modal; cascade-deletes that member&#39;s data), and <strong>rename</strong>. There is a per-central &quot;show offline members&#39; data&quot; policy (<code>includeOfflineData</code>) and filters for members / harnesses / projects / presence.</p>\n<h3 id=\"auto-reconciliation-self-healing-sync\">Auto-reconciliation (self-healing sync)</h3>\n<p><code>server/team-uploader.ts</code> fingerprints the push target as <code>sha256(endpoint \\0 token \\0 instanceId)</code> and stores it in the sync file. When the fingerprint changes — the central DB was wiped (<code>down -v</code> → new <code>instanceId</code>), the token was revoked and re-added, or the endpoint changed — the member clears its sent-state and <strong>re-pushes its full history</strong> on the next cycle (idempotent upserts, so no double-counting). No manual <code>team-sent.json</code> deletion. A persistent 401/403 (revoked token) trips <code>handleAuthError</code> after a couple of cycles: the member <strong>auto-resets to solo</strong> and emits a &quot;removed from central&quot; notification. A <code>null</code> instanceId (old/unreachable central) never triggers a spurious reset.</p>\n<h3 id=\"notifications\">Notifications</h3>\n<p><code>packages/web/src/lib/notifications.ts</code> is a small external store rendered by <code>NotificationToasts.tsx</code> (auto-dismiss, animated) and <code>NotificationBell.tsx</code> (history + unread badge). Notifications carry a <code>code</code> (+ <code>meta</code>) and are localized <strong>at render time</strong> (<code>NOTIFICATION_TEXT</code>, pt/en) so they follow the language toggle. The server emits them via <code>broadcastNotification()</code> (SSE). Fired on member auth/connection errors, &quot;removed from central&quot;, &quot;machine connected&quot;, and &quot;update available&quot;.</p>\n<h3 id=\"per-connection-repository-sharing\">Per-connection repository sharing</h3>\n<p>A member can push to more than one central (<code>preferences.team.connections: TeamConnection[]</code>) and can restrict what each connection receives <strong>per connection</strong> — a repo or project hidden from central A can still go to central B. Rules apply across <strong>two dimensions</strong> — repository (<code>git_remote</code>) and project (<code>project_path</code>) — and each connection picks one of <strong>two modes</strong>: <code>denylist</code> (&quot;share everything except…&quot;, the default and the legacy behaviour) or <code>allowlist</code> (&quot;share only…&quot;). The full design is <code>docs/superpowers/specs/2026-07-28-multi-central-and-repo-sharing-design.md</code> plus its Plan 4 addendum; this is what shipped from it. Three layers:</p>\n<ul>\n<li><strong>The pure decision layer — <code>server/share-rules.ts</code>.</strong> <code>sessionShared(session, rules, index)</code> is the one predicate that decides whether a session may leave the machine for a given connection, where <code>rules</code> is <code>{ mode: &#39;denylist&#39; | &#39;allowlist&#39;, sources: ReadonlySet&lt;string&gt; }</code> keyed as <code>`${type}:${value}`</code> (<code>repo:&lt;canonical key&gt;</code>, <code>project:&lt;project_path&gt;</code>, or the fixed <code>none:</code> bucket for sessions that resolve to no repository at all). A session <strong>matches</strong> a source when: <code>repo</code> — its canonical repo key equals the value (including via <code>buildPathRepoIndex</code>, for adapters that never stamp a remote); <code>project</code> — its <code>project_path</code> equals the value; <code>none</code> — it resolves to no repository. In <strong>denylist</strong> mode a session is shared unless it matches SOME source (&quot;deny wins across dimensions&quot;: matching a blocked repo denies it even if its project is not listed). In <strong>allowlist</strong> mode a session is shared only if it matches SOME source — and an allowlist with an EMPTY source set shares NOTHING, deliberately; the opposite reading is exactly the one that would leak everything. The <code>conflictPaths</code> rule (a directory known to hold more than one repo) survives unchanged in both modes: denylist shares such a path only if every repo under it is shared; allowlist shares it only if some repo is allowed and none is excluded — a workspace holding a shared and a blocked repo would otherwise leak the blocked one&#39;s <code>first_prompt</code> under the shared key. <code>filterShared</code> / <code>filterSharedWorkflows</code> apply the predicate to sessions and workflow runs; a run whose owning session is unknown is dropped, never kept. <code>buildSplitStatsCache</code> is the attribution-split half — see below. Every function is pure and fails closed: uncertain attribution means &quot;not shared&quot;, never &quot;shared by default&quot;.</li>\n<li><strong>The push path — <code>server/team-rules.ts</code> + <code>server/team-uploader.ts</code>.</strong> <code>planRulesReconcile</code> is the pure detector that runs every push cycle per connection: it diffs the current denylist&#39;s signature against the last-persisted one, computes which previously-sent session/run ids the <em>current</em> denylist now excludes (<code>forgetIds</code>/<code>forgetRuns</code> — see below), and advances the seal ledger (see the attribution split). <code>team-uploader.ts</code> calls <code>filterShared</code>/<code>filterSharedWorkflows</code> before every push and <code>buildSplitStatsCache</code> before every statsCache push — a restricted connection never sees the unfiltered store.</li>\n<li><strong>The central — <code>server/team-forget.ts</code> + <code>server/team-capabilities.ts</code>.</strong> <code>POST /api/team/forget</code> deletes named sessions (+ their workflow runs) scoped to the token&#39;s own <code>memberId</code>; <code>GET /api/team/policy</code> advertises <code>capabilities: [..., &#39;forget.sessions&#39;]</code> so a member can tell whether a central understands the route at all.</li>\n</ul>\n<p><strong>The attribution split.</strong> <code>stats-cache.json</code>&#39;s aggregates cannot be filtered per-session — a day&#39;s row is a sum, not a list. <code>buildSplitStatsCache</code> (<code>share-rules.ts</code>) solves this by splitting the cache at <strong><code>attributionBoundary(real)</code>: the day <em>after</em> Claude&#39;s own <code>lastComputedDate</code> rollup watermark</strong>, not wherever the consolidate store happens to begin. Days at or before the watermark are Claude&#39;s rollup verbatim — nobody, including this machine, can decompose them by repository, because the store is measurably a strict subset of what Claude already rolled up. Days at or after it are rebuilt from-scratch from the (denylist-filtered) session store, which is exact because the same store produced the field being subtracted from. <strong>Totals never shrink</strong>: the prehistory block travels unfiltered, only the decomposable window is filtered, which is what keeps a restricted machine&#39;s own dashboard numbers reconcilable with what it pushes.</p>\n<p>As Claude&#39;s watermark advances, a day that is decomposable today becomes part of the untouchable rollup tomorrow — so each cycle, while a day is still decomposable, the denied delta for that day is measured and, the moment the watermark crosses it, <strong>sealed</strong>: kept forever and subtracted from the rollup row from then on. Un-blocking a repo does not un-seal a day; a sealed day&#39;s excluded volume cannot be restored to a central because there is no longer a decomposable row to add it back to.</p>\n<p><strong>The split cache is selected by the declared rule, <code>sourcesRestrict(conn.shareMode, conn.sources)</code> — never by comparing filtered vs. unfiltered counts.</strong> A count-based switch fails open on a cold consolidate store (<code>0 &lt; 0</code> is false) and flips without any corresponding user action. Allowlist mode is <strong>always</strong> a restriction, even with an empty source list — that is the strictest case, not the absence of one; denylist mode is a restriction only once it names at least one source (the legacy <code>hasRestrictions</code> behaviour, kept for the read migration). On the restricted path there is <strong>no fallback to the unsplit cache, ever</strong>: <code>buildSplitStatsCache</code> returns <code>null</code> whenever it cannot build a faithful split (no attribution boundary, an unreadable real cache, the cold-store signature), and the push omits <code>statsCache</code> entirely rather than shipping the unfiltered one. <code>withUnresolvedSources</code> — the fail-closed default applied the moment a connection acquires its first restriction — applies to <strong>denylist mode only</strong>: in allowlist mode the unattributed (<code>none:</code>) bucket is already hidden by default like everything not explicitly listed, so there is nothing to add.</p>\n<p><strong>The removal sequence</strong> (<code>team-forget-client.ts</code>&#39;s <code>runForgetSequence</code>, triggered by <code>planRulesReconcile</code> finding <code>forgetIds</code>/<code>forgetRuns</code>) is what withdraws sessions already pushed before a repo was blocked:</p>\n<ol start=\"0\">\n<li>capability check (<code>connectionCanForget</code>) + <code>GET /api/team/whoami</code> proves which identity the central will act on — the trigger is <strong>denial, never absence</strong>: a session merely missing from a short-read store is never mistaken for a session that became denied.</li>\n<li>write the removal journal to disk <strong>before</strong> the first delete — a crash mid-sequence resumes from the journal at boot; deleting an already-deleted id is a no-op.</li>\n<li><code>POST /api/team/forget { sessionIds }</code> in batches of 500, asserting on the response <strong>body</strong> (<code>ok === true</code>), never on <code>res.ok</code> — a reverse proxy can turn a 404 into a 200 HTML page.</li>\n<li>advance the sent-state <strong>per acked batch</strong>, never up front, so an interrupted sequence is resumable rather than self-defeating.</li>\n<li>push the rebuilt split statsCache in the same cycle.</li>\n<li>delete the journal only once the cache push has landed.</li>\n</ol>\n<p>The central-side delete is a <strong>scoped delete of named sessions</strong>, never a purge — the member&#39;s <code>memberStats</code> document is never touched, so no <em>other</em> machine&#39;s machine/team filter degrades while a removal runs. <code>POST /api/team/forget</code> is minted-token-only with no legacy <code>{org,user}</code> fallback (deliberately unlike <code>POST /api/team/leave</code>), and a central that does not advertise <code>forget.sessions</code> gets <code>canForget: false</code> and a disabled rules editor — never a fallback to a destructive full purge.</p>\n<p>See <a href=\"https://github.com/blpsoares/agentistics/blob/main/security.md\" target=\"_blank\" rel=\"noopener\">security.md</a> for the precise guarantee this gives a user, and its five explicit non-guarantees.</p>\n<h2 id=\"repository-dimension-group-by-git-remote\">Repository dimension (group by git remote)</h2>\n<p>Metrics can be grouped <strong>by repository</strong> — the git remote — independently of the local checkout path or which machine produced the session. So a repo&#39;s usage aggregates across every dev&#39;s laptop <em>and</em> its CI runs, even though those live on different machines with different paths.</p>\n<h3 id=\"the-key-normalizegitremote\">The key — `normalizeGitRemote`</h3>\n<p><code>normalizeGitRemote(url)</code> in <code>@agentistics/core</code> collapses any remote form (https / ssh / scp / git, with or without credentials / port / <code>.git</code>) into a stable, <strong>protocol-less</strong> key <code>host/org/repo</code> (e.g. <code>github.com/org/repo</code>). It is the <strong>single source of truth</strong> — repos are never keyed by parsing the local path. <code>repoShortName(remote)</code> drops the host for display (<code>org/repo</code>); a session with no resolvable remote falls into the &quot;no linked repository&quot; bucket (shown, never hidden).</p>\n<h3 id=\"how-it-is-captured-and-threaded\">How it is captured and threaded</h3>\n<ul>\n<li><code>server/git.ts getGitRemote(projectPath)</code> reads <code>remote.origin.url</code> and normalizes it (same Windows/WSL + no-prompt guards as the other git helpers).</li>\n<li><code>server/data.ts scanProjectDir</code> resolves the remote <strong>once per project</strong> and <strong>stamps <code>SessionMeta.git_remote</code> onto every session</strong> (plus <code>ServerProject.gitRemote</code>). Because it lives on the session, the remote travels into the consolidate store → team uploader → Mongo — the central has no filesystem access to members&#39; repos, so per-session is the only place it can live.</li>\n<li>Frontend: <code>useDerivedStats</code> builds <code>repoStats</code> (per-remote aggregate) and honors a <code>Filters.repos</code> filter, scoping cost/tokens session-side like a project filter. The <strong>Repositories</strong> page (<code>RepositoriesPage</code> → <code>RepoDetailPage</code>) renders cards → per-repo detail (Overview / Members / Actions / Sessions / Workflows).</li>\n</ul>\n<h3 id=\"github-actions-sessionmetaci-repo-registration\">GitHub Actions — `SessionMeta.ci` + repo registration</h3>\n<p>An ephemeral Claude Code Actions runner pushes its metrics with <code>agentop ci-push</code> → <code>POST /api/team/ingest</code>, so a repo&#39;s dashboard shows <strong>local devs + cloud agents together</strong>. Attribution is <strong>server-authoritative</strong>: the central stamps <code>git_remote</code> + <code>ci: true</code> + <code>user = github-actions</code> from a <em>verified</em> identity (<code>stampCiSessions</code>) — a runner can never mis-report its repo. Two auth paths:</p>\n<ul>\n<li><strong>Keyless GitHub OIDC (recommended)</strong> — the runner presents a short-lived GitHub-signed JWT; the central verifies it against GitHub&#39;s JWKS (<code>server/team-oidc.ts</code>, via <code>jose</code>: issuer / audience / expiry) and checks the <code>repository</code> claim against the <strong>registered-repos allowlist</strong>. No secret stored. Enabled by <code>AGENTISTICS_OIDC_AUDIENCE</code> on the central.</li>\n<li><strong>Repo-bound static token (fallback)</strong> — minted by registration, stored only as a sha256 hash, sent as <code>Authorization: Bearer</code>.</li>\n</ul>\n<p><strong>Registering a repo</strong> is an admin action (central <strong>Settings → Team → Repositories</strong>, the <code>TeamRepos</code> panel, or <code>POST /api/team/repos</code>). It allowlists the normalized remote and mints the fallback CI token; the panel also generates a ready-to-paste workflow push step. Re-registering <strong>rotates</strong> the token; unregistering revokes it (both drop that repo&#39;s CI data). CI sessions are keyed by <code>ciMemberId</code> (<code>repo:&lt;remote&gt;</code>) and power the <strong>Repositories → Actions</strong> view.</p>\n<h3 id=\"ingest-only-hardening\">Ingest-only hardening</h3>\n<p>A cloud runner needs the central reachable without exposing the dashboard. <code>AGENTISTICS_INGEST_ONLY=1</code> makes a central serve <strong>only</strong> <code>POST /api/team/ingest</code> (404 for everything else, gated right after the OPTIONS handler in <code>index.ts</code>) — run it as a public ingest instance sharing Mongo with a separate private dashboard instance.</p>\n<p>See <a href=\"https://github.com/blpsoares/agentistics/blob/main/github-actions.md\" target=\"_blank\" rel=\"noopener\">`docs/github-actions.md`</a> for the end-to-end GitHub Actions setup.</p>\n<h2 id=\"cli-agentop\">CLI (`agentop`)</h2>\n<p><code>packages/server/bin/cli.ts</code> is the single command surface for the compiled binary:</p>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>setup</code></td>\n<td>Interactive first-run wizard — pick solo / central / member, then optionally enable autostart (<code>server/cli-setup.ts</code>). Bare <code>agentop</code> on a TTY when the machine is unconfigured launches this.</td>\n</tr>\n<tr>\n<td><code>server</code></td>\n<td>Dashboard + background daemon (<code>SERVE_STATIC=1</code>; API + embedded frontend + otel-watcher on one port).</td>\n</tr>\n<tr>\n<td><code>tui</code></td>\n<td>Standalone terminal dashboard.</td>\n</tr>\n<tr>\n<td><code>watch</code></td>\n<td>OTel metrics daemon only.</td>\n</tr>\n<tr>\n<td><code>central &lt;up|init|down|logs|status|restart|pull&gt;</code></td>\n<td>Wraps <code>central.sh</code> (<code>server/cli-central.ts</code>).</td>\n</tr>\n<tr>\n<td><code>member &lt;connect|leave|status&gt;</code></td>\n<td>Configure this machine as a member (<code>server/cli-member.ts</code>). <code>connect --endpoint &lt;url&gt; --token &lt;tok&gt; [--org &lt;o&gt;]</code> verifies via whoami before saving.</td>\n</tr>\n<tr>\n<td><code>autostart &lt;server|central|watch&gt; &lt;enable|disable|status&gt;</code></td>\n<td>Register a mode to start with the system (<code>server/autostart.ts</code>). Linux/WSL: a systemd <strong>user</strong> service + <code>loginctl enable-linger</code>, and installs a <code>~/.bashrc</code> hook running <code>agentop check-update</code> on terminal open. macOS/Windows print a manual step. <code>autostart status</code> (no mode) lists all.</td>\n</tr>\n<tr>\n<td><code>upgrade</code></td>\n<td>Self-update to the latest version.</td>\n</tr>\n<tr>\n<td><code>check-update</code></td>\n<td>Prints the &quot;update available&quot; banner only when outdated; silent when current (this is what the <code>.bashrc</code> hook runs).</td>\n</tr>\n</tbody></table>\n<p><strong>Update detection</strong> is everywhere: on any command run (banner via <code>checkVersionAndWarn</code>), on boot/terminal (the <code>.bashrc</code> hook), and on the dashboard (bell notification + a <strong>mode-aware</strong> <code>UpdateModal.tsx</code> with the exact upgrade+restart command — central: <code>bun run up:central</code>; member: <code>agentop upgrade</code> then <code>systemctl --user restart agentop-server</code>). A periodic (~6h) server re-check pushes the update notification over SSE. All version logic lives in <code>server/version.ts</code>.</p>\n<h2 id=\"binary-build-pipeline\">Binary build pipeline</h2>\n<pre><code>bun run build           →  packages/web/dist/                              (Vite)\nbun run build:assets    →  packages/server/server/embedded-dist.generated.ts\nbun build --compile     →  release/agentop                                 (self-contained binary)\n</code></pre>\n<p>The binary embeds the full Bun runtime + all JS/TS code + frontend assets. No external dependencies needed — <code>agentop server</code> binds two ports with one shared request handler: the <strong>web dashboard on 47292</strong> (the URL you open) and the <strong>api + mcp on 47291</strong>. Because both ports run the same handler, the dashboard served from 47292 makes same-origin <code>/api/*</code> calls that resolve locally.</p>\n<p>In dev mode, the API runs on port 47291 and Vite serves the frontend with hot reload on port 47292 — the same web-on-92 / api-on-91 split.</p>\n<h2 id=\"windows-desktop-app\">Windows desktop app</h2>\n<p>The Tauri app (<code>packages/desktop/</code>) is a native Windows wrapper:</p>\n<ol>\n<li>On launch, reads config from <code>%APPDATA%\\Agentistics\\config.json</code></li>\n<li>If not configured: shows onboarding screen — auto-detects <code>%USERPROFILE%\\.claude</code> and WSL paths via <code>\\\\wsl.localhost\\{distro}\\home\\*\\.claude</code></li>\n<li>Once configured: spawns <code>agentop.exe</code> as a sidecar with <code>CLAUDE_DIR</code> env var</li>\n<li>Polls <code>http://localhost:47291/api/health</code> every 250ms (up to 30s), then navigates the WebView to the dashboard</li>\n<li>On window close: kills the sidecar process</li>\n</ol>\n<p>CI builds the installer on <code>windows-latest</code> after the Linux runner cross-compiles <code>agentop.exe</code>.</p>\n<h2 id=\"port-configuration\">Port configuration</h2>\n<p>Ports are configured in <code>.env.config</code> at the repository root:</p>\n<pre><code class=\"language-ini\">PORT=47291      # api + mcp (binary mode also binds PORT+1 = 47292 for the web dashboard)\nVITE_PORT=47292 # Vite dev server (dev mode only)\n</code></pre>\n<p>Edit via the <code>&lt;/&gt;</code> button in the header or directly in the file (restart required).</p>\n<h2 id=\"calculation-functions-single-source-of-truth\">Calculation functions — single source of truth</h2>\n<p>All layers import from <code>@agentistics/core</code> (<code>packages/core/src/types.ts</code>). Never inline pricing calculations.</p>\n<table>\n<thead>\n<tr>\n<th>Function</th>\n<th>Usage</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>MODEL_PRICING</code></td>\n<td>Pricing table, USD per 1M tokens</td>\n</tr>\n<tr>\n<td><code>getModelPrice(modelId)</code></td>\n<td>Resolves price by model ID (exact then partial match)</td>\n</tr>\n<tr>\n<td><code>calcCost(usage, modelId)</code></td>\n<td>Total cost from a <code>ModelUsage</code> record</td>\n</tr>\n<tr>\n<td><code>blendedCostPerToken(modelUsage)</code></td>\n<td>Weighted average rate — used in <code>useData.ts</code> for filtered views and PDF export</td>\n</tr>\n</tbody></table>\n<h2 id=\"tech-stack\">Tech stack</h2>\n<h3 id=\"frontend-packagesweb\">Frontend (`packages/web/`)</h3>\n<table>\n<thead>\n<tr>\n<th>Library</th>\n<th>Version</th>\n<th>Usage</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>React</td>\n<td>19.2</td>\n<td>UI</td>\n</tr>\n<tr>\n<td>Vite</td>\n<td>8.0</td>\n<td>Build tool + dev server</td>\n</tr>\n<tr>\n<td>TypeScript</td>\n<td>5</td>\n<td>Strict typing</td>\n</tr>\n<tr>\n<td>Recharts</td>\n<td>3.8</td>\n<td>Area charts, bar charts</td>\n</tr>\n<tr>\n<td>react-markdown</td>\n<td>10.x</td>\n<td>Markdown rendering in Nay chat</td>\n</tr>\n<tr>\n<td>lucide-react</td>\n<td>1.7</td>\n<td>SVG icons</td>\n</tr>\n<tr>\n<td>date-fns</td>\n<td>4.1</td>\n<td>Date manipulation</td>\n</tr>\n<tr>\n<td>html2canvas + jspdf</td>\n<td>1.4 / 4.2</td>\n<td>PDF export</td>\n</tr>\n</tbody></table>\n<h3 id=\"backend-packagesserver\">Backend (`packages/server/`)</h3>\n<table>\n<thead>\n<tr>\n<th>Technology</th>\n<th>Usage</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Bun</td>\n<td>HTTP server, subprocess spawning, file I/O</td>\n</tr>\n<tr>\n<td>chokidar</td>\n<td>File watching for live updates and OTel daemon</td>\n</tr>\n<tr>\n<td>@modelcontextprotocol/sdk</td>\n<td>MCP server implementation</td>\n</tr>\n<tr>\n<td>@opentelemetry/*</td>\n<td>Metrics export (optional)</td>\n</tr>\n</tbody></table>\n<h3 id=\"desktop-packagesdesktop\">Desktop (`packages/desktop/`)</h3>\n<table>\n<thead>\n<tr>\n<th>Technology</th>\n<th>Usage</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Tauri v2</td>\n<td>Native window + WebView wrapper</td>\n</tr>\n<tr>\n<td>Rust</td>\n<td>Sidecar spawn, health polling, config management</td>\n</tr>\n<tr>\n<td>tauri-plugin-shell</td>\n<td>Sidecar process lifecycle</td>\n</tr>\n<tr>\n<td>reqwest</td>\n<td>HTTP health check in Rust async</td>\n</tr>\n</tbody></table>\n<h2 id=\"key-design-decisions\">Key design decisions</h2>\n<p><strong>No database</strong> — all data read directly from Claude Code&#39;s local files. Zero setup, zero schema migrations, always fresh.</p>\n<p><strong>Single API endpoint</strong> — <code>/api/data</code> returns everything in one call. The frontend derives all views from this response using <code>useDerivedStats()</code>. Filtering is purely client-side.</p>\n<p><strong><code>stats-cache.json</code> for aggregates, JSONL for details</strong> — the stats cache is fast (pre-computed by Claude Code) but has no project granularity. Project breakdowns are computed from individual session records.</p>\n<p><strong>Nay runs as a subprocess</strong> — <code>claude --print</code> is spawned by the server, not called via API. Nay inherits the full Claude Code CLI environment without extra integration work.</p>\n<p><strong>Binary embeds the frontend</strong> — <code>agentop server</code> serves both API and UI from a single process on a single port. No Nginx needed.</p>\n<p><strong><code>@agentistics/core</code> as shared package</strong> — types, pricing functions, and formatters live in one place. Server, web, and MCP all import from <code>@agentistics/core</code>. Nothing is duplicated.</p>\n<p><strong>PWA installable</strong> — <code>vite-plugin-pwa</code> makes the web app installable as a PWA (enabled even in dev mode via <code>devOptions: { enabled: true }</code>). API calls are always <code>NetworkOnly</code>; static assets are cached. Icons live at <code>packages/web/public/icons/</code>.</p>\n<p><strong>Unified Settings modal</strong> — <code>PreferencesModal.tsx</code> replaced separate modals with a single tabbed interface: Preferences (lang/theme/currency/sounds), Live (update interval), and Install (web PWA + desktop download). The old Environment (port config) tab was removed.</p>\n<p><strong>Team Mode ships no per-machine secrets to the wire</strong> — members push computed metrics only, never chat; tokens are stored <strong>only as sha256 hashes</strong> (<code>server/team-tokens.ts</code>) and never logged; the central&#39;s session cookie secret is kept separate from the dashboard password; auth comparisons are constant-time; Mongo is not published to the host; and <code>BIND_IP</code> can pin the listener to a private tailnet (Tailscale encrypts the transport, so plain http inside it is fine). See the &quot;Team Mode&quot; section above.</p>\n<p><strong><code>files_modified</code> takes max of two sources</strong> — <code>server/jsonl.ts</code> tracks unique file paths from Edit/Write/MultiEdit tool calls, then takes <code>Math.max(gitFileStats.filesModified, claudeFilesModified.size)</code>. The FILES KPI in <code>useData.ts</code> prefers the session-level count and only falls back to project-level git stats if sessions show 0.</p>\n<p><strong><code>getProjectGitStats</code> handles workspace folders</strong> — if a project path is not itself a git repo, <code>server/git.ts</code> scans one level of subdirectories and aggregates stats from all git repos found there. This covers workspace folders like <code>~/zuke</code> that contain multiple repos.</p>\n",
    "headings": [
      {
        "id": "architecture",
        "text": "Architecture",
        "level": 1
      },
      {
        "id": "monorepo-layout",
        "text": "Monorepo layout",
        "level": 2
      },
      {
        "id": "request-lifecycle",
        "text": "Request lifecycle",
        "level": 2
      },
      {
        "id": "team-mode",
        "text": "Team Mode",
        "level": 2
      },
      {
        "id": "roles",
        "text": "Roles",
        "level": 3
      },
      {
        "id": "centralsh-agentop-central",
        "text": "central.sh + `agentop central`",
        "level": 3
      },
      {
        "id": "restart-policy-both-services-survive-a-host-reboot",
        "text": "Restart policy — both services survive a host reboot",
        "level": 3
      },
      {
        "id": "member-identity",
        "text": "Member identity",
        "level": 3
      },
      {
        "id": "push-model-central-owned-interval-push-on-change",
        "text": "Push model — central-owned interval + push-on-change",
        "level": 3
      },
      {
        "id": "real-time-central",
        "text": "Real-time central",
        "level": 3
      },
      {
        "id": "presence-websocket-authoritative",
        "text": "Presence — WebSocket-authoritative",
        "level": 3
      },
      {
        "id": "auto-reconciliation-self-healing-sync",
        "text": "Auto-reconciliation (self-healing sync)",
        "level": 3
      },
      {
        "id": "notifications",
        "text": "Notifications",
        "level": 3
      },
      {
        "id": "per-connection-repository-sharing",
        "text": "Per-connection repository sharing",
        "level": 3
      },
      {
        "id": "repository-dimension-group-by-git-remote",
        "text": "Repository dimension (group by git remote)",
        "level": 2
      },
      {
        "id": "the-key-normalizegitremote",
        "text": "The key — `normalizeGitRemote`",
        "level": 3
      },
      {
        "id": "how-it-is-captured-and-threaded",
        "text": "How it is captured and threaded",
        "level": 3
      },
      {
        "id": "github-actions-sessionmetaci-repo-registration",
        "text": "GitHub Actions — `SessionMeta.ci` + repo registration",
        "level": 3
      },
      {
        "id": "ingest-only-hardening",
        "text": "Ingest-only hardening",
        "level": 3
      },
      {
        "id": "cli-agentop",
        "text": "CLI (`agentop`)",
        "level": 2
      },
      {
        "id": "binary-build-pipeline",
        "text": "Binary build pipeline",
        "level": 2
      },
      {
        "id": "windows-desktop-app",
        "text": "Windows desktop app",
        "level": 2
      },
      {
        "id": "port-configuration",
        "text": "Port configuration",
        "level": 2
      },
      {
        "id": "calculation-functions-single-source-of-truth",
        "text": "Calculation functions — single source of truth",
        "level": 2
      },
      {
        "id": "tech-stack",
        "text": "Tech stack",
        "level": 2
      },
      {
        "id": "frontend-packagesweb",
        "text": "Frontend (`packages/web/`)",
        "level": 3
      },
      {
        "id": "backend-packagesserver",
        "text": "Backend (`packages/server/`)",
        "level": 3
      },
      {
        "id": "desktop-packagesdesktop",
        "text": "Desktop (`packages/desktop/`)",
        "level": 3
      },
      {
        "id": "key-design-decisions",
        "text": "Key design decisions",
        "level": 2
      }
    ]
  },
  "deploy": {
    "id": "deploy",
    "file": "docs/DEPLOY.md",
    "title": "Deploying a central",
    "group": "Team Mode",
    "html": "<h1 id=\"agentistics-docker-deployment-guide\">agentistics — Docker Deployment Guide</h1>\n<p>This guide covers running <strong>agentistics Team Mode</strong> as a central aggregator using Docker Compose.</p>\n<hr>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>Docker Engine ≥ 24 and Docker Compose ≥ 2.20</li>\n<li>A Linux/macOS host with at least 512 MB of free RAM</li>\n<li>Outbound internet access for the initial image pull</li>\n</ul>\n<hr>\n<h2 id=\"quick-start\">Quick Start</h2>\n<blockquote>\n<p><strong>No repo? Use the binary.</strong> If you have the <code>agentop</code> binary installed, you can skip cloning\nentirely: <code>agentop central up</code> pulls the published image and runs the central from any\ndirectory (see <a href=\"#the-same-thing-from-the-cli--agentop-central\">`agentop central`</a>). The steps\nbelow are the repo-based flow (builds the image from source).</p>\n</blockquote>\n<h3 id=\"1-clone-the-repository-and-enter-the-project-root\">1. Clone the repository and enter the project root</h3>\n<pre><code class=\"language-bash\">git clone https://github.com/blpsoares/agentistics.git\ncd agentistics\n</code></pre>\n<h3 id=\"2-create-your-centralenv\">2. Create your `central.env`</h3>\n<p><strong>Easiest — interactive setup</strong> (recommended). <code>central.sh</code> asks for each value and\nauto-generates the secrets with <code>openssl</code> (press Enter on a secret field to generate it),\ndetects your Tailscale IP for the bind, and writes <code>central.env</code> with <code>chmod 600</code>:</p>\n<pre><code class=\"language-bash\">bun run init:central     # or: ./central.sh init\n</code></pre>\n<p>You&#39;ll be prompted for the port, org name, session secret, an optional ingest token, and the\nbind interface. <code>bun run up:central</code> runs this automatically the first time (when <code>central.env</code>\nis missing).</p>\n<blockquote>\n<p><strong>There is no team/dashboard password.</strong> A central authenticates <strong>accounts</strong>: on first boot it\nhas none, so it prints a <strong>one-time setup token</strong> to its log and the dashboard opens on the\n&quot;create the owner account&quot; screen (name, e-mail, password + that token). Everyone else gets\ntheir own account, invited by the owner. <code>up</code> prints the token for you; otherwise read it with\n<code>docker compose -p team-mode logs app | grep -A6 &quot;OWNER SETUP&quot;</code>.</p>\n</blockquote>\n<blockquote>\n<p><strong>Forgot the owner password?</strong> There is no e-mail-based reset — a self-hosted central has no\nmail server, and a reset link it cannot deliver would be worse than none. Recovery is at the\nhost, where whoever runs it already holds the database:</p>\n<pre><code class=\"language-bash\">./central.sh reset-password --email you@example.com      # run without --email to list accounts\n./central.sh reset-password --email you@example.com --clear-mfa   # also lost the 2FA device\n</code></pre>\n<p>It prints a one-time password, forces a change at the next sign-in, and signs out every\nexisting session for that account.</p>\n</blockquote>\n<blockquote>\n<p>Name it <code>central.env</code>, <strong>not</strong> <code>.env</code>. A plain <code>.env</code> is auto-loaded by <code>bun run dev</code>\n(a developer&#39;s local/member instance) and would make that instance wrongly think it is\nthe central. Using <code>central.env</code> keeps the two roles cleanly separated.</p>\n</blockquote>\n<blockquote>\n<p><strong>Security note</strong>: <code>AGENTISTICS_TEAM_SESSION_SECRET</code> signs session cookies and is never derived\nfrom any password — a value equal to the legacy <code>AGENTISTICS_TEAM_PASSWORD</code> refuses to boot.\nAn account password being leaked still does not let an attacker forge session cookies.</p>\n</blockquote>\n<p><strong>Manual alternative</strong> — copy the example and edit it yourself:</p>\n<pre><code class=\"language-bash\">cp .env.example central.env\nopenssl rand -hex 32   # → AGENTISTICS_TEAM_SESSION_SECRET\n</code></pre>\n<h3 id=\"3-start-the-stack\">3. Start the stack</h3>\n<p>The repo ships a helper script, <strong><code>central.sh</code></strong>, that wraps <code>docker compose</code> with\nthe project name (<code>-p team-mode</code>) and <code>--env-file central.env</code> pre-set so you don&#39;t\nhave to remember the flags:</p>\n<pre><code class=\"language-bash\">bun run up:central     # = ./central.sh up — build + (re)create the containers  [most common]\n</code></pre>\n<p>The first time (no <code>central.env</code> yet) this runs the interactive setup from step 2\nautomatically before deploying.</p>\n<p>The first run builds the image and initialises the MongoDB replica set. This takes about 30–60 seconds.</p>\n<blockquote>\n<p><strong>Why the script?</strong> Two easy-to-forget details are baked in:</p>\n<ul>\n<li><code>up</code> uses <code>--build --force-recreate</code>. A plain <code>docker compose up -d</code> does <strong>not</strong>\nrecreate the container after a rebuild, so your new code silently would not run.</li>\n<li><code>-p team-mode</code> names the stack, keeping it isolated from any other compose project\non the same host.</li>\n</ul>\n</blockquote>\n<p>Prefer raw compose? The equivalent is:</p>\n<pre><code class=\"language-bash\">docker compose -p team-mode --env-file central.env up -d --build --force-recreate\n</code></pre>\n<h3 id=\"4-open-the-dashboard\">4. Open the dashboard</h3>\n<p>Navigate to <code>http://&lt;your-host&gt;:&lt;APP_PORT&gt;</code> (default: <code>http://localhost:48080</code>).</p>\n<hr>\n<h2 id=\"managing-the-central-centralsh\">Managing the central — `central.sh`</h2>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>./central.sh init</code></td>\n<td>(Re)generate <code>central.env</code> interactively (auto-generates secrets with openssl)</td>\n</tr>\n<tr>\n<td><code>./central.sh up</code></td>\n<td>Build the image and (re)create the containers (<code>--build --force-recreate</code>); offers <code>init</code> if <code>central.env</code> is missing</td>\n</tr>\n<tr>\n<td><code>./central.sh restart</code></td>\n<td>Restart the <code>app</code> container <strong>without</strong> rebuilding</td>\n</tr>\n<tr>\n<td><code>./central.sh logs</code></td>\n<td>Follow the <code>app</code> container logs (Ctrl-C to stop)</td>\n</tr>\n<tr>\n<td><code>./central.sh status</code></td>\n<td>Show container + health status</td>\n</tr>\n<tr>\n<td><code>./central.sh down</code></td>\n<td>Stop and remove the containers — <strong>keeps</strong> the Mongo data volume</td>\n</tr>\n<tr>\n<td><code>./central.sh pull</code></td>\n<td>Rebuild from a fresh base image (run <code>git pull</code> first)</td>\n</tr>\n<tr>\n<td><code>./central.sh setup-token</code></td>\n<td>Reissue the one-time OWNER setup token (refused once an owner exists)</td>\n</tr>\n<tr>\n<td><code>./central.sh reset-password --email &lt;address&gt;</code></td>\n<td>Reset an account&#39;s password — the recovery path for a locked-out owner. <code>--clear-mfa</code> also drops its second factor</td>\n</tr>\n<tr>\n<td><code>./central.sh help</code></td>\n<td>Print the usage summary</td>\n</tr>\n</tbody></table>\n<p>Override the defaults with env vars: <code>PROJECT=... ENV_FILE=... ./central.sh up</code>.</p>\n<blockquote>\n<p><code>down</code> never passes <code>-v</code>, so your stored team data survives. Only add <code>-v</code> manually\nwhen you deliberately want to wipe everything.</p>\n</blockquote>\n<h3 id=\"the-same-thing-from-the-cli-agentop-central\">The same thing from the cli — `agentop central`</h3>\n<p><code>agentop central &lt;action&gt;</code> manages the central via Docker. It picks its path automatically:</p>\n<ul>\n<li><strong>In a repo checkout</strong> — it shells out to the repo&#39;s <code>central.sh</code> (locating it in the\ncheckout, inheriting stdio so <code>init</code>&#39;s prompts and <code>logs</code> streaming work). The image is\nbuilt from source (<code>build: .</code>).</li>\n<li><strong>From the standalone binary (no repo)</strong> — it materializes a Compose file that <strong>pulls</strong>\nthe published image <code>ghcr.io/blpsoares/agentistics:&lt;version&gt;</code> into <code>~/.agentistics/central/</code>,\ngenerates <code>central.env</code> interactively on first <code>up</code>, and drives <code>docker compose</code> directly.\nNo clone required — run it from any directory.</li>\n</ul>\n<p>The actions map one-to-one:</p>\n<table>\n<thead>\n<tr>\n<th>central.sh</th>\n<th>agentop equivalent</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>./central.sh init</code></td>\n<td><code>agentop central init</code></td>\n<td>(Re)generate <code>central.env</code> interactively</td>\n</tr>\n<tr>\n<td><code>./central.sh up</code></td>\n<td><code>agentop central up</code></td>\n<td>Deploy: build (repo) or pull (standalone) + <code>--force-recreate</code></td>\n</tr>\n<tr>\n<td><code>./central.sh restart</code></td>\n<td><code>agentop central restart</code></td>\n<td>Restart the <code>app</code> container without rebuilding</td>\n</tr>\n<tr>\n<td><code>./central.sh logs</code></td>\n<td><code>agentop central logs</code></td>\n<td>Follow the <code>app</code> logs</td>\n</tr>\n<tr>\n<td><code>./central.sh status</code></td>\n<td><code>agentop central status</code></td>\n<td>Show container + health status</td>\n</tr>\n<tr>\n<td><code>./central.sh down</code></td>\n<td><code>agentop central down</code></td>\n<td>Stop + remove containers (keeps the data volume)</td>\n</tr>\n<tr>\n<td><code>./central.sh pull</code></td>\n<td><code>agentop central pull</code></td>\n<td>Pull a fresh image and recreate</td>\n</tr>\n<tr>\n<td><code>./central.sh setup-token</code></td>\n<td><code>agentop central setup-token</code></td>\n<td>Reissue the one-time owner setup token</td>\n</tr>\n<tr>\n<td><code>./central.sh reset-password …</code></td>\n<td><code>agentop central reset-password …</code></td>\n<td>Reset an account&#39;s password from the host</td>\n</tr>\n</tbody></table>\n<pre><code class=\"language-bash\">agentop central up        # first time offers the interactive init, then deploys\nagentop central status\nagentop central logs\n</code></pre>\n<blockquote>\n<p><strong>Standalone requirements:</strong> the published image must be reachable. It&#39;s public on GHCR by\ndefault (no auth needed); if the package is private, run <code>docker login ghcr.io</code> first. Pin a\nspecific build with <code>AGENTISTICS_IMAGE=ghcr.io/blpsoares/agentistics:&lt;tag&gt;</code> (defaults to the\nbinary&#39;s own version). The standalone Compose runs a dedicated central and does <strong>not</strong> mount\nthe host harness dirs — for a self-contributing central (one machine = central + member), use\nthe repo <code>central.sh</code> with <code>AGENTISTICS_CENTRAL_USER</code> set.</p>\n</blockquote>\n<h3 id=\"external-database-atlas-remote-mongo-no-docker-required\">External database (Atlas / remote Mongo) — no Docker required</h3>\n<p>By default the standalone <code>agentop central</code> runs a <strong>bundled MongoDB in Docker</strong>. If you&#39;d rather\nuse a managed database (MongoDB Atlas) or an existing Mongo server, <code>agentop central init</code> (or the\nfirst <code>agentop central up</code>) now asks:</p>\n<pre><code>Database:\n  › Bundled Mongo (Docker starts it for you)\n    External URI — Atlas/remote (runs natively, no Docker)\n</code></pre>\n<p>Pick <strong>External URI</strong> and paste a connection string (<code>mongodb+srv://…</code> for Atlas, or\n<code>mongodb://host:27017/db</code>). When an external DB is configured, the central runs <strong>natively — the\n<code>agentop</code> binary is the server itself, no Docker and no local Mongo container</strong>. This is the path\nfor a user who installed <strong>only the CLI</strong> and points it at a cloud database.</p>\n<pre><code class=\"language-bash\">agentop central init      # choose &quot;External URI&quot;, paste your Atlas connection string\nagentop central up        # runs the central natively in the FOREGROUND (Ctrl-C to stop)\n</code></pre>\n<ul>\n<li>The connection string is written to <code>~/.agentistics/central/central.env</code> as <code>MONGO_URL</code>\n(<code>chmod 600</code>) — treat that file as a secret.</li>\n<li><code>up</code> runs in the <strong>foreground</strong>. For a background service, use\n<code>agentop autostart central</code> instead.</li>\n<li>Docker-only actions (<code>down</code>, <code>logs</code>, <code>status</code>, <code>pull</code>) don&#39;t apply to a native central — they\nprint guidance instead. Stop it with Ctrl-C (or the autostart service).</li>\n<li>Atlas is a replica set, so MongoDB change streams (used for live team refresh) work out of the\nbox. Whitelist this host&#39;s outbound IP in Atlas and use a database user scoped to the\n<code>agentistics</code> DB.</li>\n</ul>\n<blockquote>\n<p><strong>Bundled vs external is detected from <code>MONGO_URL</code>.</strong> A URL pointing at the Docker service\n(<code>mongo:27017</code>) → Docker path; anything else (Atlas, a remote host) → native path. To switch,\nre-run <code>agentop central init</code> and change the database choice.</p>\n</blockquote>\n<hr>\n<h2 id=\"environment-variables\">Environment Variables</h2>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>APP_PORT</code></td>\n<td><code>48080</code></td>\n<td>Host port the central is served on (distinct from a member/dev&#39;s <code>47291</code>)</td>\n</tr>\n<tr>\n<td><code>BIND_IP</code></td>\n<td><code>127.0.0.1</code></td>\n<td>Interface the app binds to. Default <code>127.0.0.1</code> = this host only, which is all a tunnel or a local reverse proxy needs. Set <code>0.0.0.0</code> for LAN access, or a specific IP (e.g. your Tailscale address) to restrict it to that network</td>\n</tr>\n<tr>\n<td><code>MONGO_URL</code></td>\n<td><code>mongodb://mongo:27017/?replicaSet=rs0</code></td>\n<td>MongoDB connection string. Point it at Atlas (<code>mongodb+srv://…</code>) or a remote Mongo to skip the bundled container — the standalone <code>agentop central</code> then runs natively without Docker (see <a href=\"#external-database-atlas--remote-mongo--no-docker-required\">External database</a>)</td>\n</tr>\n<tr>\n<td><code>MONGO_DB</code></td>\n<td><code>agentistics</code></td>\n<td>MongoDB database name</td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_TEAM_CENTRAL</code></td>\n<td><code>1</code></td>\n<td>Enable central aggregator mode (always <code>1</code> in Docker)</td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_TEAM_ORG</code></td>\n<td><code>default</code></td>\n<td>Organisation namespace for team sessions</td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_TEAM_PASSWORD</code></td>\n<td><em>(unset)</em></td>\n<td><strong>Legacy, pre-accounts shared password. Do not set it on a new central</strong> — it does not gate a central&#39;s <code>/api</code> routes (accounts do) and only makes the setup look like it needs a shared credential</td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_TEAM_SESSION_SECRET</code></td>\n<td><em>(generated)</em></td>\n<td>HMAC key for session cookies. Leave empty and the central generates a random one and persists it. It <strong>never</strong> falls back to a password — setting it equal to <code>AGENTISTICS_TEAM_PASSWORD</code> refuses to boot</td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_EXPOSURE</code></td>\n<td><code>lan</code> on a central</td>\n<td><code>local</code> | <code>lan</code> | <code>public</code>. <code>public</code> permanently revokes the local-shell, local-chat, host-transcript and MCP-admin routes and requires every owner to hold a second factor. An unrecognised value fails closed to <code>public</code>. See <a href=\"https://github.com/blpsoares/agentistics/blob/main/exposure.md\" target=\"_blank\" rel=\"noopener\">docs/exposure.md</a></td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_ALLOW_LOCAL_SHELL</code></td>\n<td><em>(unset)</em></td>\n<td>Re-enables those host-power routes on a <code>lan</code> central. <strong>Ignored on <code>public</code></strong></td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_TRUST_PROXY</code></td>\n<td><em>(unset)</em></td>\n<td>Believe <code>CF-Connecting-IP</code> / <code>X-Forwarded-For</code> for rate limiting and audit logging. Set to <code>1</code> <strong>only</strong> when a proxy that rewrites them is the sole way in</td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_ALLOWED_ORIGINS</code></td>\n<td><em>(empty)</em></td>\n<td>Comma-separated browser origins allowed cross-origin. Normally empty — the dashboard is same-origin</td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_TEAM_TLS</code></td>\n<td><em>(unset)</em></td>\n<td><code>1</code> when TLS is terminated in front: adds <code>Secure</code> + the <code>__Host-</code> cookie prefix and emits HSTS</td>\n</tr>\n<tr>\n<td><code>AGENTISTICS_TEAM_INGEST_TOKEN</code></td>\n<td><em>(empty)</em></td>\n<td>Bearer token for <code>/api/team/ingest</code>; leave empty to allow unauthenticated ingestion</td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"network-exposure-which-interface-the-dashboard-listens-on\">Network exposure (which interface the dashboard listens on)</h2>\n<p><code>BIND_IP</code> controls the interface the app binds to:</p>\n<table>\n<thead>\n<tr>\n<th>Goal</th>\n<th><code>BIND_IP</code></th>\n<th>Reachable from</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>This host only (default)</td>\n<td><em>unset</em> → <code>127.0.0.1</code></td>\n<td>Just this host&#39;s browser, a tunnel, or a local reverse proxy</td>\n</tr>\n<tr>\n<td>Serve a private tailnet</td>\n<td>your Tailscale IP (e.g. <code>100.x.y.z</code>)</td>\n<td>This host + Tailscale peers</td>\n</tr>\n<tr>\n<td>Serve the whole LAN</td>\n<td><code>0.0.0.0</code></td>\n<td>Every interface that can route to the host</td>\n</tr>\n</tbody></table>\n<p><strong>The default is loopback on purpose.</strong> It used to be <code>0.0.0.0</code>, which meant a fresh install\nlanded on every interface the host could route — including, on plenty of machines, one facing\nthe internet. Widening it is now a decision you make, not one you inherit.</p>\n<p>Binding your Tailscale IP serves remote teammates over Tailscale&#39;s encrypted network, so plain\nHTTP inside the tailnet is fine. To publish <strong>outside</strong> a tailnet, do not widen <code>BIND_IP</code> —\nkeep it on loopback and put a tunnel or a TLS reverse proxy in front, then set\n<code>AGENTISTICS_TEAM_TLS=1</code> and <code>AGENTISTICS_EXPOSURE=public</code>. That path is documented end to end\nin <strong><a href=\"https://github.com/blpsoares/agentistics/blob/main/exposure.md\" target=\"_blank\" rel=\"noopener\">docs/exposure.md</a></strong>, and <code>agentop doctor --exposed</code> checks it for you.</p>\n<blockquote>\n<p><strong>WSL2 note:</strong> binding to a specific non-loopback IP (e.g. Tailscale) means Windows&#39;\n<code>localhost</code> forwarding no longer reaches the app — browse via that IP instead. The default\n<code>127.0.0.1</code> keeps <code>http://localhost:&lt;APP_PORT&gt;</code> working from this host.</p>\n</blockquote>\n<h3 id=\"wsl2-tailscale-use-tailscale-serve-recommended\">WSL2 + Tailscale: use `tailscale serve` (recommended)</h3>\n<p>Running the central in <strong>Docker inside WSL2</strong> and exposing it over Tailscale is flaky with a\nraw published port: <code>http://localhost:&lt;APP_PORT&gt;</code> works, but hitting the machine&#39;s Tailscale\nIP (<code>http://100.x.y.z:&lt;APP_PORT&gt;</code>) often hangs or &quot;works, then stops&quot;. The cause is packets\narriving on the <code>tailscale0</code> interface having to traverse Docker&#39;s DNAT/FORWARD inside WSL2 —\nthat path is unreliable in WSL2&#39;s NAT networking. It is <strong>not</strong> an agentistics or container\nproblem (localhost proves the app is fine).</p>\n<p>The robust fix is to let <code>tailscaled</code> proxy the port instead of relying on the Docker/interface\npath — <code>tailscale serve</code> accepts the tailnet connection in-process and forwards to\n<code>127.0.0.1:&lt;APP_PORT&gt;</code> (which always works):</p>\n<pre><code class=\"language-bash\">sudo tailscale set --operator=$USER      # once — lets you run serve without sudo afterwards\ntailscale serve --bg &lt;APP_PORT&gt;          # e.g. 48080\ntailscale serve status                   # confirm: https://&lt;host&gt;.&lt;tailnet&gt;.ts.net → 127.0.0.1:&lt;APP_PORT&gt;\n</code></pre>\n<p>Everyone then uses the <strong>HTTPS MagicDNS URL with no port</strong> — e.g.\n<code>https://your-host.your-tailnet.ts.net</code> — both in the browser and as the member endpoint\n(<code>agentop member connect --endpoint https://your-host.your-tailnet.ts.net …</code>). <code>serve</code> proxies\nthe reverse-channel WebSocket (<code>/api/team/agent</code>) too, so presence and on-demand chat keep\nworking. Undo with <code>tailscale serve reset</code>. (Requires HTTPS certificates enabled for your\ntailnet — the default on modern Tailscale.)</p>\n<hr>\n<h2 id=\"upgrading-an-existing-central\">Upgrading an existing central</h2>\n<p>Three changes in this release affect an instance that is already running. <code>central.sh up</code>\nhandles the first two for you and warns about the third.</p>\n<table>\n<thead>\n<tr>\n<th>Change</th>\n<th>Effect</th>\n<th>What happens</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>The container now runs as <strong>uid 10001</strong> instead of root</td>\n<td>the existing <code>agentistics_data</code> volume is owned by root, so the app could not write preferences or the consolidate store</td>\n<td><code>central.sh up</code> chowns the volume once, automatically. Doing it by hand: <code>docker run --rm --user 0 -v team-mode_agentistics_data:/d alpine chown -R 10001:10001 /d</code></td>\n</tr>\n<tr>\n<td><strong><code>BIND_IP</code> now defaults to <code>127.0.0.1</code></strong> instead of <code>0.0.0.0</code></td>\n<td>a central teammates reached directly over the LAN or a tailnet becomes unreachable</td>\n<td><code>central.sh up</code> prints a notice when <code>BIND_IP</code> is unset. Add <code>BIND_IP=0.0.0.0</code> (LAN, as before) or your Tailscale address to <code>central.env</code>. Tunnels and local access are unaffected</td>\n</tr>\n<tr>\n<td>The <strong>session cookie format changed</strong> (it now carries a signed issue time for the idle timeout)</td>\n<td>every existing session cookie is invalid</td>\n<td>Everyone signs in again once, after the upgrade. No action needed</td>\n</tr>\n</tbody></table>\n<p>Nothing else migrates: accounts, machine tokens, teams, tags and all metrics are untouched, and\nexisting passwords keep working — the 12-character policy applies when a password is <em>set</em>, not\nwhen it is used.</p>\n<hr>\n<h2 id=\"security-controls\">Security controls</h2>\n<p>A central is multi-tenant and can be published on the internet, so it carries controls a solo\nmachine does not. This is the summary. The <strong>security model</strong> — threat model, trust boundaries,\nthe request pipeline and the limits of each control — is <strong><a href=\"https://github.com/blpsoares/agentistics/blob/main/security.md\" target=\"_blank\" rel=\"noopener\">docs/security.md</a></strong>;\nthe deployment runbook and go-live checklist are <strong><a href=\"https://github.com/blpsoares/agentistics/blob/main/exposure.md\" target=\"_blank\" rel=\"noopener\">docs/exposure.md</a></strong>.</p>\n<table>\n<thead>\n<tr>\n<th>Control</th>\n<th>Where</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Exposure profile</strong> — <code>local</code> / <code>lan</code> / <code>public</code> decides which host-power routes exist at all. <code>public</code> permanently revokes <code>/api/exec</code>, <code>/api/chat-tty</code>, the host transcript readers and <code>/api/mcp-action</code>, checked <em>before</em> the auth gate</td>\n<td><code>server/exposure.ts</code>, <code>server/capability-guard.ts</code></td>\n</tr>\n<tr>\n<td><strong>Accounts</strong> — argon2id hashes, generic 401 (no user enumeration), <code>sessionVersion</code> revokes every live cookie instantly, role + memberships read fresh from the DB per request</td>\n<td><code>server/accounts.ts</code>, <code>server/auth.ts</code></td>\n</tr>\n<tr>\n<td><strong>Two-factor</strong> — RFC 6238 TOTP with single-use recovery codes (sha256-hashed). The password stage issues no cookie when a factor is enrolled; a <code>public</code> profile requires it of every owner</td>\n<td><code>server/totp.ts</code>, <code>server/mfa-store.ts</code></td>\n</tr>\n<tr>\n<td><strong>Password policy</strong> — 12-character floor with a 1024 ceiling, common-password blocklist, rejects anything containing the account&#39;s own name or e-mail</td>\n<td><code>server/password-policy.ts</code></td>\n</tr>\n<tr>\n<td><strong>Rate limiting</strong> — 5 logins per 15 min per IP with doubling backoff; a separate soft per-account bucket checked <em>before</em> the argon2 verify; token and ingest rules of their own</td>\n<td><code>server/rate-limit.ts</code></td>\n</tr>\n<tr>\n<td><strong>Step-up (&quot;sudo mode&quot;)</strong> — creating/editing/deleting an account, deleting a team and changing a password each require a fresh five-minute grant obtained with the password or a TOTP code, so a stolen cookie alone cannot promote itself or take over the account</td>\n<td><code>server/stepup.ts</code></td>\n</tr>\n<tr>\n<td><strong>Session cookie</strong> — <code>__Host-</code> prefixed when Secure, <code>SameSite=Strict</code>, <code>HttpOnly</code>, 12h idle timeout with a 15-minute sliding refresh inside a 7-day absolute cap</td>\n<td><code>server/auth.ts</code></td>\n</tr>\n<tr>\n<td><strong>Session secret</strong> — never derived from the dashboard password; a value equal to it refuses to boot</td>\n<td><code>server/secret-store.ts</code></td>\n</tr>\n<tr>\n<td><strong>Security headers</strong> — CSP (no inline script, <code>frame-ancestors &#39;none&#39;</code>), HSTS under TLS, <code>X-Content-Type-Options</code>, <code>Referrer-Policy</code>, <code>Permissions-Policy</code>, COOP/CORP, <code>no-store</code> on <code>/api</code></td>\n<td><code>server/security-headers.ts</code></td>\n</tr>\n<tr>\n<td><strong>CORS + CSRF</strong> — explicit origin allowlist (no wildcard, no ACAO for an unknown origin); unsafe methods carrying a cookie must prove same-origin provenance. Bearer clients are exempt, since CSRF rides cookies</td>\n<td><code>server/cors.ts</code>, <code>server/csrf.ts</code></td>\n</tr>\n<tr>\n<td><strong>Authorization</strong> — deny-by-default gate; owner-only admin paths incl. nested detail routes; per-team scoping of every response. The exact public allowlist is asserted by a test</td>\n<td><code>server/index-routes.ts</code>, <code>server/team-scope.ts</code>, <code>server/authz-gate.test.ts</code></td>\n</tr>\n<tr>\n<td><strong>Audit log</strong> — append-only, 180-day TTL: logins, failures, lockouts, MFA events, password changes, account/team/token changes, every gate denial. Secret-shaped fields are redacted before write. Owner-only at <code>GET /api/iam/audit</code></td>\n<td><code>server/audit.ts</code></td>\n</tr>\n<tr>\n<td><strong>Resource limits</strong> — bodies read through a byte counter that abandons an oversized payload mid-stream, SSE client cap, <code>maxRequestBodySize</code>, timeouts on every outbound fetch</td>\n<td><code>server/limits.ts</code></td>\n</tr>\n<tr>\n<td><strong>Error hygiene</strong> — clients get a code plus a correlation ref; the real message goes to the log only</td>\n<td><code>server/errors.ts</code></td>\n</tr>\n<tr>\n<td><strong>Container</strong> — uid 10001, read-only root filesystem, <code>cap_drop: ALL</code>, <code>no-new-privileges</code>, loopback bind, Mongo unpublished. Host harness dirs are mounted only when <code>AGENTISTICS_CENTRAL_USER</code> is set</td>\n<td><code>Dockerfile</code>, <code>docker-compose*.yml</code></td>\n</tr>\n</tbody></table>\n<p>Before exposing anything, run the preflight <strong>inside the container</strong>, where <code>central.env</code> is the\nlive environment and the database is reachable:</p>\n<pre><code class=\"language-bash\">./central.sh doctor --exposed        # repo checkout\nagentop central doctor --exposed     # standalone binary\n</code></pre>\n<p>It prints the nine-point checklist and exits non-zero on any failure. A check that could not be\nverified (e.g. the database was unreachable) reports a failure, never a reassuring pass.</p>\n<hr>\n<h2 id=\"generating-a-env-via-the-api\">Generating a `.env` via the API</h2>\n<p>When the server is running in central mode you can generate a pre-filled <code>central.env</code> via:</p>\n<pre><code class=\"language-bash\">curl -s http://localhost:48080/api/team/deploy | jq .\n</code></pre>\n<p>The response includes:</p>\n<ul>\n<li><code>env</code> — ready-to-write <code>central.env</code> file content</li>\n<li><code>command</code> — <code>docker compose --env-file central.env up -d</code></li>\n<li><code>password</code> / <code>sessionSecret</code> — the generated secrets (shown <strong>once</strong>; not stored by the server)</li>\n</ul>\n<blockquote>\n<p>Store these values immediately. The server never logs or re-exposes them.</p>\n</blockquote>\n<hr>\n<h2 id=\"connect-a-member\">Connect a member</h2>\n<p>A <strong>member</strong> is a developer&#39;s machine that pushes its <em>computed</em> metrics (never chat content)\nto the central. Membership is configured with the <code>agentop</code> binary — the central never needs\nto reach back out to the member to onboard it.</p>\n<h3 id=\"1-mint-a-token-on-the-central\">1. Mint a token on the central</h3>\n<p>Log in to the central dashboard, open <strong>Settings → Team</strong>, and mint a token for the new\nmachine. The <strong>machine&#39;s name is set here, on the token</strong> — there is no name field on the\nmember side; the member resolves its own name from the central via <code>/api/team/whoami</code>.</p>\n<p>From the members panel you can also <strong>rotate</strong> a token (issues a new credential while\nmigrating that member&#39;s sessions + stats to the new identity, so history is preserved),\n<strong>revoke</strong> it (confirmation modal, then cascade-deletes that member&#39;s data), and <strong>rename</strong>\nthe machine.</p>\n<h3 id=\"2-connect-the-member-machine\">2. Connect the member machine</h3>\n<p>Interactive wizard (recommended for first-time setup — pick &quot;join a central&quot; and paste the\nendpoint + token):</p>\n<pre><code class=\"language-bash\">agentop setup\n</code></pre>\n<p>Non-interactive equivalent:</p>\n<pre><code class=\"language-bash\">agentop member connect --endpoint http://&lt;central-host&gt;:48080 --token &lt;minted-token&gt; [--org &lt;org&gt;]\n</code></pre>\n<p><code>member connect</code> verifies the token against <code>GET &lt;endpoint&gt;/api/team/whoami</code> before writing\nanything — a bad token never leaves a half-written config. On success it prints\n<code>connected as &lt;name&gt;</code> (the name comes from the central).</p>\n<blockquote>\n<p>Use the central&#39;s reachable address for <code>--endpoint</code>. Inside a tailnet this is the central&#39;s\nTailscale IP (e.g. <code>http://100.x.y.z:48080</code>). The bearer token is stored locally and never\nlogged.</p>\n</blockquote>\n<h3 id=\"3-check-leave\">3. Check / leave</h3>\n<pre><code class=\"language-bash\">agentop member status    # mode, endpoint, org, user + last sync state\nagentop member leave     # notify the central and reset this machine back to solo\n</code></pre>\n<p>Presence is <strong>WebSocket-authoritative</strong>: a member shows online in real time while its\nreverse channel is live and flips to offline within ~8s of the app being killed (a heartbeat\ncovers http-only members). Members follow the <strong>central&#39;s</strong> push cadence (default 30s, 15s floor, down to\n5s in express mode) and can only go slower, plus they push on local change (debounced). If the\ncentral DB is wiped, the token is rotated, or the endpoint changes, the member detects the\nsignature change and re-pushes its full history automatically — no manual <code>team-sent.json</code>\nreset. A revoked machine auto-resets itself back to solo.</p>\n<h3 id=\"machine-in-docker\">Machine in Docker</h3>\n<p>A machine (solo or member) can also run in a container instead of natively. Configure it on the\nhost first (<code>agentop member connect …</code>, which writes <code>~/.agentistics</code>), then bring the container\nup from the repo — <code>agentop start</code> offers this as the <strong>docker</strong> option, or run it directly:</p>\n<pre><code class=\"language-bash\">docker compose -f docker-compose.machine.yml up -d --build   # web: http://localhost:47292\ndocker compose -f docker-compose.machine.yml logs -f\ndocker compose -f docker-compose.machine.yml down\n</code></pre>\n<p>It reuses the same image as the central (minus Mongo and central mode), mounts the host&#39;s harness\ndirs (<code>~/.claude</code>, <code>~/.codex</code>, <code>~/.gemini</code> — which also contains Antigravity&#39;s <code>antigravity-cli/</code> — and <code>~/.copilot</code>) <strong>read-only</strong>, mounts <code>~/.agentistics</code>\nread-write (so it inherits the endpoint/token and persists the archive/sync state), and uses host\nnetworking so it reaches the central and opens the reverse channel.</p>\n<blockquote>\n<p>Run the machine in Docker <strong>or</strong> natively — not both at once. Two members sharing the same token\nwould push the same data and flap presence. The container writes <code>~/.agentistics</code> as root; <code>chown</code>\nit back to your user if you later switch to running natively.</p>\n</blockquote>\n<hr>\n<h2 id=\"autostart-with-agentop\">Autostart with `agentop`</h2>\n<p>Beyond the Docker snippets below, the <code>agentop</code> binary can register itself as a <strong>systemd\n<em>user</em> service</strong> (no root) so a mode starts with the system:</p>\n<pre><code class=\"language-bash\">agentop autostart server enable      # a member/solo dashboard + daemon\nagentop autostart central enable     # the Docker central (runs central.sh up)\nagentop autostart watch enable       # the OpenTelemetry daemon only\n\nagentop autostart server disable     # stop + remove the service\nagentop autostart status             # state of every service (omit the mode to list all)\n</code></pre>\n<p>On Linux/WSL, <code>enable</code> writes <code>~/.config/systemd/user/agentop-&lt;mode&gt;.service</code>, runs\n<code>systemctl --user enable --now agentop-&lt;mode&gt;</code>, and sets <code>loginctl enable-linger &lt;you&gt;</code> so the\nservice also starts at boot without an active login. It additionally installs a guarded hook in\n<code>~/.bashrc</code> that runs <code>agentop check-update</code> on every terminal open (prints a banner only when\nan update exists). macOS (launchd) and Windows (Task Scheduler) are not yet wired up — those\nplatforms print the exact manual step instead.</p>\n<blockquote>\n<p><code>autostart central</code> runs <code>central.sh up</code>, so it needs the agentistics repo present. For a\nproduction central you can use either this or the raw systemd unit in <strong>Autostart Snippets</strong>\nbelow.</p>\n</blockquote>\n<hr>\n<h2 id=\"mongodb-replica-set\">MongoDB Replica Set</h2>\n<p>The bundled <code>mongo</code> service starts with <code>--replSet rs0</code>. The healthcheck runs an\n<strong>idempotent</strong> <code>rs.initiate()</code> that configures the replica set on first boot and is\na no-op thereafter:</p>\n<pre><code class=\"language-javascript\">try {\n  rs.status();\n} catch (e) {\n  rs.initiate({ _id: &#39;rs0&#39;, members: [{ _id: 0, host: &#39;mongo:27017&#39; }] });\n}\n</code></pre>\n<p>The member host <strong>must</strong> be <code>mongo:27017</code> — the same hostname the <code>app</code> service uses in\n<code>MONGO_URL</code> — so the driver&#39;s replica set topology check passes.</p>\n<p>MongoDB does <strong>not</strong> publish port <code>27017</code> to the host; only the <code>app</code> container can reach it.</p>\n<hr>\n<h2 id=\"autostart-snippets-raw-for-the-docker-central\">Autostart Snippets (raw — for the Docker central)</h2>\n<p>These run the Docker central directly via the host&#39;s init system. Prefer them over\n<code>agentop autostart central</code> when the machine has no <code>agentop</code> binary or you want to manage the\ncompose stack yourself.</p>\n<h3 id=\"systemd-linux-recommended-for-production-servers\">systemd (Linux — recommended for production servers)</h3>\n<p>Save as <code>/etc/systemd/system/agentistics.service</code>:</p>\n<pre><code class=\"language-ini\">[Unit]\nDescription=agentistics Team Dashboard\nAfter=network.target docker.service\nRequires=docker.service\n\n[Service]\nType=simple\nWorkingDirectory=/opt/agentistics\nExecStart=docker compose --env-file central.env up -d\nRestart=on-failure\nRestartSec=5\nStandardOutput=journal\nStandardError=journal\n\n[Install]\nWantedBy=multi-user.target\n</code></pre>\n<p>Enable and start:</p>\n<pre><code class=\"language-bash\">sudo systemctl daemon-reload\nsudo systemctl enable --now agentistics\n</code></pre>\n<hr>\n<h3 id=\"launchd-macos\">launchd (macOS)</h3>\n<p>Save as <code>~/Library/LaunchAgents/com.agentistics.team.plist</code>:</p>\n<pre><code class=\"language-xml\">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;\n&lt;!DOCTYPE plist PUBLIC &quot;-//Apple//DTD PLIST 1.0//EN&quot;\n  &quot;http://www.apple.com/DTDs/PropertyList-1.0.dtd&quot;&gt;\n&lt;plist version=&quot;1.0&quot;&gt;\n&lt;dict&gt;\n  &lt;key&gt;Label&lt;/key&gt;\n  &lt;string&gt;com.agentistics.team&lt;/string&gt;\n  &lt;key&gt;ProgramArguments&lt;/key&gt;\n  &lt;array&gt;\n    &lt;string&gt;docker&lt;/string&gt;\n    &lt;string&gt;compose&lt;/string&gt;\n    &lt;string&gt;-f&lt;/string&gt;\n    &lt;string&gt;/opt/agentistics/docker-compose.yml&lt;/string&gt;\n    &lt;string&gt;up&lt;/string&gt;\n    &lt;string&gt;-d&lt;/string&gt;\n  &lt;/array&gt;\n  &lt;key&gt;RunAtLoad&lt;/key&gt;\n  &lt;true/&gt;\n  &lt;key&gt;KeepAlive&lt;/key&gt;\n  &lt;true/&gt;\n  &lt;key&gt;StandardOutPath&lt;/key&gt;\n  &lt;string&gt;/tmp/agentistics.log&lt;/string&gt;\n  &lt;key&gt;StandardErrorPath&lt;/key&gt;\n  &lt;string&gt;/tmp/agentistics.err&lt;/string&gt;\n&lt;/dict&gt;\n&lt;/plist&gt;\n</code></pre>\n<p>Load it:</p>\n<pre><code class=\"language-bash\">launchctl load ~/Library/LaunchAgents/com.agentistics.team.plist\n</code></pre>\n<hr>\n<h3 id=\"pm2-node-process-manager-useful-on-vps-without-systemd\">PM2 (Node process manager — useful on VPS without systemd)</h3>\n<p>Create <code>ecosystem.config.cjs</code> next to <code>docker-compose.yml</code>:</p>\n<pre><code class=\"language-javascript\">module.exports = {\n  apps: [{\n    name: &#39;agentistics&#39;,\n    script: &#39;docker&#39;,\n    args: &#39;compose up -d&#39;,\n    restart_delay: 5000,\n    env: { NODE_ENV: &#39;production&#39; },\n  }],\n}\n</code></pre>\n<p>Start and persist:</p>\n<pre><code class=\"language-bash\">pm2 start ecosystem.config.cjs\npm2 save\npm2 startup   # follow the printed command\n</code></pre>\n<hr>\n<h2 id=\"staying-up-to-date\">Staying up to date</h2>\n<p>agentistics surfaces &quot;update available&quot; everywhere: as a banner when you run a command, via\nthe <code>~/.bashrc</code> hook on terminal open/boot, and on the dashboard (a bell notification plus a\n<strong>mode-aware</strong> update modal with the exact command for your role). A periodic server re-check\n(every 6h) pushes the notification live over SSE.</p>\n<pre><code class=\"language-bash\">agentop check-update   # prints the banner if outdated, nothing if current\nagentop upgrade        # download + replace the binary with the latest release\nagentop --version      # current version (also flags an update if one exists)\n</code></pre>\n<h3 id=\"upgrade-a-central-docker\">Upgrade a central (Docker)</h3>\n<p>The dashboard update modal on a <strong>central</strong> shows a single command:</p>\n<pre><code class=\"language-bash\">bun run up:central     # = ./central.sh up — rebuild + --force-recreate with the new code\n</code></pre>\n<p>Raw-compose equivalent (from the repo, after <code>git pull</code>):</p>\n<pre><code class=\"language-bash\">git pull\ndocker compose -p team-mode --env-file central.env build --pull\ndocker compose -p team-mode --env-file central.env up -d --force-recreate\n</code></pre>\n<h3 id=\"upgrade-a-member\">Upgrade a member</h3>\n<p>The update modal on a <strong>member/solo</strong> machine shows:</p>\n<pre><code class=\"language-bash\">agentop upgrade                            # replace the binary\nsystemctl --user restart agentop-server    # restart the autostart service to run the new binary\n</code></pre>\n<p>If you don&#39;t run <code>agentop server</code> as a systemd user service, just restart it however you\nlaunched it (e.g. re-run <code>agentop server</code>).</p>\n<hr>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table>\n<thead>\n<tr>\n<th>Symptom</th>\n<th>Likely cause</th>\n<th>Fix</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>MongoServerError: not primary</code></td>\n<td>Replica set not yet initialised</td>\n<td>Wait for the <code>mongo</code> healthcheck to turn green (<code>docker compose ps</code>)</td>\n</tr>\n<tr>\n<td>Dashboard shows <strong>zero members</strong> after a host reboot</td>\n<td><code>mongo</code> container stayed down (e.g. OOM-killed on boot) while <code>app</code> came back</td>\n<td><code>docker start team-mode-mongo-1</code> — the <code>mongo_data</code> volume is intact, no data is lost. Permanent fix ships via the <code>restart: unless-stopped</code> policy on <code>mongo</code>; migrate an old deploy with <code>agentop central up</code> or <code>docker update --restart unless-stopped team-mode-mongo-1</code></td>\n</tr>\n<tr>\n<td>Dashboard shows 401 on every request</td>\n<td>Password set but no session cookie</td>\n<td>Navigate to the login page at <code>/login</code></td>\n</tr>\n<tr>\n<td>Port already in use</td>\n<td>Another process on the host port</td>\n<td>Change <code>APP_PORT</code> in <code>.env</code></td>\n</tr>\n<tr>\n<td>Container exits immediately</td>\n<td>Missing required env var</td>\n<td>Check <code>docker compose logs app</code></td>\n</tr>\n</tbody></table>\n",
    "headings": [
      {
        "id": "agentistics-docker-deployment-guide",
        "text": "agentistics — Docker Deployment Guide",
        "level": 1
      },
      {
        "id": "prerequisites",
        "text": "Prerequisites",
        "level": 2
      },
      {
        "id": "quick-start",
        "text": "Quick Start",
        "level": 2
      },
      {
        "id": "1-clone-the-repository-and-enter-the-project-root",
        "text": "1. Clone the repository and enter the project root",
        "level": 3
      },
      {
        "id": "2-create-your-centralenv",
        "text": "2. Create your `central.env`",
        "level": 3
      },
      {
        "id": "3-start-the-stack",
        "text": "3. Start the stack",
        "level": 3
      },
      {
        "id": "4-open-the-dashboard",
        "text": "4. Open the dashboard",
        "level": 3
      },
      {
        "id": "managing-the-central-centralsh",
        "text": "Managing the central — `central.sh`",
        "level": 2
      },
      {
        "id": "the-same-thing-from-the-cli-agentop-central",
        "text": "The same thing from the cli — `agentop central`",
        "level": 3
      },
      {
        "id": "external-database-atlas-remote-mongo-no-docker-required",
        "text": "External database (Atlas / remote Mongo) — no Docker required",
        "level": 3
      },
      {
        "id": "environment-variables",
        "text": "Environment Variables",
        "level": 2
      },
      {
        "id": "network-exposure-which-interface-the-dashboard-listens-on",
        "text": "Network exposure (which interface the dashboard listens on)",
        "level": 2
      },
      {
        "id": "wsl2-tailscale-use-tailscale-serve-recommended",
        "text": "WSL2 + Tailscale: use `tailscale serve` (recommended)",
        "level": 3
      },
      {
        "id": "upgrading-an-existing-central",
        "text": "Upgrading an existing central",
        "level": 2
      },
      {
        "id": "security-controls",
        "text": "Security controls",
        "level": 2
      },
      {
        "id": "generating-a-env-via-the-api",
        "text": "Generating a `.env` via the API",
        "level": 2
      },
      {
        "id": "connect-a-member",
        "text": "Connect a member",
        "level": 2
      },
      {
        "id": "1-mint-a-token-on-the-central",
        "text": "1. Mint a token on the central",
        "level": 3
      },
      {
        "id": "2-connect-the-member-machine",
        "text": "2. Connect the member machine",
        "level": 3
      },
      {
        "id": "3-check-leave",
        "text": "3. Check / leave",
        "level": 3
      },
      {
        "id": "machine-in-docker",
        "text": "Machine in Docker",
        "level": 3
      },
      {
        "id": "autostart-with-agentop",
        "text": "Autostart with `agentop`",
        "level": 2
      },
      {
        "id": "mongodb-replica-set",
        "text": "MongoDB Replica Set",
        "level": 2
      },
      {
        "id": "autostart-snippets-raw-for-the-docker-central",
        "text": "Autostart Snippets (raw — for the Docker central)",
        "level": 2
      },
      {
        "id": "systemd-linux-recommended-for-production-servers",
        "text": "systemd (Linux — recommended for production servers)",
        "level": 3
      },
      {
        "id": "launchd-macos",
        "text": "launchd (macOS)",
        "level": 3
      },
      {
        "id": "pm2-node-process-manager-useful-on-vps-without-systemd",
        "text": "PM2 (Node process manager — useful on VPS without systemd)",
        "level": 3
      },
      {
        "id": "staying-up-to-date",
        "text": "Staying up to date",
        "level": 2
      },
      {
        "id": "upgrade-a-central-docker",
        "text": "Upgrade a central (Docker)",
        "level": 3
      },
      {
        "id": "upgrade-a-member",
        "text": "Upgrade a member",
        "level": 3
      },
      {
        "id": "troubleshooting",
        "text": "Troubleshooting",
        "level": 2
      }
    ]
  },
  "github-actions": {
    "id": "github-actions",
    "file": "docs/github-actions.md",
    "title": "GitHub Actions",
    "group": "Team Mode",
    "html": "<h1 id=\"github-actions-track-claude-code-actions-usage-per-repositor\">GitHub Actions — track Claude Code Actions usage per repository</h1>\n<p>agentistics can track token/cost usage from <strong>Claude Code running inside GitHub Actions</strong>, not\njust from developers&#39; laptops. Each Actions run is an <em>ephemeral machine</em>: it populates\n<code>~/.claude</code>, then dies. <code>agentop ci-push</code> sends that run&#39;s computed metrics to your <strong>central</strong>\nbefore the runner is torn down, attributed to the repository it ran in — so a repo&#39;s dashboard\nshows <strong>local devs + cloud agents together</strong>, grouped by git remote.</p>\n<p>This builds on the <a href=\"https://github.com/blpsoares/agentistics/blob/main/architecture.md\" target=\"_blank\" rel=\"noopener\">repository dimension</a>: every session carries a normalized\n<code>git_remote</code> (<code>host/org/repo</code>, no protocol), and CI runs are additionally flagged <code>ci: true</code> and\nsurfaced under <strong>Repositories → Actions</strong>.</p>\n<h2 id=\"how-it-works-keyless-github-oidc-recommended\">How it works — keyless (GitHub OIDC), recommended</h2>\n<pre><code>GitHub Actions runner (ephemeral)\n  ├── Claude Code Action runs  → writes ~/.claude\n  └── agentop ci-push          → GitHub mints a short-lived OIDC JWT (repository claim)\n                                  → POST /api/team/ingest  (Bearer &lt;OIDC JWT&gt;)\n                                        ↓\n                    Central verifies the JWT vs GitHub&#39;s JWKS (issuer/audience/expiry),\n                    checks `repository` ∈ registered repos, then stamps git_remote + ci=true\n                                        ↓\n                            Repositories → Actions  (per repo, cross-user)\n</code></pre>\n<ul>\n<li><strong>Keyless — no secret to store or leak.</strong> GitHub signs a short-lived token identifying the exact\n<code>repository</code>; the central verifies it cryptographically. There is no long-lived secret in the repo.</li>\n<li><strong>Attribution is authoritative on the central.</strong> <code>git_remote</code>, <code>ci: true</code>, and\n<code>user = github-actions</code> are stamped from the <em>verified</em> claim — a runner cannot mis-report its repo.</li>\n<li><strong>No chat leaves the runner</strong> — computed metrics only (sessions/tokens/cost aggregates +\nstatsCache), never raw transcripts.</li>\n<li><strong><code>ci-push</code> never fails your job.</strong> A push error (central down, unverifiable token) logs and exits 0.</li>\n</ul>\n<p>A <strong>static repo token</strong> is also supported as a fallback (non-GitHub CI, or when OIDC is unavailable) —\nsee the end of this page.</p>\n<h2 id=\"1-register-the-repository-on-the-central\">1. Register the repository on the central</h2>\n<p>Registration is an <strong>admin action</strong> (central Team settings → Repositories, or the API). For keyless\nOIDC it just <strong>allowlists</strong> the repo — no secret is stored.</p>\n<pre><code class=\"language-bash\"># From an authenticated admin session on the central (cookie from the dashboard login):\ncurl -sS -X POST &quot;$CENTRAL_URL/api/team/repos&quot; \\\n  -H &#39;Content-Type: application/json&#39; \\\n  -H &quot;Cookie: $ADMIN_SESSION_COOKIE&quot; \\\n  -d &#39;{ &quot;url&quot;: &quot;git@github.com:org/repo.git&quot; }&#39;\n# → { &quot;token&quot;: &quot;&lt;static token — only needed for the fallback path&gt;&quot;, &quot;remote&quot;: &quot;github.com/org/repo&quot; }\n# (the display name is always derived from the remote — org/repo — there is no name field)\n</code></pre>\n<p><code>url</code> accepts any remote form (https / ssh / scp); it is normalized to <code>github.com/org/repo</code>.\n(List/unregister: <code>GET /api/team/repos</code>, <code>DELETE /api/team/repos { &quot;remote&quot;: … }</code>.)</p>\n<p>Then enable OIDC on the central by setting an <strong>audience</strong> (any stable value; using the central URL\nis natural and unique per central):</p>\n<pre><code class=\"language-bash\">AGENTISTICS_OIDC_AUDIENCE=&quot;https://central.example.com&quot;   # central env; the workflow requests this same audience\n</code></pre>\n<h2 id=\"2-add-the-push-step-to-your-workflow-keyless\">2. Add the push step to your workflow (keyless)</h2>\n<p>The job needs <code>id-token: write</code> so GitHub will mint an OIDC token; add the push as the <strong>last</strong> step\nof the job that already runs Claude Code (it does <strong>not</strong> run Claude — your workflow already does):</p>\n<pre><code class=\"language-yaml\">jobs:\n  claude:\n    permissions:\n      id-token: write        # ← lets the runner request a GitHub OIDC token\n      contents: write        # (your existing Claude permissions)\n    steps:\n      # ... your existing Claude Code Action step ...\n\n      - name: Push agentistics metrics\n        if: always()          # report usage even if the Claude step failed\n        env:\n          AGENTISTICS_CENTRAL_URL: ${{ vars.AGENTISTICS_CENTRAL_URL }}\n        run: |\n          curl -fsSL &quot;https://github.com/blpsoares/agentistics/releases/latest/download/agentop&quot; -o agentop\n          chmod +x agentop\n          ./agentop ci-push\n</code></pre>\n<p><code>ci-push</code> requests the OIDC token itself (audience defaults to <code>AGENTISTICS_CENTRAL_URL</code>), so the\nonly repo config is the <code>AGENTISTICS_CENTRAL_URL</code> <strong>variable</strong> — no secret:</p>\n<pre><code class=\"language-bash\">gh variable set AGENTISTICS_CENTRAL_URL --repo org/repo --body &#39;https://central.example.com&#39;\n</code></pre>\n<p>The central must be reachable from the runner (see Networking below).</p>\n<h3 id=\"authenticating-the-claude-step-api-key-or-your-promax-subscr\">Authenticating the Claude step — API key *or* your Pro/Max subscription</h3>\n<p>Two <strong>independent</strong> credentials are in play, and it helps to keep them separate:</p>\n<table>\n<thead>\n<tr>\n<th>Purpose</th>\n<th>Who handles it</th>\n<th>Credential</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Running Claude</strong> in the workflow</td>\n<td><code>anthropics/claude-code-action</code></td>\n<td><code>anthropic_api_key</code> <strong>or</strong> <code>claude_code_oauth_token</code></td>\n</tr>\n<tr>\n<td><strong>Pushing metrics</strong> to your central</td>\n<td><code>agentop ci-push</code> (this project)</td>\n<td>keyless GitHub OIDC (or the fallback <code>AGENTISTICS_CI_TOKEN</code>)</td>\n</tr>\n</tbody></table>\n<p>agentistics never touches your Anthropic credentials — it only reads the <code>~/.claude</code> the Claude\nstep already wrote. So <strong>yes, you can run the workflow on your Claude subscription</strong> instead of a\nmetered API key. The official action supports a <strong>Pro/Max OAuth token</strong>: generate it once locally\nwith</p>\n<pre><code class=\"language-bash\">claude setup-token          # requires a Claude Pro or Max subscription\n</code></pre>\n<p>store the printed value as the repo secret <code>CLAUDE_CODE_OAUTH_TOKEN</code>, and point the action at it:</p>\n<pre><code class=\"language-yaml\">      - name: Run Claude Code\n        uses: anthropics/claude-code-action@v1\n        with:\n          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}\n          # (instead of: anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }})\n</code></pre>\n<p>Either way the metrics push step below is <strong>unchanged</strong> — it works the same on API-key or\nsubscription auth. Trade-offs to know: an OAuth token is tied to <strong>one personal account</strong> and its\nsubscription rate limits (fine for a small repo, less so for a busy shared one), and inline-comment\nclassification currently still needs <code>anthropic_api_key</code>. For heavy or org-wide automation a\ndedicated API key (or Bedrock/Vertex) is usually steadier.</p>\n<h2 id=\"3-see-it-in-the-dashboard\">3. See it in the dashboard</h2>\n<p>Open <strong>Repositories</strong> → click the repo → <strong>Actions</strong> tab, or <strong>Repositories → Actions</strong> for the\ncross-repo view. CI runs are counted separately (<code>user = github-actions</code>) but roll up into the\nsame per-repo totals as your local sessions.</p>\n<h2 id=\"networking-how-a-cloud-runner-reaches-the-central\">Networking — how a cloud runner reaches the central</h2>\n<p>GitHub-hosted runners live in the cloud, so the central must be reachable from wherever the\nrunner runs. <strong>Do not expose your full dashboard to the public internet</strong> — it holds sensitive\ndata (repo names, member names, the first 200 chars of each prompt, titles). Pick one:</p>\n<h3 id=\"option-a-private-runner-nothing-exposed\">Option A — private runner (nothing exposed)</h3>\n<p>Use a <strong>self-hosted runner</strong> inside your network / tailnet and point it at the private central\n(<code>http://localhost:48080</code>, a LAN IP, or a Tailscale MagicDNS URL). Set <code>runs-on: self-hosted</code>.\nThe central never touches the public internet. Best for sensitive data.</p>\n<h3 id=\"option-b-a-public-ingest-only-central-for-cloud-runners\">Option B — a public **ingest-only** central (for cloud runners)</h3>\n<p>Run a <strong>second</strong> central instance in <strong>ingest-only mode</strong> that shares the same MongoDB as your\nprivate dashboard instance. Set <code>AGENTISTICS_INGEST_ONLY=1</code>: it serves <strong>only</strong>\n<code>POST /api/team/ingest</code> and returns <strong>404 for everything else</strong> — no dashboard, no <code>/api/data</code>,\nno login, no static assets. Exposing it is low-risk: an attacker finds only a token-gated write\nendpoint with nothing to read.</p>\n<pre><code>        cloud runner ── POST /api/team/ingest ──▶  ingest-only central   ┐\n                                                   (AGENTISTICS_INGEST_ONLY=1)  │  same\n                                                                                ├─ MongoDB\n   you (private) ──────── dashboard ─────────────▶  private central       ┘\n                                                   (Tailscale / LAN, not public)\n</code></pre>\n<p>Harden the exposed instance further:</p>\n<ul>\n<li><strong>IP-allowlist</strong> GitHub&#39;s runner ranges at the proxy (<code>actions</code> field of\n<code>https://api.github.com/meta</code>).</li>\n<li>Terminate <strong>TLS</strong> (a Cloudflare Tunnel / Tailscale Funnel gives HTTPS for free; or a reverse\nproxy + <code>AGENTISTICS_TEAM_TLS=1</code>).</li>\n</ul>\n<h2 id=\"static-token-fallback-non-github-ci-or-no-oidc\">Static-token fallback (non-GitHub CI, or no OIDC)</h2>\n<p>If OIDC isn&#39;t available, use the repo-bound static token from registration. Store it as a secret\nand add it to the step&#39;s env — <code>ci-push</code> uses it when no OIDC token can be fetched:</p>\n<pre><code class=\"language-bash\">gh secret set AGENTISTICS_CI_TOKEN --repo org/repo --body &#39;&lt;the token&gt;&#39;\n</code></pre>\n<pre><code class=\"language-yaml\">        env:\n          AGENTISTICS_CENTRAL_URL: ${{ vars.AGENTISTICS_CENTRAL_URL }}\n          AGENTISTICS_CI_TOKEN: ${{ secrets.AGENTISTICS_CI_TOKEN }}\n</code></pre>\n<p>Re-registering the repo <strong>rotates</strong> the token; removing the repo revokes it (both delete that\nrepo&#39;s CI data).</p>\n<h2 id=\"security-notes\">Security notes</h2>\n<ul>\n<li><strong>Prefer keyless OIDC</strong> — it stores no long-lived secret, so there is nothing to leak or rotate.\nThe token is short-lived, GitHub-signed, and cryptographically bound to the <code>repository</code>.</li>\n<li>The static fallback token is stored only as a <strong>SHA-256 hash</strong> on the central and used as\n<code>Authorization: Bearer</code>; treat it like any secret.</li>\n<li>A repo must be <strong>registered</strong> (allowlisted) on the central before either path is accepted — a\nvalid OIDC token for an unregistered repo is rejected (403).</li>\n<li>On a public central, set a strong <code>AGENTISTICS_TEAM_SESSION_SECRET</code> (the dashboard itself is\ngated by accounts — there is no shared password to set), and prefer the ingest-only pattern\nabove so the dashboard is never the exposed surface.</li>\n</ul>\n",
    "headings": [
      {
        "id": "github-actions-track-claude-code-actions-usage-per-repositor",
        "text": "GitHub Actions — track Claude Code Actions usage per repository",
        "level": 1
      },
      {
        "id": "how-it-works-keyless-github-oidc-recommended",
        "text": "How it works — keyless (GitHub OIDC), recommended",
        "level": 2
      },
      {
        "id": "1-register-the-repository-on-the-central",
        "text": "1. Register the repository on the central",
        "level": 2
      },
      {
        "id": "2-add-the-push-step-to-your-workflow-keyless",
        "text": "2. Add the push step to your workflow (keyless)",
        "level": 2
      },
      {
        "id": "authenticating-the-claude-step-api-key-or-your-promax-subscr",
        "text": "Authenticating the Claude step — API key *or* your Pro/Max subscription",
        "level": 3
      },
      {
        "id": "3-see-it-in-the-dashboard",
        "text": "3. See it in the dashboard",
        "level": 2
      },
      {
        "id": "networking-how-a-cloud-runner-reaches-the-central",
        "text": "Networking — how a cloud runner reaches the central",
        "level": 2
      },
      {
        "id": "option-a-private-runner-nothing-exposed",
        "text": "Option A — private runner (nothing exposed)",
        "level": 3
      },
      {
        "id": "option-b-a-public-ingest-only-central-for-cloud-runners",
        "text": "Option B — a public **ingest-only** central (for cloud runners)",
        "level": 3
      },
      {
        "id": "static-token-fallback-non-github-ci-or-no-oidc",
        "text": "Static-token fallback (non-GitHub CI, or no OIDC)",
        "level": 2
      },
      {
        "id": "security-notes",
        "text": "Security notes",
        "level": 2
      }
    ]
  },
  "security": {
    "id": "security",
    "file": "docs/security.md",
    "title": "Security model",
    "group": "Security",
    "html": "<h1 id=\"security-model\">Security model</h1>\n<p>What protects an Agentistics instance, how the pieces fit together, and — as importantly —\nwhat each control does <strong>not</strong> do.</p>\n<p>This is the reference. Two neighbouring documents cover the other angles:\n<a href=\"https://github.com/blpsoares/agentistics/blob/main/exposure.md\" target=\"_blank\" rel=\"noopener\">exposure.md</a> is the operator runbook for publishing a central, and\n<a href=\"https://github.com/blpsoares/agentistics/blob/main/../SECURITY.md\" target=\"_blank\" rel=\"noopener\">SECURITY.md</a> is the vulnerability disclosure policy.</p>\n<hr>\n<h2 id=\"1-what-is-being-protected\">1. What is being protected</h2>\n<p>A central holds, for every developer and CI runner in an organisation: project paths, git\nremotes, session titles and first prompts, token and cost aggregates, machine tokens (hashed),\naccount password hashes, and — only when <code>AGENTISTICS_CENTRAL_USER</code> is set — read-only mounts of\nthe host&#39;s <code>~/.claude</code>, <code>~/.codex</code>, <code>~/.gemini</code> and <code>~/.copilot</code>, which contain <strong>raw\nconversation transcripts</strong>.</p>\n<p>Members never push chat. Raw transcripts are fetched on demand over the reverse WebSocket and\nare never stored centrally.</p>\n<h2 id=\"2-threat-model\">2. Threat model</h2>\n<p>Defended against:</p>\n<table>\n<thead>\n<tr>\n<th>Attacker</th>\n<th>Primary controls</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Unauthenticated internet</strong> — scans the hostname, hits every route, brute-forces login, fuzzes tokens</td>\n<td>capability guard, deny-by-default gate, rate limiting, security headers</td>\n</tr>\n<tr>\n<td><strong>Authenticated low-privilege account</strong> (legitimate or stolen) escalating to owner, to other teams&#39; data, or to the host</td>\n<td>role gate, per-team scoping, capability guard, step-up</td>\n</tr>\n<tr>\n<td><strong>Malicious website in a logged-in user&#39;s browser</strong></td>\n<td><code>SameSite=Strict</code>, CSRF origin checks, CSP <code>frame-ancestors &#39;none&#39;</code>, no wildcard CORS</td>\n</tr>\n<tr>\n<td><strong>Compromised member machine</strong> with a leaked machine token</td>\n<td>per-machine tokens, individually revocable, sha256-hashed at rest</td>\n</tr>\n<tr>\n<td><strong>Supply chain</strong> — a malicious transitive dependency or tampered image</td>\n<td><code>bun audit</code> in CI, lockfile drift check, Dependabot</td>\n</tr>\n</tbody></table>\n<p>Explicitly <strong>out of scope</strong> (see §7 for why this matters):</p>\n<ul>\n<li>A compromised <strong>host</strong>. Shell on the machine reads <code>central.env</code> and the Mongo volume.</li>\n<li>A malicious or compromised <strong>owner account</strong>. Owner is fully trusted by design.</li>\n<li>A compromised <strong>Cloudflare account</strong> or tunnel credential.</li>\n<li>Physical access.</li>\n<li>Vulnerabilities in the AI coding assistants whose data this project reads.</li>\n</ul>\n<h2 id=\"3-trust-boundaries\">3. Trust boundaries</h2>\n<pre><code>   internet ─┬─► Cloudflare edge  (WAF, rate limit, optional Access)\n             │\n             └─► tunnel (outbound-only; no inbound port on the host)\n                   │\n                   ▼\n              ┌──────────────────────────────────────────┐\n              │ container: uid 10001, read-only rootfs,   │\n              │            cap_drop ALL, no-new-privs     │\n              │  ┌────────────────────────────────────┐   │\n              │  │ app: capability guard → rate limit │   │\n              │  │      → CSRF → auth → role → MFA    │   │\n              │  │      → step-up → handler → scoping │   │\n              │  └────────────────────────────────────┘   │\n              └───────────────┬──────────────────────────┘\n                              │ compose network only\n                              ▼\n                        MongoDB (never published, never tunnelled)\n</code></pre>\n<p>Each boundary is independent. A failure of the application logic still meets a container with no\nroot, no capabilities and an immutable filesystem; a failure of the container still meets a host\nwith no inbound port open.</p>\n<h2 id=\"4-the-request-pipeline\">4. The request pipeline</h2>\n<p>Order matters, and every step is where it is for a reason\n(<code>packages/server/server/index.ts</code>):</p>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>Step</th>\n<th>Why here</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td>Path normalisation</td>\n<td><code>//api/x</code> must not slip past exact-match route tables</td>\n</tr>\n<tr>\n<td>2</td>\n<td><code>INGEST_ONLY</code> short-circuit</td>\n<td>a public ingest instance exposes nothing else, not even a 401</td>\n</tr>\n<tr>\n<td>3</td>\n<td>Client IP resolution</td>\n<td>everything below keys off it; forwarded headers only trusted under <code>AGENTISTICS_TRUST_PROXY</code></td>\n</tr>\n<tr>\n<td>4</td>\n<td><strong>Rate limiting</strong></td>\n<td>before any expensive work, so an unauthenticated caller cannot spend CPU</td>\n</tr>\n<tr>\n<td>5</td>\n<td><strong>CSRF</strong></td>\n<td>before auth, so a cross-site request is refused without touching the session</td>\n</tr>\n<tr>\n<td>6</td>\n<td><strong>Capability guard</strong></td>\n<td>before auth, so an exposed instance does not reveal whether the caller is authenticated</td>\n</tr>\n<tr>\n<td>7</td>\n<td><strong>Auth</strong> — session cookie → principal</td>\n<td>deny-by-default: everything under <code>/api</code> outside <code>AUTH_PUBLIC</code></td>\n</tr>\n<tr>\n<td>8</td>\n<td><strong>Role</strong> — owner-only admin paths</td>\n<td>includes nested detail routes</td>\n</tr>\n<tr>\n<td>9</td>\n<td><strong>MFA enrolment</strong> gate</td>\n<td>a <code>public</code> owner without a second factor reaches only enrolment</td>\n</tr>\n<tr>\n<td>10</td>\n<td><strong>Step-up</strong></td>\n<td>destructive operations need proof of presence, not just of identity</td>\n</tr>\n<tr>\n<td>11</td>\n<td>Handler</td>\n<td>per-resource authority (tags by source, machines by ownership)</td>\n</tr>\n<tr>\n<td>12</td>\n<td><strong>Team scoping</strong> of the response</td>\n<td>a principal never receives another team&#39;s rows</td>\n</tr>\n<tr>\n<td>13</td>\n<td>Security headers stamped on the way out</td>\n<td>in a wrapper, so a new route cannot forget them</td>\n</tr>\n</tbody></table>\n<h2 id=\"5-identity-and-sessions\">5. Identity and sessions</h2>\n<p><strong>Login</strong> (<code>/api/iam/login</code>) verifies an argon2id hash and answers a generic 401 for both an\nunknown e-mail and a wrong password, so it cannot be used to enumerate accounts. When a second\nfactor is enrolled it issues <strong>no cookie</strong>: it returns a five-minute HMAC challenge that grants\nnothing on its own, exchanged at <code>/api/iam/login/mfa</code> for a session.</p>\n<p><strong>The session cookie</strong> is stateless: <code>expiryMs.accountId.sessionVersion.issuedAt.HMAC</code>. It is\n<code>HttpOnly</code>, <code>SameSite=Strict</code>, <code>Path=/</code>, and — whenever it is <code>Secure</code> — carries the <code>__Host-</code>\nprefix, which stops a sibling subdomain or a plain-HTTP network attacker from overwriting it.</p>\n<p>Three clocks bound it:</p>\n<table>\n<thead>\n<tr>\n<th>Clock</th>\n<th>Value</th>\n<th>Effect</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Absolute</td>\n<td>7 days</td>\n<td>hard ceiling regardless of activity</td>\n</tr>\n<tr>\n<td>Idle</td>\n<td>12 hours</td>\n<td>a cookie not reissued within the window is dead</td>\n</tr>\n<tr>\n<td>Refresh</td>\n<td>15 minutes</td>\n<td>active use reissues it, so a working session never hits the idle wall</td>\n</tr>\n</tbody></table>\n<p><strong>Revocation is immediate.</strong> Every account carries a <code>sessionVersion</code>; a password change, a\nlogout-all, enabling MFA or deleting the account bumps it, and every outstanding cookie —\nand any outstanding step-up grant — dies with it. Role and team memberships are read <strong>fresh\nfrom the database on every request</strong>, so a permission change takes effect on the next call, not\non the next login.</p>\n<p><strong>Step-up</strong> (<code>/api/iam/stepup</code>) covers what a session cannot: proof that the person is still\nthere. Creating, editing or deleting an account (role and memberships live there — it is where a\nsession becomes an owner), deleting a team, and changing a password each require a five-minute\ngrant obtained with the password or a TOTP code, presented in <code>X-Stepup</code>. It travels in a header\nrather than a cookie deliberately — a cookie would ride along automatically, which is the property\nbeing avoided.</p>\n<p>The list is short by decision. Enrolling a machine, minting or rotating its token and registering\na repository are <strong>not</strong> gated: they are routine work on a growing fleet, reversible by revoking\nthe token they mint, and bounded by the account they belong to — while a prompt met daily is one\npeople learn to clear without reading, which is paid for by the three prompts that matter.\n<code>stepup.test.ts</code> asserts the table exactly, so it cannot grow by accident.</p>\n<p>Three tokens are signed with the same key over similar payloads — session, MFA challenge,\nstep-up grant — and only <strong>domain separation</strong> stops one being replayed as another. There is a\ntest asserting exactly that (<code>auth-principal.test.ts</code>, <code>stepup.test.ts</code>).</p>\n<h2 id=\"6-the-controls-and-what-each-one-does-not-do\">6. The controls, and what each one does not do</h2>\n<table>\n<thead>\n<tr>\n<th>Control</th>\n<th>Does</th>\n<th>Does <strong>not</strong></th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Exposure profile</strong> (<code>exposure.ts</code>)</td>\n<td>decides whether host-power routes exist at all; <code>public</code> revokes them permanently and ignores the opt-in flag; an unknown value fails closed</td>\n<td>protect you from marking a public instance <code>local</code> — that env value is the trust anchor, which is why <code>doctor --exposed</code> re-checks against the strict bar</td>\n</tr>\n<tr>\n<td><strong>Capability guard</strong> (<code>capability-guard.ts</code>)</td>\n<td>403s <code>/api/exec</code>, <code>/api/chat-tty</code>, host transcript readers and MCP admin before auth</td>\n<td>cover a route nobody registered — an unregistered route is assumed harmless</td>\n</tr>\n<tr>\n<td><strong>Rate limiting</strong> (<code>rate-limit.ts</code>)</td>\n<td>5 logins / 15 min per IP with doubling backoff; a soft per-account bucket checked before the argon2 verify</td>\n<td>survive a process restart, or coordinate across replicas — the edge limiter is the front line</td>\n</tr>\n<tr>\n<td><strong>Password policy</strong> (<code>@agentistics/core</code>, re-exported by <code>password-policy.ts</code>)</td>\n<td>8-char floor, one uppercase, one symbol, 1024 ceiling</td>\n<td>a length floor beats composition rules (NIST SP 800-63B) — this is a deliberate product choice, taken knowing that; there is no breach-corpus or common-password check, so <code>Agentistics@123!</code> is accepted</td>\n</tr>\n<tr>\n<td><strong>TOTP</strong> (<code>totp.ts</code>)</td>\n<td>RFC 6238 second factor with single-use, hashed recovery codes</td>\n<td>help if the authenticator device itself is compromised</td>\n</tr>\n<tr>\n<td><strong>Session cookie</strong></td>\n<td>HttpOnly, Strict, <code>__Host-</code>, three clocks, instant revocation</td>\n<td>stop a stolen cookie being used inside its window — that is what step-up narrows</td>\n</tr>\n<tr>\n<td><strong>Step-up</strong> (<code>stepup.ts</code>)</td>\n<td>requires fresh proof for destructive operations</td>\n<td>protect non-destructive reads; a stolen cookie can still read everything in scope</td>\n</tr>\n<tr>\n<td><strong>CSRF</strong> (<code>csrf.ts</code>)</td>\n<td>rejects unsafe methods that carry a cookie without same-origin provenance</td>\n<td>apply to Bearer clients, which carry no cookie and are exempt by definition</td>\n</tr>\n<tr>\n<td><strong>CORS</strong> (<code>cors.ts</code>)</td>\n<td>exact-match allowlist; no ACAO at all for an unknown origin</td>\n<td>matter to non-browser clients, which ignore CORS entirely</td>\n</tr>\n<tr>\n<td><strong>CSP / headers</strong> (<code>security-headers.ts</code>)</td>\n<td>no inline script, <code>frame-ancestors &#39;none&#39;</code>, HSTS under TLS, <code>no-store</code> on <code>/api</code></td>\n<td>prevent an XSS — it reduces what one can do</td>\n</tr>\n<tr>\n<td><strong>Team scoping</strong> (<code>team-scope.ts</code>)</td>\n<td>filters sessions, projects, caches and presence to the principal&#39;s teams plus machines they own</td>\n<td>apply to routes that do not go through it; new data routes must opt in</td>\n</tr>\n<tr>\n<td><strong>Audit log</strong> (<code>audit.ts</code>)</td>\n<td>append-only, 180-day TTL, secret-shaped fields redacted before write</td>\n<td>prevent anything — it is how you find out</td>\n</tr>\n<tr>\n<td><strong>Resource limits</strong> (<code>limits.ts</code>)</td>\n<td>byte-counted bodies abandoned mid-stream, SSE cap, outbound timeouts</td>\n<td>bound memory used by a legitimate large aggregation</td>\n</tr>\n<tr>\n<td><strong>Error hygiene</strong> (<code>errors.ts</code>)</td>\n<td>generic code + correlation ref to the client</td>\n<td>apply to logs, which keep the full message on purpose</td>\n</tr>\n<tr>\n<td><strong>Container</strong></td>\n<td>uid 10001, read-only rootfs, <code>cap_drop: ALL</code>, no-new-privileges, loopback bind</td>\n<td>protect the app from a compromised host</td>\n</tr>\n</tbody></table>\n<h2 id=\"7-honest-limits\">7. Honest limits</h2>\n<ul>\n<li><strong>The owner role is unbounded by design.</strong> An owner reaches every team and every admin route.\nMFA and step-up raise the cost of using a stolen owner session; they do not cap its authority.</li>\n<li><strong>None of this has had an external audit or a penetration test.</strong> The tests assert that the\ncode does what its author intended. That is a check against anticipated mistakes, not against\nunanticipated ones.</li>\n<li><strong>A public repository does not weaken any of this</strong> — every secret is operator-supplied at\nruntime and none is committed — but it does mean the defaults are read by attackers too, which\nis why they are the conservative ones. See <a href=\"https://github.com/blpsoares/agentistics/blob/main/../SECURITY.md\" target=\"_blank\" rel=\"noopener\">SECURITY.md</a>.</li>\n<li><strong>Configuration is the weakest link.</strong> Most of these controls are switched on by an\nenvironment variable, and OWASP ranks security misconfiguration second among current risks.\nThat is the entire reason <code>agentop doctor --exposed</code> exists and refuses to declare readiness\non a check it could not verify.</li>\n</ul>\n<h2 id=\"8-per-connection-sharing-rules-the-guarantee-stated-precisel\">8. Per-connection sharing rules — the guarantee, stated precisely</h2>\n<p>A member can restrict what each central connection receives, across <strong>two dimensions</strong> —\nrepository (<code>git_remote</code>) and project (<code>project_path</code>) — under one of <strong>two modes</strong>\n(<code>share-rules.ts</code>, <code>team-rules.ts</code>, <code>team-forget-client.ts</code> — see\n<a href=\"https://github.com/blpsoares/agentistics/blob/main/architecture.md#per-connection-repository-sharing\" target=\"_blank\" rel=\"noopener\">architecture.md</a> for how it works):</p>\n<ul>\n<li><strong><code>denylist</code></strong> (&quot;share everything except…&quot;) — the default, and the same behaviour every\nexisting <code>deniedRepos</code> config had before this shipped.</li>\n<li><strong><code>allowlist</code></strong> (&quot;share only…&quot;) — nothing reaches this central unless it matches a listed\nrepo or project.</li>\n</ul>\n<p>The typed rule list (<code>TeamConnection.sources: ShareSource[]</code>, plus <code>shareMode</code>) exists in exactly\nthree places — <code>~/.agentistics/preferences.json</code>, the in-memory <code>TeamConnection</code> on the member, and\nthe browser tab talking to that machine&#39;s own origin — and appears in <strong>no</strong> request body sent to\na central: <code>IngestBody</code> is unchanged, and <code>GET /api/team/status</code> exposes only <code>shareMode</code> and a\nper-dimension <strong>count</strong> (<code>deniedRepos</code>/<code>deniedProjects</code>, or <code>allowedCount</code> in allowlist mode) —\nnever the values, same-origin only.</p>\n<p><strong>What is guaranteed:</strong> a central never learns <em>which</em> repositories or projects are hidden (or\nallowed), nor how many, nor their names, sessions, prompts, titles, models or cost — <strong>provided\nthat data was never pushed to it and had no activity before the attribution boundary.</strong> This holds\nidentically in both modes: allowlist mode does not disclose the <em>complement</em> of what it shares\neither — a central sees only what was let through, never a hint of what else exists.</p>\n<p><strong>Allowlist mode is the safer default to choose for an untrusted central</strong>, specifically because\nof how it treats the unknown: a repository or project that appears on the machine <em>after</em> the rule\nwas set is <strong>hidden</strong> under allowlist (it matches nothing, so it is not shared) but <strong>shared</strong> under\ndenylist (it matches no <em>block</em>, so it goes through). Denylist requires the user to notice and add\nevery new thing they want hidden; allowlist requires them to notice and add every new thing they\nwant shared. For a central the user does not fully trust, the fail-closed direction is the one\nwhere forgetting to update the rules leaks nothing new.</p>\n<p><strong>One thing &quot;share only…&quot; must not be read as promising, and the UI must say so explicitly:\nallowlist mode still ships the prehistory rollup.</strong> Work done at or before Claude&#39;s own\n<code>lastComputedDate</code> summarisation watermark cannot be decomposed by repository or project by\n<em>anyone</em>, including this machine — the consolidate store is a strict subset of what Claude already\nrolled up into <code>stats-cache.json</code>, and there is no per-session record left to filter. That block\ntravels to every connection, allowlist or denylist, as unattributed daily volume (tokens, cost,\nsession/message counts with no repo, no project, no session id, no prompt attached) — exactly as it\ndoes today under a denylist. Choosing &quot;share only project X&quot; narrows everything <em>decomposable</em>, not\nthat rollup; the existing <code>prehistorySessions</code> marker (surfaced in the confirm modal and the read\nview) reports its size so the user can judge how much of their history that covers. A stronger-\nsounding mode name must never imply a stronger guarantee than the attribution boundary allows.</p>\n<p><strong>What is NOT guaranteed, and must be said in the UI — do not present this feature as stronger\nthan this:</strong></p>\n<ol>\n<li><strong>A repo that was already pushed is disclosed by its removal.</strong> The central holds those\ndocuments with <code>git_remote</code>, <code>project_path</code>, <code>first_prompt</code>, <code>title</code>, <code>model</code>, tokens and cost,\nand the forget request names them by id. Deleting data you have already handed over is\ninherently observable — the strong promise above applies only to repos that were <em>never</em>\nshared with that central; the weak one applies to repos that were.</li>\n<li><strong>Work done before the attribution boundary rides inside the prehistory block</strong> as unattributed\ndaily volume — no repo, no project, no session, no prompt attached to it — and <strong>no later rule\ncan withdraw it</strong>, because there is no document left to name.</li>\n<li><strong>The existence of a filter is observable.</strong> A restricted machine&#39;s session documents stop\ncovering its own filtered days, and a scoped delete is visible on the central&#39;s change stream.\nThis is inherent to withholding data; there is no marker field creating it.</li>\n<li><strong>Colluding centrals can reconstruct each other&#39;s denied set.</strong> Two centrals (or one operator\nwith accounts on both) seeing the same machine, with overlapping presence windows and\noverlapping shared-session sets, can take a set difference and recover the other&#39;s rules.\nPer-connection restrictions are confidential against a <em>single</em> central operator, not against\ncollusion between them.</li>\n<li><strong>CI ingest and the OpenTelemetry exporter are outside these rules entirely, in either mode.</strong>\nA connection&#39;s sharing rules are a <em>member push</em> rule; they do not reach <code>agentop ci-push</code> (CI\nsessions are stamped server-side under a different <code>memberId</code>, keyed by repo) or\n<code>otel-watcher.ts</code>&#39;s OTLP export. Blocking (or failing to allowlist) a repo on a member\nconnection does not stop that repo&#39;s GitHub Actions runs or OTel metrics from reaching the same\ncentral by a different path.</li>\n</ol>\n<h3 id=\"81-rules-are-per-machine-and-how-a-machine-finds-out\">8.1 Rules are per machine, and how a machine finds out</h3>\n<p>Sharing rules live on the machine that declares them. Restricting a repository on one laptop does\nnothing on a second laptop signed in to the same account, which will keep pushing it.</p>\n<p>A machine detects that situation <strong>without disclosing anything</strong>. It calls\n<code>GET /api/team/account-repos</code>, which returns the distinct repositories the central holds <em>for the\ncaller&#39;s own account</em> and which of that account&#39;s machines pushed each one. The request names no\nrepository and carries no rule — it is byte-identical whether the caller just restricted something\nor is idly refreshing — and the response is data the account already owns and can already read from\nits dashboard. The comparison against the private rules happens <strong>on the machine</strong>\n(<code>server/account-repos.ts</code>, <code>findStillShared</code>); the central never learns the outcome. The result is\nthe orange banner on the connection card naming the repository and the sibling machine.</p>\n<p>Scope: the route is minted-token-only and scoped to the token&#39;s <strong>owner accounts</strong>\n(<code>listSiblingMachines</code>), never by team and never globally — a token with no owner account sees only\nitself. CI and repo tokens are excluded.</p>\n<h3 id=\"82-the-sealed-envelope-telling-the-other-machines-through-a-\">8.2 The sealed envelope — telling the other machines, through a central that cannot read it</h3>\n<p>§8.1 lets a machine <em>detect</em> the problem. Telling the account&#39;s OTHER machines is the opposite\ndirection, and it cannot be done without something crossing the central. So it crosses encrypted.</p>\n<p><strong>Construction</strong> (<code>envelope-crypto.ts</code>, composed from standard primitives, nothing invented):</p>\n<pre><code>E            = fresh X25519 keypair, one per message\ndh1          = X25519(E.priv,      recipient.pub)     confidentiality + freshness\ndh2          = X25519(sender.priv, recipient.pub)     sender authenticity\nkey          = HKDF-SHA256(ikm = dh1 ‖ dh2, salt = E.pub ‖ recipient.pub, info = header)\nciphertext   = AES-256-GCM(key, random 12-byte iv, aad = the full header)\n</code></pre>\n<p>This is the Noise <code>X</code> / X3DH-style composition. The ephemeral DH means the same rule set never\nseals to the same bytes twice. The static-static DH is the <strong>authenticator</strong>: only a holder of the\nsender&#39;s private key can produce a <code>dh2</code> the recipient reproduces. A signature was rejected\ndeliberately — it would prove authorship to anyone who ever obtained the plaintext, whereas the DH\nauthenticator is verifiable only by the intended recipient, and two machines of one account need no\ntransferable proof of what they told each other. The whole header is the GCM AAD, so the central\ncannot re-address, relabel or re-date an envelope it relays.</p>\n<p><strong>The recipient checks the header it authenticated.</strong> Binding sender, recipient, instance and time\ninto the AAD proves the central cannot <em>rewrite</em> them; it says nothing about the central <em>choosing</em>\nthe routing fields it reports beside the ciphertext. <code>open()</code> therefore requires the caller to state\nthe sender the transport claimed, this machine&#39;s own id, and the connection&#39;s instanceId, and\nrefuses on any disagreement <strong>before</strong> any key agreement. The sender&#39;s pin is looked up by the id\n<strong>inside the seal</strong>, never the one supplied beside it. Without this, a central could publish a\ndirectory entry under a machine id it invented pointing at a real peer&#39;s key, then relay that peer&#39;s\ngenuine envelopes under the invented identity: pin matches, GCM verifies, and the proposal is filed\nas authored by a machine that does not exist under a display name the central chose.</p>\n<p><strong>Replay is refused by memory, not by freshness alone.</strong> Every opened envelope&#39;s digest — SHA-256 of\n<code>ciphertext ‖ tag</code>, never the central&#39;s own envelope id, which it mints and can vary — is persisted\nin the inbox and <strong>is not cleared by dismissing a proposal</strong>. Ignoring a proposal is therefore\npermanent. Without it, a pre-restriction envelope (<code>denylist</code> with no sources = share everything)\ncould be replayed after the sender tightened up, offering the user a one-click downgrade.\n<code>createdAt</code> is additionally bounded (<code>ENVELOPE_FRESH_MS</code>, 7 days, with an hour of clock skew) as the\nbackstop against a central withholding an envelope and delivering it much later. Days rather than\nminutes is deliberate: the mailbox exists <em>because</em> peers are offline, so a minutes-wide window\nwould drop exactly the messages the channel was built to deliver. The card also renders the\nproposal&#39;s age and calls out anything over a day old.</p>\n<p><strong>Key distribution and its honest limit.</strong> Each machine generates its keypair locally and publishes\nonly the public half (<code>POST /api/team/keys</code>, authenticated by its existing minted token). Nothing to\ntype, working the moment a second machine joins — which is what the product required.</p>\n<blockquote>\n<p><strong>A central that publishes a public key it controls, under any machine id, reads that channel.</strong>\nThis is not a narrow first-sight race. The central does not need to <em>substitute</em> an existing key:\nit can simply <strong>invent a machine</strong> at any time under a key it holds. A peer that never existed has\nno prior key to contradict, so trust-on-first-use accepts it, and from that moment every\nrestriction message is encrypted to the central as well as to the real siblings. No fully\nautomatic scheme closes this: two parties whose only channel is the adversary cannot bootstrap a\nsecret without a pre-shared secret or out-of-band verification.</p>\n</blockquote>\n<p>What is done instead — the point of every item below is that trust can be established\nautomatically, but never <strong>silently</strong>:</p>\n<ul>\n<li><strong>Pin on first sight.</strong> The first time B sees A&#39;s key it stores it (<code>envelope-keys.ts</code>). If it\never changes, B <strong>refuses to decrypt</strong> — it does not guess between a reinstall and an attack —\nraises a red alarm on the connection card and a <code>member.peer_key_changed</code> notification, and\n<strong>leaves the envelope on the central</strong> so resolving the key does not cost the message. A sender\nlikewise refuses to seal <em>to</em> a changed key. The pin is per connection: the same machine id on two\ncentrals is two different machines.</li>\n<li><strong>A sender must be in the directory.</strong> An envelope from a machine the key directory did not just\nlist is refused outright, pinned or not. This closes the cheaper twin of the fabricated-peer\nattack: rather than <em>publishing</em> a peer (which is announced, below), a central can <em>omit</em> one and\nseal under an id it invented — no directory entry means no pin, and an unpinned sender would\notherwise skip the pin comparison entirely and be filed as an apply-ready proposal with no\nnotification at all. There is no legitimate race, because a sender publishes its own key before\nit deposits.</li>\n<li><strong>Announce every new pin.</strong> The first time a peer is pinned — including a fabricated one — the\nconnection card and a <code>member.peer_pinned</code> notification name it: &quot;a new machine of your account\nwill now receive your sharing rules&quot;. Same alarm class as a changed key. Silent\ntrust-establishment <em>is</em> the exposure, so this is the mitigation for the limit above, not a\nnicety.</li>\n<li><strong>Show the fingerprints.</strong> The expanded connection card lists this machine&#39;s own fingerprint and\nevery pinned peer&#39;s, so a user who cares can compare two machines they own. Never required.</li>\n<li><strong>One bad key cannot disable the channel.</strong> A directory entry whose key cannot be used is skipped\nand counted, not thrown — an unguarded <code>seal()</code> in the peer loop would have let a central publish\none junk key and silently stop every sibling from ever being told anything.</li>\n</ul>\n<p><strong>What the central inevitably learns, and this is not implied away:</strong> that a machine deposited a\nsealed envelope, when, for whom, and how big it was. Since the channel carries only rule changes,\n&quot;an envelope exists&quot; ≈ &quot;that machine changed its rules&quot; — which the scoped delete\n(<code>POST /api/team/forget</code>) arriving at the same instant already reveals, so it concedes nothing new.\nIt does <strong>not</strong> reveal which repository, in which direction, or whether the peer acted on it.</p>\n<p><strong>Propose, never apply.</strong> A decrypted message NEVER changes the receiving machine&#39;s rules. It is\nstored as a proposal (<code>envelope-inbox.ts</code>), raises a notification, and waits for an explicit click\nthat runs the ordinary <code>PATCH /api/team/connections/:id</code> — the same validated path a hand-edited\nrule takes. There is no apply endpoint anywhere on the server, and <code>envelope-client.test.ts</code> asserts\nthat the inbox module exposes no such function: a machine that silently reconfigures another\nbecause a message arrived would be a remote-control channel, and this is not one.</p>\n<p><strong>One keypair per machine, not per central.</strong> A machine publishes the same public key to every\ncentral it connects to, so two centrals comparing notes can confirm they are looking at the same\nphysical machine. That is accepted rather than fixed: the machine already presents the same\n<code>git_remote</code> set and the same statsCache shape to both, so per-connection keys would not make it\nunlinkable, only harder to reason about. An envelope still cannot cross centrals — the instanceId is\nbound into the AAD and re-checked on arrival.</p>\n<p><strong>Mailbox scoping.</strong> Deposit/fetch/ack are minted-token-only. The SENDER is stamped from the token,\nnever read from the body; the RECIPIENT must be a machine of the caller&#39;s own account\n(<code>allowedRecipients</code>), so the mailbox is not a write primitive against strangers; a refused\nrecipient is silently skipped rather than named, because naming it would answer &quot;does this machine\nbelong to my account&quot;. Retention is bounded by age (in step with the recipient&#39;s freshness window) and per-recipient\ncount, and revoking a machine&#39;s token drops its published key and all of its mail. The private key\nnever leaves the machine, never enters a log, an audit event or any response body.</p>\n<p><strong>Rotating a token is a change of identity, and is treated as one.</strong> <code>memberId = sha256(token)</code>, so\nrotation renames the machine in every collection keyed by that id. <code>rotateToken</code> carries the\nhistory across — sessions, memberStats, workflows, the tags pinned to the machine, and the\npublished envelope key — and the enumeration of what is keyed by a machine id lives in\n<code>rotate-identity.ts</code>. Two things it deliberately does <strong>not</strong> do:</p>\n<ul>\n<li><strong>It never re-addresses a sealed envelope.</strong> The whole header is the GCM AAD and <code>open</code> compares\nevery routing field against what the transport claimed. Mail addressed to the old id therefore\nyields <code>recipient_mismatch</code> no matter who relays it, so it is <strong>deleted</strong> rather than left to\nexpire (the audit event reports the count — it is a loss, not a move). Mail <em>sent</em> by the old id\nstill opens exactly as sealed and is left untouched; re-stamping its sender would turn a true,\ndeliverable announcement into <code>sender_mismatch</code>. The loss is bounded: every message is a full\nsnapshot that its sender re-announces on its next rules change, and facts already collected live\nin the machine&#39;s own inbox and survive.</li>\n<li><strong>It never carries a sibling&#39;s pin across.</strong> To a sibling the rotated machine is a machine it has\nnever seen: it pins on first sight and <strong>announces</strong> it, exactly as above. Continuity cannot be\nestablished here without a claim the central could forge — a &quot;formerly <code>&lt;oldId&gt;</code>&quot; field is a\ncentral assertion by construction, and &quot;the key is the same, so the machine is the same&quot; is no\nbetter, because a public key is public: a central can list an invented machine carrying a key it\ncopied from a real one, and treating a familiar key as proof of continuity would let it suppress\nthe very announcement that defends against fabricated peers. A sound proof exists in principle\n(the old private key signing the new id) but rotation is initiated on the central and the machine\nlearns its new id only afterwards, so it cannot sign it in advance. The rotation dialog says the\nsiblings will see a new machine instead of implying they will not.\n<code>GET /api/team/proposals</code> returns a sibling&#39;s full source list, so it is registered in\n<code>capability-guard.ts</code> and is unreachable on an internet-exposed instance.</li>\n</ul>\n<h2 id=\"9-verifying-it-yourself\">9. Verifying it yourself</h2>\n<p>Each control has tests next to it; these are the ones worth reading first:</p>\n<table>\n<thead>\n<tr>\n<th>Question</th>\n<th>Test</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Can a route become public by accident?</td>\n<td><code>authz-gate.test.ts</code> — asserts the exact <code>AUTH_PUBLIC</code> set</td>\n</tr>\n<tr>\n<td>Can a low-privilege account see another team?</td>\n<td><code>authz-gate.test.ts</code> → <em>data scoping (BOLA)</em></td>\n</tr>\n<tr>\n<td>Is the TOTP implementation real?</td>\n<td><code>totp.test.ts</code> — RFC 6238 published vectors</td>\n</tr>\n<tr>\n<td>Can one signed token be replayed as another?</td>\n<td><code>auth-principal.test.ts</code>, <code>stepup.test.ts</code></td>\n</tr>\n<tr>\n<td>Does a bad exposure value fail open?</td>\n<td><code>exposure.test.ts</code></td>\n</tr>\n<tr>\n<td>Is the lockout a DoS against a colleague?</td>\n<td><code>rate-limit.test.ts</code></td>\n</tr>\n</tbody></table>\n<pre><code class=\"language-bash\">bun test                    # the whole suite\nagentop doctor --exposed    # the deployment&#39;s own state\n</code></pre>\n<p>And end to end, against a running instance: the checklist at the end of\n<a href=\"https://github.com/blpsoares/agentistics/blob/main/exposure.md\" target=\"_blank\" rel=\"noopener\">exposure.md</a>.</p>\n",
    "headings": [
      {
        "id": "security-model",
        "text": "Security model",
        "level": 1
      },
      {
        "id": "1-what-is-being-protected",
        "text": "1. What is being protected",
        "level": 2
      },
      {
        "id": "2-threat-model",
        "text": "2. Threat model",
        "level": 2
      },
      {
        "id": "3-trust-boundaries",
        "text": "3. Trust boundaries",
        "level": 2
      },
      {
        "id": "4-the-request-pipeline",
        "text": "4. The request pipeline",
        "level": 2
      },
      {
        "id": "5-identity-and-sessions",
        "text": "5. Identity and sessions",
        "level": 2
      },
      {
        "id": "6-the-controls-and-what-each-one-does-not-do",
        "text": "6. The controls, and what each one does not do",
        "level": 2
      },
      {
        "id": "7-honest-limits",
        "text": "7. Honest limits",
        "level": 2
      },
      {
        "id": "8-per-connection-sharing-rules-the-guarantee-stated-precisel",
        "text": "8. Per-connection sharing rules — the guarantee, stated precisely",
        "level": 2
      },
      {
        "id": "81-rules-are-per-machine-and-how-a-machine-finds-out",
        "text": "8.1 Rules are per machine, and how a machine finds out",
        "level": 3
      },
      {
        "id": "82-the-sealed-envelope-telling-the-other-machines-through-a-",
        "text": "8.2 The sealed envelope — telling the other machines, through a central that cannot read it",
        "level": 3
      },
      {
        "id": "9-verifying-it-yourself",
        "text": "9. Verifying it yourself",
        "level": 2
      }
    ]
  },
  "exposure": {
    "id": "exposure",
    "file": "docs/exposure.md",
    "title": "Exposing a central",
    "group": "Security",
    "html": "<h1 id=\"exposing-a-central-on-the-internet\">Exposing a central on the internet</h1>\n<p>How to publish an Agentistics central so named people sign in with their own e-mail and\npassword, without handing the internet a shell on the host.</p>\n<p>Read this in order. The last section is a checklist you must be able to tick before opening the\ntunnel; <code>agentop doctor --exposed</code> checks most of it for you.</p>\n<p>This document is the <em>how</em>. For the <em>why</em> — threat model, trust boundaries, the request pipeline\nand what each control does not do — see <strong><a href=\"https://github.com/blpsoares/agentistics/blob/main/security.md\" target=\"_blank\" rel=\"noopener\">security.md</a></strong>.</p>\n<hr>\n<h2 id=\"1-topology\">1. Topology</h2>\n<pre><code>        internet\n           │  HTTPS\n    ┌──────▼───────┐\n    │  Cloudflare  │   WAF, DDoS, rate limiting, (optionally) Access\n    └──────┬───────┘\n           │  outbound-only tunnel, established BY the host\n    ┌──────▼──────────────────────────────────┐\n    │ host                                     │\n    │   cloudflared ──► 127.0.0.1:48080        │\n    │                     agentistics central  │\n    │                     mongo (compose net)  │\n    └──────────────────────────────────────────┘\n</code></pre>\n<p>Three properties matter and each one is load-bearing:</p>\n<ul>\n<li><strong>No inbound port is opened.</strong> <code>cloudflared</code> dials out; the firewall stays closed.</li>\n<li><strong>The app binds <code>127.0.0.1</code>.</strong> That is the compose default now. Binding wider adds a way in\nthat bypasses every control at the edge.</li>\n<li><strong>The database is never routed through the tunnel.</strong> Mongo is not published to the host at\nall; only the app container reaches it over the compose network.</li>\n</ul>\n<h2 id=\"2-the-exposure-profile\">2. The exposure profile</h2>\n<p>One setting decides what the instance is allowed to do:</p>\n<table>\n<thead>\n<tr>\n<th><code>AGENTISTICS_EXPOSURE</code></th>\n<th>Meaning</th>\n<th>Local shell / chat / transcripts / MCP admin</th>\n<th>Owner MFA</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>unset (non-central) → <code>local</code></td>\n<td>solo machine on 127.0.0.1</td>\n<td>available</td>\n<td>not required</td>\n</tr>\n<tr>\n<td>unset (central) → <code>lan</code></td>\n<td>trusted network</td>\n<td>off unless <code>AGENTISTICS_ALLOW_LOCAL_SHELL=1</code></td>\n<td>not required</td>\n</tr>\n<tr>\n<td><code>public</code></td>\n<td>published on the internet</td>\n<td><strong>permanently off</strong></td>\n<td><strong>required</strong></td>\n</tr>\n</tbody></table>\n<p><code>public</code> ignores <code>AGENTISTICS_ALLOW_LOCAL_SHELL</code> — there is no opt-in for arbitrary shell on an\ninstance strangers can reach. An unrecognised value resolves to <code>public</code>, so a typo fails closed.</p>\n<p>What <code>public</code> revokes, and why each one matters:</p>\n<ul>\n<li><code>POST /api/exec</code> — ran <code>bash -c</code> with the caller&#39;s string, reachable by any authenticated\naccount including a plain <code>member</code>.</li>\n<li><code>POST /api/chat-tty</code> — spawned the local coding-assistant CLI and rewrote <code>~/.claude.json</code>.</li>\n<li><code>GET /api/{claude,codex,gemini,copilot,nay}-sessions</code> — returned the <strong>central host&#39;s own raw\ntranscripts</strong>; team scoping does not cover them.</li>\n<li><code>POST /api/mcp-action</code>, <code>GET /api/mcp-list</code> — edit the host&#39;s MCP registration.</li>\n</ul>\n<p>They answer <code>403 {&quot;error&quot;:&quot;capability_disabled&quot;}</code> <strong>before</strong> the auth gate runs, so an exposed\ninstance does not even reveal whether the caller is authenticated.</p>\n<h2 id=\"3-configuration\">3. Configuration</h2>\n<p>In <code>central.env</code>:</p>\n<pre><code class=\"language-sh\">AGENTISTICS_TEAM_CENTRAL=1\nAGENTISTICS_EXPOSURE=public\nAGENTISTICS_TEAM_TLS=1              # Secure + __Host- cookie prefix, and HSTS\nAGENTISTICS_TRUST_PROXY=1           # believe CF-Connecting-IP (ONLY behind the tunnel)\nAGENTISTICS_TEAM_SESSION_SECRET=    # openssl rand -hex 32\nBIND_IP=127.0.0.1\nAPP_PORT=48080\n</code></pre>\n<p>Do <strong>not</strong> set <code>AGENTISTICS_ALLOW_LOCAL_SHELL</code>. Do not set <code>AGENTISTICS_CENTRAL_USER</code> unless you\ngenuinely want this machine&#39;s own usage in the dashboard — with it, compose mounts the host&#39;s\n<code>~/.claude</code>, <code>~/.codex</code>, <code>~/.gemini</code> and <code>~/.copilot</code> read-only into the container, which means\nan exposed instance is holding every raw transcript on the machine. Without it there is no host\nfilesystem access at all.</p>\n<p>Two notes on the secrets:</p>\n<ul>\n<li><strong><code>AGENTISTICS_TEAM_SESSION_SECRET</code> no longer falls back to the dashboard password.</strong> It used\nto, which meant a leaked or shared password was enough to forge a session cookie for any\naccount. Setting it to the password now refuses to boot. Leave it empty and the central\ngenerates a random secret and persists it in Mongo.</li>\n<li><code>AGENTISTICS_TRUST_PROXY=1</code> is only correct when the tunnel is the <em>only</em> way in. If the app\nis also reachable directly, a client can pick its own rate-limit bucket by sending the header.</li>\n</ul>\n<p>Then <code>chmod 600 central.env</code> — it holds secrets.</p>\n<h2 id=\"4-bringing-it-up\">4. Bringing it up</h2>\n<p>From a repo checkout:</p>\n<pre><code class=\"language-sh\">./central.sh up          # builds, migrates the data volume, (re)creates the containers\n./central.sh status      # containers + health\n./central.sh logs        # follow the app log\n</code></pre>\n<p>From the standalone binary, with no checkout (materialises a compose + <code>central.env</code> under\n<code>~/.agentistics/central/</code> and pulls the published image):</p>\n<pre><code class=\"language-sh\">agentop central up\nagentop central status\nagentop central logs\n</code></pre>\n<p>The first boot with no owner account prints a <strong>one-time setup token</strong> to the log. Capture it —\nit is shown once:</p>\n<pre><code class=\"language-sh\">./central.sh logs | grep -A 6 &quot;OWNER SETUP REQUIRED&quot;\n</code></pre>\n<p>Then check the deployment before anything is reachable from outside:</p>\n<pre><code class=\"language-sh\">./central.sh doctor --exposed\n</code></pre>\n<p>Run it through <code>central.sh</code> rather than as bare <code>agentop doctor</code>: inside the container\n<code>central.env</code> is the live environment <strong>and</strong> MongoDB is reachable, so the owner-MFA and\nmachine-token checks actually run. On the host, <code>agentop doctor --exposed</code> still reads\n<code>central.env</code> (it says which file it used) but cannot reach the database, so those two checks\nreport as unverified — which counts as a failure, deliberately.</p>\n<p>Nothing above opens a port. The container binds <code>127.0.0.1</code>, so at this point the central is\nreachable only from the host itself. The tunnel below is what publishes it.</p>\n<h2 id=\"5-the-tunnel\">5. The tunnel</h2>\n<pre><code class=\"language-sh\">cloudflared tunnel login\ncloudflared tunnel create agentistics\n</code></pre>\n<p><code>~/.cloudflared/config.yml</code>:</p>\n<pre><code class=\"language-yaml\">tunnel: &lt;TUNNEL-UUID&gt;\ncredentials-file: /home/&lt;you&gt;/.cloudflared/&lt;TUNNEL-UUID&gt;.json\ningress:\n  - hostname: metrics.example.com\n    service: http://127.0.0.1:48080\n  - service: http_status:404\n</code></pre>\n<pre><code class=\"language-sh\">cloudflared tunnel route dns agentistics metrics.example.com\nsudo cloudflared service install     # run it as a service, not in a shell\n</code></pre>\n<p>Rotate the tunnel token periodically. If it leaks, delete the tunnel and create a new one — that\ninvalidates the old credentials immediately.</p>\n<h2 id=\"6-at-the-edge-cloudflare\">6. At the edge (Cloudflare)</h2>\n<p>The in-app limiter is the backstop, not the front line. Configure:</p>\n<ul>\n<li><strong>Rate limiting</strong> on <code>/api/iam/login</code>, <code>/api/iam/login/mfa</code> and <code>/api/team/login</code> — 10 requests\nper minute per IP is ample for humans.</li>\n<li><strong>Managed WAF rules</strong> and <strong>bot fight mode</strong> on.</li>\n<li>Optionally <strong>Cloudflare Access</strong> in front of the dashboard, as a second and independent\nauthentication layer (deny-by-default, before the login page is ever reached).</li>\n</ul>\n<p><strong>If you enable Access, mind the machine traffic.</strong> <code>POST /api/team/ingest</code> and the\n<code>/api/team/agent</code> WebSocket are members and CI runners, not browsers — they cannot complete an\nAccess login. Either bypass those paths, give them a service token, or (cleanest) run a second\nhostname pointed at a separate instance with <code>AGENTISTICS_INGEST_ONLY=1</code> sharing the same Mongo:\nthat instance serves only the token-gated ingest endpoint and 404s everything else.</p>\n<h2 id=\"7-onboarding-a-person\">7. Onboarding a person</h2>\n<ol>\n<li>On first boot with no owner, the central prints a one-time setup token. Use it once to create\nthe owner account.</li>\n<li>The owner enrols TOTP immediately — on a <code>public</code> profile an owner without a second factor can\nreach only the enrolment and identity routes, and nothing else.</li>\n<li>The owner creates each account with <code>mustChangePassword</code>, and sends the temporary password out\nof band (not in the same channel as the URL).</li>\n<li>The person signs in, is forced to set a password (minimum 12 characters, no common passwords,\nnothing containing their own name or e-mail), and enrols their own second factor from the\naccount menu → <strong>Two-factor</strong>.</li>\n</ol>\n<p>Roles: <code>owner</code> reaches everything; a team <code>manager</code> manages their own teams&#39; machines, members\nand <code>user</code> accounts; a plain <code>user</code> sees only the teams they belong to plus machines they own.\nThat scoping is enforced server-side in <code>team-scope.ts</code> and asserted in <code>authz-gate.test.ts</code>.</p>\n<h2 id=\"8-incident-response\">8. Incident response</h2>\n<table>\n<thead>\n<tr>\n<th>Situation</th>\n<th>Action</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>A session may be compromised</td>\n<td>Change that account&#39;s password, or have an owner delete/recreate it — both bump <code>sessionVersion</code>, which invalidates every existing cookie for that account instantly.</td>\n</tr>\n<tr>\n<td>A machine token leaked</td>\n<td>Revoke it in Settings → Team. Tokens are stored only as sha256 hashes and are individually revocable; the member auto-resets to solo on a persistent 401.</td>\n</tr>\n<tr>\n<td>The session secret leaked</td>\n<td>Set a new <code>AGENTISTICS_TEAM_SESSION_SECRET</code> and restart. Every session everywhere is invalidated.</td>\n</tr>\n<tr>\n<td>Something looks wrong</td>\n<td><code>GET /api/iam/audit</code> (owner only) — logins, failures, lockouts, MFA events, password changes, account/team/token changes and every gate denial, kept 180 days.</td>\n</tr>\n<tr>\n<td>The tunnel token leaked</td>\n<td>Delete the tunnel, create a new one.</td>\n</tr>\n</tbody></table>\n<h2 id=\"9-go-live-checklist\">9. Go-live checklist</h2>\n<p>Run <code>./central.sh doctor --exposed</code> (or <code>agentop central doctor --exposed</code>). It must print\nno <code>✗</code>.</p>\n<p>Then verify from outside, against the real hostname:</p>\n<pre><code class=\"language-sh\">H=https://metrics.example.com\n\ncurl -s -o /dev/null -w &#39;%{http_code}\\n&#39; -X POST $H/api/exec \\\n  -H &#39;Content-Type: application/json&#39; -d &#39;{&quot;command&quot;:&quot;id&quot;}&#39;      # 403\ncurl -s -o /dev/null -w &#39;%{http_code}\\n&#39; $H/api/claude-sessions   # 403\ncurl -s -o /dev/null -w &#39;%{http_code}\\n&#39; $H/api/data              # 401\n\nfor i in $(seq 1 7); do                                           # 401×5, then 429\n  curl -s -o /dev/null -w &quot;$i: %{http_code}\\n&quot; -X POST $H/api/iam/login \\\n    -H &#39;Content-Type: application/json&#39; -d &#39;{&quot;email&quot;:&quot;a@b.c&quot;,&quot;password&quot;:&quot;x&quot;}&#39;\ndone\n\ncurl -sI $H/ | grep -iE &#39;content-security-policy|strict-transport|x-frame|referrer|permissions&#39;\ncurl -sI -H &#39;Origin: https://evil.tld&#39; $H/api/health | grep -i access-control-allow-origin  # empty\n</code></pre>\n<p>And on the host:</p>\n<pre><code class=\"language-sh\">ss -ltnp | grep 48080                      # 127.0.0.1, never 0.0.0.0\ndocker compose -p team-mode exec app id    # uid=10001, not root\ndocker compose -p team-mode exec app touch /x   # Read-only file system\n</code></pre>\n<p>In the browser, on the real hostname:</p>\n<ul>\n<li>The dashboard loads with <strong>zero</strong> CSP violations in the console. A violation means an inline\nscript slipped in — fix the source, never widen the policy.</li>\n<li>The session cookie is named <code>__Host-agentistics_session</code> and shows <code>Secure</code>, <code>HttpOnly</code>,\n<code>SameSite=Strict</code>.</li>\n<li>Signing in as an owner asks for a TOTP code.</li>\n<li>Two accounts in different teams see different data on the dashboard.</li>\n</ul>\n",
    "headings": [
      {
        "id": "exposing-a-central-on-the-internet",
        "text": "Exposing a central on the internet",
        "level": 1
      },
      {
        "id": "1-topology",
        "text": "1. Topology",
        "level": 2
      },
      {
        "id": "2-the-exposure-profile",
        "text": "2. The exposure profile",
        "level": 2
      },
      {
        "id": "3-configuration",
        "text": "3. Configuration",
        "level": 2
      },
      {
        "id": "4-bringing-it-up",
        "text": "4. Bringing it up",
        "level": 2
      },
      {
        "id": "5-the-tunnel",
        "text": "5. The tunnel",
        "level": 2
      },
      {
        "id": "6-at-the-edge-cloudflare",
        "text": "6. At the edge (Cloudflare)",
        "level": 2
      },
      {
        "id": "7-onboarding-a-person",
        "text": "7. Onboarding a person",
        "level": 2
      },
      {
        "id": "8-incident-response",
        "text": "8. Incident response",
        "level": 2
      },
      {
        "id": "9-go-live-checklist",
        "text": "9. Go-live checklist",
        "level": 2
      }
    ]
  },
  "mcp": {
    "id": "mcp",
    "file": "docs/mcp.md",
    "title": "MCP server",
    "group": "Integrations",
    "html": "<h1 id=\"agentistics-mcp-server\">agentistics MCP Server</h1>\n<p>The agentistics MCP server exposes your usage analytics as tools that any MCP-compatible client can call — including Claude Code, Nay (the built-in chat), and any third-party agent that supports MCP.</p>\n<h2 id=\"what-is-mcp\">What is MCP?</h2>\n<p><a href=\"https://modelcontextprotocol.io/\" target=\"_blank\" rel=\"noopener\">Model Context Protocol (MCP)</a> is an open standard that lets AI models call structured tools defined by external servers. The agentistics MCP server translates the <code>/api/data</code> response into a set of typed tools so that AI agents can query your usage data programmatically.</p>\n<h2 id=\"starting-the-mcp-server\">Starting the MCP server</h2>\n<p>The MCP server runs as a stdio process. It is <strong>not</strong> a separate HTTP server — it communicates via stdin/stdout with the MCP client (Claude Code, etc.) and makes HTTP calls internally to the agentistics API.</p>\n<pre><code class=\"language-bash\"># agentistics must be running first (provides /api/data)\nagentop server\n\n# Then register the MCP (done automatically on first server start, but you can do it manually):\nclaude mcp add -s user agentistics \\\n  -e AGENTISTICS_API=http://localhost:47291 \\\n  -- bun run /path/to/agentistics/packages/mcp/agentistics-mcp.ts\n</code></pre>\n<p>The agentistics server registers the MCP automatically at startup via <code>claude mcp add -s user</code>. If the registration already exists with the correct URL, it is skipped.</p>\n<h3 id=\"verify-registration\">Verify registration</h3>\n<pre><code class=\"language-bash\">claude mcp list\n# Should show: agentistics  bun run .../mcp/agentistics-mcp.ts\n</code></pre>\n<h2 id=\"environment-variables\">Environment variables</h2>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>AGENTISTICS_API</code></td>\n<td><code>http://localhost:47291</code></td>\n<td>Base URL of the agentistics API server</td>\n</tr>\n</tbody></table>\n<h2 id=\"multi-harness-scope\">Multi-harness scope</h2>\n<p>agentistics tracks multiple coding harnesses (Claude Code, Codex CLI, Gemini CLI, Copilot CLI). The MCP server reads the same <code>/api/data</code> endpoint, so by default its numbers reflect the <strong>unified (all-harness)</strong> view.</p>\n<ul>\n<li><strong>Per-harness filtering:</strong> <code>agentistics_summary</code>, <code>agentistics_projects</code>, <code>agentistics_sessions</code>, and <code>agentistics_costs</code> accept an optional <code>harness</code> parameter (<code>claude</code> | <code>codex</code> | <code>gemini</code> | <code>copilot</code> | <code>antigravity</code>, or <code>all</code> — the default). When set, the tool scopes its result to that harness only.</li>\n<li><strong>Comparison:</strong> <code>agentistics_harnesses</code> lists every harness present in the data with its sessions, messages, tokens, estimated cost, and last-active date — the quickest way to answer &quot;which harness do I use most / costs most&quot;.</li>\n<li><strong>Caveats</strong> match the dashboard: <code>currentStreak</code> is Claude-only (returned as <code>null</code> when scoping to a single non-Claude harness); harnesses that don&#39;t emit token usage (e.g. Gemini&#39;s local files) contribute 0 tokens/cost; the unified/<code>claude</code> cost breakdown comes from the Claude-complete stats-cache, while a specific non-Claude harness&#39;s cost breakdown is aggregated per-model from its sessions.</li>\n</ul>\n<h2 id=\"available-tools\">Available tools</h2>\n<h3 id=\"agentistics_summary\">`agentistics_summary`</h3>\n<p>All-time totals aggregated from sessions. Token counts and cost are computed directly from session records (not from the stats-cache snapshot), so they are always accurate even if the cache is stale.</p>\n<p><strong>Parameters:</strong> <code>harness</code> <em>(optional)</em> — <code>claude</code> | <code>codex</code> | <code>gemini</code> | <code>copilot</code> | <code>antigravity</code> | <code>all</code> (default <code>all</code>).</p>\n<p><strong>Returns:</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;harness&quot;: &quot;all&quot;,\n  &quot;totalInputTokens&quot;: 12500000,\n  &quot;totalOutputTokens&quot;: 890000,\n  &quot;totalCacheReadTokens&quot;: 45000000,\n  &quot;totalCacheWriteTokens&quot;: 1200000,\n  &quot;estimatedCostUSD&quot;: 18.42,\n  &quot;totalSessions&quot;: 142,\n  &quot;topModel&quot;: &quot;claude-opus-4-8&quot;,\n  &quot;topProject&quot;: &quot;my-app&quot;,\n  &quot;activeDays&quot;: 38,\n  &quot;currentStreak&quot;: 7\n}\n</code></pre>\n<p><code>currentStreak</code> is <code>null</code> when scoped to a single non-Claude harness (streak is Claude-only).</p>\n<hr>\n<h3 id=\"agentistics_harnesses\">`agentistics_harnesses`</h3>\n<p>Side-by-side comparison of every harness present in the data, sorted by total tokens. No parameters.</p>\n<p><strong>Returns:</strong></p>\n<pre><code class=\"language-json\">[\n  {\n    &quot;harness&quot;: &quot;claude&quot;,\n    &quot;sessions&quot;: 157,\n    &quot;messages&quot;: 38664,\n    &quot;inputTokens&quot;: 2467133,\n    &quot;outputTokens&quot;: 29339498,\n    &quot;cacheReadTokens&quot;: 5981874127,\n    &quot;cacheWriteTokens&quot;: 208186404,\n    &quot;totalTokens&quot;: 6221867162,\n    &quot;estimatedCostUSD&quot;: 3031.51,\n    &quot;lastActive&quot;: &quot;2026-06-29T13:52:24.823Z&quot;\n  },\n  { &quot;harness&quot;: &quot;codex&quot;, &quot;sessions&quot;: 20, &quot;estimatedCostUSD&quot;: 0.20, &quot;...&quot;: &quot;...&quot; }\n]\n</code></pre>\n<hr>\n<h3 id=\"agentistics_projects\">`agentistics_projects`</h3>\n<p>Per-project breakdown with aggregated token and cost data, sorted by total tokens descending.</p>\n<p><strong>Parameters:</strong> <code>harness</code> <em>(optional)</em> — scope to one harness, or <code>all</code> (default). When scoped, projects with no sessions in that harness are omitted and <code>sessions</code> reflects that harness&#39;s count.</p>\n<p><strong>Returns:</strong> array of projects</p>\n<pre><code class=\"language-json\">[\n  {\n    &quot;name&quot;: &quot;my-app&quot;,\n    &quot;path&quot;: &quot;/home/user/projects/my-app&quot;,\n    &quot;sessions&quot;: 34,\n    &quot;messages&quot;: 820,\n    &quot;inputTokens&quot;: 3200000,\n    &quot;outputTokens&quot;: 220000,\n    &quot;totalTokens&quot;: 3420000,\n    &quot;estimatedCostUSD&quot;: 5.21,\n    &quot;lastActive&quot;: &quot;2025-01-15T14:30:00Z&quot;,\n    &quot;languages&quot;: [&quot;TypeScript&quot;, &quot;CSS&quot;]\n  }\n]\n</code></pre>\n<blockquote>\n<p><strong>Note:</strong> Token/cost data is aggregated from session records grouped by <code>project_path</code>. Projects are matched by exact path.</p>\n</blockquote>\n<hr>\n<h3 id=\"agentistics_sessions\">`agentistics_sessions`</h3>\n<p>Recent sessions with duration, model, and cost.</p>\n<p><strong>Parameters:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Name</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>limit</code></td>\n<td>number</td>\n<td>20</td>\n<td>Max sessions to return (1–50)</td>\n</tr>\n<tr>\n<td><code>harness</code></td>\n<td>string</td>\n<td><code>all</code></td>\n<td>Scope to one harness (<code>claude</code> | <code>codex</code> | <code>gemini</code> | <code>copilot</code> | <code>antigravity</code>)</td>\n</tr>\n</tbody></table>\n<p><strong>Returns:</strong> array of sessions sorted by start time descending (each row includes its <code>harness</code>)</p>\n<pre><code class=\"language-json\">[\n  {\n    &quot;id&quot;: &quot;abc123&quot;,\n    &quot;harness&quot;: &quot;claude&quot;,\n    &quot;project&quot;: &quot;/home/user/projects/my-app&quot;,\n    &quot;startedAt&quot;: &quot;2025-01-15T14:30:00Z&quot;,\n    &quot;durationMinutes&quot;: 47,\n    &quot;messages&quot;: 34,\n    &quot;inputTokens&quot;: 85000,\n    &quot;outputTokens&quot;: 6200,\n    &quot;cacheReadTokens&quot;: 310000,\n    &quot;cacheWriteTokens&quot;: 12000,\n    &quot;totalTokens&quot;: 413200,\n    &quot;estimatedCostUSD&quot;: 0.348,\n    &quot;model&quot;: &quot;claude-sonnet-4-6&quot;\n  }\n]\n</code></pre>\n<hr>\n<h3 id=\"agentistics_costs\">`agentistics_costs`</h3>\n<p>Model pricing breakdown and cache analysis.</p>\n<p><strong>Parameters:</strong> <code>harness</code> <em>(optional)</em> — <code>all</code>/<code>claude</code> (default) uses the Claude-complete stats-cache; a specific non-Claude harness is aggregated per-model from its sessions.</p>\n<p><strong>Returns:</strong> array of models sorted by total tokens descending</p>\n<pre><code class=\"language-json\">[\n  {\n    &quot;model&quot;: &quot;claude-sonnet-4-6&quot;,\n    &quot;inputTokens&quot;: 8200000,\n    &quot;outputTokens&quot;: 610000,\n    &quot;cacheReadTokens&quot;: 31000000,\n    &quot;cacheWriteTokens&quot;: 1200000,\n    &quot;totalTokens&quot;: 41010000,\n    &quot;estimatedCostUSD&quot;: 12.80\n  }\n]\n</code></pre>\n<hr>\n<h3 id=\"agentistics_component_catalog\">`agentistics_component_catalog`</h3>\n<p>Lists all dashboard components available for placement on the custom <code>/custom</code> page. <strong>Always call this before building a layout.</strong></p>\n<p><strong>Parameters:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Name</th>\n<th>Type</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>category</code></td>\n<td>string</td>\n<td>Optional filter: <code>kpi</code>, <code>activity</code>, <code>costs</code>, <code>projects</code>, <code>tools</code>, <code>sessions</code></td>\n</tr>\n</tbody></table>\n<p><strong>Returns:</strong> array of components with grid sizes and descriptions</p>\n<pre><code class=\"language-json\">[\n  {\n    &quot;id&quot;: &quot;kpi.cost&quot;,\n    &quot;label&quot;: &quot;Estimated cost&quot;,\n    &quot;category&quot;: &quot;kpi&quot;,\n    &quot;defaultW&quot;: 3,\n    &quot;defaultH&quot;: 3,\n    &quot;minW&quot;: 2,\n    &quot;minH&quot;: 2,\n    &quot;description&quot;: &quot;Estimated USD cost&quot;\n  },\n  {\n    &quot;id&quot;: &quot;activity.chart&quot;,\n    &quot;label&quot;: &quot;Activity chart (full)&quot;,\n    &quot;category&quot;: &quot;activity&quot;,\n    &quot;defaultW&quot;: 8,\n    &quot;defaultH&quot;: 7,\n    &quot;minW&quot;: 4,\n    &quot;minH&quot;: 4,\n    &quot;description&quot;: &quot;Full activity chart with all metrics&quot;\n  }\n]\n</code></pre>\n<hr>\n<h3 id=\"agentistics_get_layouts\">`agentistics_get_layouts`</h3>\n<p>Returns all saved custom layouts and which one is currently active.</p>\n<p><strong>Returns:</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;activeLayout&quot;: &quot;overview&quot;,\n  &quot;layouts&quot;: [\n    {\n      &quot;name&quot;: &quot;overview&quot;,\n      &quot;isActive&quot;: true,\n      &quot;componentCount&quot;: 5,\n      &quot;components&quot;: [\n        { &quot;instanceId&quot;: &quot;1&quot;, &quot;componentId&quot;: &quot;kpi.cost&quot;, &quot;x&quot;: 0, &quot;y&quot;: 0, &quot;w&quot;: 3, &quot;h&quot;: 3 }\n      ]\n    }\n  ]\n}\n</code></pre>\n<hr>\n<h3 id=\"agentistics_build_layout\">`agentistics_build_layout`</h3>\n<p>Creates a complete layout in one call: creates it, adds all requested components with auto-positioning, and optionally activates it. Ideal for building a themed dashboard from scratch.</p>\n<p>Components are positioned using first-fit shelf packing on a 12-column grid. After placement, any component with empty space to its right (no right neighbour in its row range) is extended to fill the full row width.</p>\n<p><strong>Parameters:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>name</code></td>\n<td>string</td>\n<td>yes</td>\n<td>Layout name</td>\n</tr>\n<tr>\n<td><code>componentIds</code></td>\n<td>string[]</td>\n<td>yes</td>\n<td>Ordered list of component IDs from the catalog</td>\n</tr>\n<tr>\n<td><code>activate</code></td>\n<td>boolean</td>\n<td>no</td>\n<td>Set as active layout after creating (default: true)</td>\n</tr>\n</tbody></table>\n<p><strong>Grid sizing guidelines (from the catalog defaults):</strong></p>\n<ul>\n<li>KPI cards (<code>kpi.*</code>): w=3, h=3 — 4 per row</li>\n<li>Wide charts (<code>activity.chart</code>, <code>tools.*</code>, <code>sessions.*</code>): w=8–12, h=6–8</li>\n<li>Medium panels (<code>costs.budget</code>, <code>costs.cache</code>, <code>activity.heatmap</code>): w=6, h=7</li>\n<li>Full-width (<code>costs.models</code>, <code>sessions.highlights</code>): w=12, h=6–8</li>\n<li>Projects: <code>projects.top</code> w=7, <code>projects.languages</code> w=5</li>\n</ul>\n<p>Order <code>componentIds</code> thoughtfully: KPI cards first, then charts, then tables.</p>\n<hr>\n<h3 id=\"agentistics_add_component\">`agentistics_add_component`</h3>\n<p>Adds a single component to an existing layout. Auto-positioned after existing items unless <code>x</code>/<code>y</code> are specified.</p>\n<p><strong>Parameters:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>layoutName</code></td>\n<td>string</td>\n<td>no</td>\n<td>Target layout (defaults to active layout)</td>\n</tr>\n<tr>\n<td><code>componentId</code></td>\n<td>string</td>\n<td>yes</td>\n<td>Component ID from the catalog</td>\n</tr>\n<tr>\n<td><code>x</code></td>\n<td>number</td>\n<td>no</td>\n<td>Column 0–11 (auto-placed if omitted)</td>\n</tr>\n<tr>\n<td><code>y</code></td>\n<td>number</td>\n<td>no</td>\n<td>Row (auto-placed if omitted)</td>\n</tr>\n<tr>\n<td><code>w</code></td>\n<td>number</td>\n<td>no</td>\n<td>Width in grid columns (defaults to catalog default)</td>\n</tr>\n<tr>\n<td><code>h</code></td>\n<td>number</td>\n<td>no</td>\n<td>Height in grid rows (defaults to catalog default)</td>\n</tr>\n</tbody></table>\n<hr>\n<h3 id=\"agentistics_remove_component\">`agentistics_remove_component`</h3>\n<p>Removes a component by its instance ID from a layout. Get instance IDs from <code>agentistics_get_layouts</code>.</p>\n<p><strong>Parameters:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>layoutName</code></td>\n<td>string</td>\n<td>no</td>\n<td>Layout name (defaults to active layout)</td>\n</tr>\n<tr>\n<td><code>itemId</code></td>\n<td>string</td>\n<td>yes</td>\n<td>Instance ID of the item to remove</td>\n</tr>\n</tbody></table>\n<hr>\n<h3 id=\"agentistics_create_layout\">`agentistics_create_layout`</h3>\n<p>Creates a new empty named layout without adding any components.</p>\n<p><strong>Parameters:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>name</code></td>\n<td>string</td>\n<td>yes</td>\n<td>Layout name</td>\n</tr>\n</tbody></table>\n<hr>\n<h3 id=\"agentistics_set_active_layout\">`agentistics_set_active_layout`</h3>\n<p>Switches the <code>/custom</code> page to display a different layout.</p>\n<p><strong>Parameters:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>name</code></td>\n<td>string</td>\n<td>yes</td>\n<td>Layout name to activate</td>\n</tr>\n</tbody></table>\n<hr>\n<h3 id=\"agentistics_delete_layout\">`agentistics_delete_layout`</h3>\n<p>Permanently deletes a layout. Cannot be undone. Cannot delete the last remaining layout.</p>\n<p><strong>Parameters:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Name</th>\n<th>Type</th>\n<th>Required</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>name</code></td>\n<td>string</td>\n<td>yes</td>\n<td>Layout name to delete</td>\n</tr>\n</tbody></table>\n<hr>\n<h3 id=\"agentistics_export_pdf\">`agentistics_export_pdf`</h3>\n<p>Generates a PDF report download link. Returns a <code>[⬇ Download PDF](pdf:URL)</code> link that the Nay chat renders as a styled download button. Clicking it opens the PDF export modal pre-configured with the requested date range.</p>\n<p><strong>Parameters:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Name</th>\n<th>Type</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>range</code></td>\n<td>string</td>\n<td><code>&quot;all&quot;</code></td>\n<td>Date range: <code>&quot;7d&quot;</code>, <code>&quot;30d&quot;</code>, <code>&quot;90d&quot;</code>, or <code>&quot;all&quot;</code></td>\n</tr>\n</tbody></table>\n<p><strong>Returns:</strong> a markdown button link</p>\n<pre><code>[⬇ Download PDF — last 30d](pdf:http://localhost:47292/?export=pdf&amp;range=30d)\n</code></pre>\n<p>The Nay chat detects the <code>pdf:</code> protocol and renders it as an orange download button. Clicking opens the PDF export modal where you can review and download the report.</p>\n<hr>\n<h2 id=\"using-the-mcp-from-claude-code\">Using the MCP from Claude Code</h2>\n<p>Once registered, you can invoke agentistics tools directly from any Claude Code session:</p>\n<pre><code># In a Claude Code chat (not Nay), you can ask:\n&quot;What&#39;s my total spend so far this month according to agentistics?&quot;\n&quot;Build me a layout in agentistics with cost KPIs and the activity chart&quot;\n&quot;Generate a PDF of my last 30 days usage&quot;\n</code></pre>\n<p>Claude Code will automatically use the registered MCP tools. No explicit configuration needed per-project — the registration is at user scope (<code>~/.claude.json</code>).</p>\n<h2 id=\"using-the-mcp-from-a-custom-agent\">Using the MCP from a custom agent</h2>\n<pre><code class=\"language-typescript\">// Example: calling agentistics tools from a Claude agent\nimport Anthropic from &#39;@anthropic-ai/sdk&#39;\n\nconst client = new Anthropic()\n\nconst response = await client.beta.messages.create({\n  model: &#39;claude-sonnet-4-6&#39;,\n  max_tokens: 1024,\n  tools: [/* your agentistics MCP tools */],\n  messages: [{ role: &#39;user&#39;, content: &#39;What project spent the most tokens?&#39; }],\n})\n</code></pre>\n<h2 id=\"mcp-server-implementation\">MCP server implementation</h2>\n<p>The server lives at <code>packages/mcp/agentistics-mcp.ts</code>. It uses the <code>@modelcontextprotocol/sdk</code> to expose tools over stdio, fetching data from the agentistics HTTP API (<code>AGENTISTICS_API</code>).</p>\n<p>Key design decisions:</p>\n<ul>\n<li><strong>No direct file access</strong> — all data goes through the agentistics API so the same parsing/aggregation logic applies everywhere</li>\n<li><strong>Cost calculation</strong> — the MCP has its own inline <code>calcCostUSD</code> that mirrors <code>src/lib/types.ts</code> to avoid bundling the frontend module; totals in <code>agentistics_summary</code> are aggregated from sessions (not from the stats-cache snapshot) for accuracy</li>\n<li><strong>Auto-position algorithm</strong> — <code>agentistics_build_layout</code> uses first-fit shelf packing on a 12-column grid, then <code>fillGaps</code> extends items that have empty space to their right (no right neighbour in the same row range)</li>\n<li><strong>PDF links</strong> — <code>agentistics_export_pdf</code> returns a <code>[label](pdf:URL)</code> markdown link; the Nay chat component detects the <code>pdf:</code> protocol and renders it as a download button</li>\n</ul>\n<h2 id=\"see-also\">See also</h2>\n<ul>\n<li><a href=\"https://github.com/blpsoares/agentistics/blob/main/nay.md\" target=\"_blank\" rel=\"noopener\">Nay chat</a> — how the built-in AI assistant uses these tools</li>\n<li><a href=\"https://github.com/blpsoares/agentistics/blob/main/data-sources.md\" target=\"_blank\" rel=\"noopener\">Data sources</a> — what <code>/api/data</code> returns and how it&#39;s computed</li>\n</ul>\n",
    "headings": [
      {
        "id": "agentistics-mcp-server",
        "text": "agentistics MCP Server",
        "level": 1
      },
      {
        "id": "what-is-mcp",
        "text": "What is MCP?",
        "level": 2
      },
      {
        "id": "starting-the-mcp-server",
        "text": "Starting the MCP server",
        "level": 2
      },
      {
        "id": "verify-registration",
        "text": "Verify registration",
        "level": 3
      },
      {
        "id": "environment-variables",
        "text": "Environment variables",
        "level": 2
      },
      {
        "id": "multi-harness-scope",
        "text": "Multi-harness scope",
        "level": 2
      },
      {
        "id": "available-tools",
        "text": "Available tools",
        "level": 2
      },
      {
        "id": "agentistics_summary",
        "text": "`agentistics_summary`",
        "level": 3
      },
      {
        "id": "agentistics_harnesses",
        "text": "`agentistics_harnesses`",
        "level": 3
      },
      {
        "id": "agentistics_projects",
        "text": "`agentistics_projects`",
        "level": 3
      },
      {
        "id": "agentistics_sessions",
        "text": "`agentistics_sessions`",
        "level": 3
      },
      {
        "id": "agentistics_costs",
        "text": "`agentistics_costs`",
        "level": 3
      },
      {
        "id": "agentistics_component_catalog",
        "text": "`agentistics_component_catalog`",
        "level": 3
      },
      {
        "id": "agentistics_get_layouts",
        "text": "`agentistics_get_layouts`",
        "level": 3
      },
      {
        "id": "agentistics_build_layout",
        "text": "`agentistics_build_layout`",
        "level": 3
      },
      {
        "id": "agentistics_add_component",
        "text": "`agentistics_add_component`",
        "level": 3
      },
      {
        "id": "agentistics_remove_component",
        "text": "`agentistics_remove_component`",
        "level": 3
      },
      {
        "id": "agentistics_create_layout",
        "text": "`agentistics_create_layout`",
        "level": 3
      },
      {
        "id": "agentistics_set_active_layout",
        "text": "`agentistics_set_active_layout`",
        "level": 3
      },
      {
        "id": "agentistics_delete_layout",
        "text": "`agentistics_delete_layout`",
        "level": 3
      },
      {
        "id": "agentistics_export_pdf",
        "text": "`agentistics_export_pdf`",
        "level": 3
      },
      {
        "id": "using-the-mcp-from-claude-code",
        "text": "Using the MCP from Claude Code",
        "level": 2
      },
      {
        "id": "using-the-mcp-from-a-custom-agent",
        "text": "Using the MCP from a custom agent",
        "level": 2
      },
      {
        "id": "mcp-server-implementation",
        "text": "MCP server implementation",
        "level": 2
      },
      {
        "id": "see-also",
        "text": "See also",
        "level": 2
      }
    ]
  },
  "nay": {
    "id": "nay",
    "file": "docs/nay.md",
    "title": "Nay chat",
    "group": "Integrations",
    "html": "<h1 id=\"nay-ai-chat-assistant\">Nay — AI Chat Assistant</h1>\n<p>Nay is an AI chat assistant built into the agentistics dashboard. It connects directly to your usage data via MCP tools and can answer questions about your spending, projects, sessions, and layouts without you having to leave the dashboard.</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<p>Nay runs as a floating chat panel (bottom-right corner of any page). When you send a message, the dashboard calls Claude Code CLI (<code>claude --print</code>) in a sandboxed workspace at <code>~/.agentistics/nay-chat/</code>. Claude has access to 13 MCP tools that talk directly to the agentistics API, so every answer is backed by your real data.</p>\n<pre><code>Your message\n  → /api/chat-tty (POST)\n    → claude --print --output-format stream-json\n      → agentistics MCP tools → /api/data, /api/rates, etc.\n    → streamed JSON events (text chunks + tool calls)\n  → rendered in the chat panel\n</code></pre>\n<h2 id=\"requirements\">Requirements</h2>\n<ul>\n<li><strong>Claude Code CLI</strong> installed and authenticated (<code>claude --version</code>)</li>\n<li><strong>agentistics server running</strong> — Nay calls MCP tools that talk to the local API</li>\n<li><strong>Claude subscription</strong> — Nay uses your Claude Code session quota (see <a href=\"#subscription-and-quota-usage\">usage warning</a> below)</li>\n</ul>\n<h2 id=\"subscription-and-quota-usage\">Subscription and quota usage</h2>\n<blockquote>\n<p><strong>Important:</strong> Every Nay conversation counts against your Claude Code subscription usage.</p>\n<p>Nay runs <code>claude --print</code> under the hood, which is the same Claude Code process used for coding. Each message sends your conversation history + tool results to the API, and this is billed/counted exactly like a regular Claude Code session.</p>\n<ul>\n<li><strong>Claude Max / Pro subscribers</strong>: usage comes out of your monthly session quota</li>\n<li><strong>API key users</strong>: each message is billed at standard Claude API rates for the selected model</li>\n</ul>\n<p>Prefer <strong>Haiku 4.5</strong> for quick data queries (cheapest). Use <strong>Sonnet 4.6</strong> for analysis and layout building. <strong>Opus 4.7</strong> for complex multi-step reasoning.</p>\n</blockquote>\n<h2 id=\"model-selection\">Model selection</h2>\n<p>The first time you open Nay, a model picker screen appears. You can change the model at any time by starting a new conversation (clear chat → model picker reappears).</p>\n<table>\n<thead>\n<tr>\n<th>Model</th>\n<th>Speed</th>\n<th>Best for</th>\n<th>Input / Output</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Haiku 4.5</td>\n<td>Fastest</td>\n<td>Quick data lookups</td>\n<td>$0.80 / $4.00 per 1M</td>\n</tr>\n<tr>\n<td>Sonnet 4.6</td>\n<td>Balanced</td>\n<td>Analysis, layout building</td>\n<td>$3.00 / $15.00 per 1M</td>\n</tr>\n<tr>\n<td>Opus 4.7</td>\n<td>Most capable</td>\n<td>Complex reasoning</td>\n<td>$15.00 / $75.00 per 1M</td>\n</tr>\n</tbody></table>\n<h2 id=\"nays-identity\">Nay's identity</h2>\n<p>Nay presents herself as <strong>Nay</strong>, the agentistics analytics assistant — not as &quot;Claude&quot; or &quot;an AI by Anthropic&quot;. When asked &quot;who are you?&quot;, she introduces herself as:</p>\n<blockquote>\n<p><em>Nay — assistente de analytics integrada ao agentistics. Analiso uso do Claude Code: custos, tokens, sessões, projetos e métricas de produtividade.</em></p>\n</blockquote>\n<p>This is enforced via the <code>CLAUDE.md</code> written to <code>~/.agentistics/nay-chat/</code> on every server start.</p>\n<h2 id=\"what-nay-can-answer\">What Nay can answer</h2>\n<table>\n<thead>\n<tr>\n<th>Question</th>\n<th>What it calls</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;How much did I spend this month?&quot;</td>\n<td><code>agentistics_summary</code>, <code>agentistics_costs</code></td>\n</tr>\n<tr>\n<td>&quot;Which project cost the most?&quot;</td>\n<td><code>agentistics_projects</code></td>\n</tr>\n<tr>\n<td>&quot;What were my most expensive sessions?&quot;</td>\n<td><code>agentistics_sessions</code></td>\n</tr>\n<tr>\n<td>&quot;Build me a cost overview layout&quot;</td>\n<td><code>agentistics_component_catalog</code>, <code>agentistics_build_layout</code></td>\n</tr>\n<tr>\n<td>&quot;Show me my cache hit rate&quot;</td>\n<td><code>agentistics_summary</code></td>\n</tr>\n<tr>\n<td>&quot;Generate a PDF of my last 30 days&quot;</td>\n<td><code>agentistics_export_pdf</code></td>\n</tr>\n<tr>\n<td>&quot;How much have I spent talking to you?&quot;</td>\n<td><code>agentistics_projects</code> filtered to <code>nay-chat</code></td>\n</tr>\n</tbody></table>\n<h3 id=\"how-much-have-i-spent-talking-to-you\">\"How much have I spent talking to you?\"</h3>\n<p>When the user asks about the cost of conversations with Nay specifically, Nay calls <code>agentistics_projects</code> and filters to the project at path <code>~/.agentistics/nay-chat</code>. This is where Nay&#39;s own sessions are stored and tracked.</p>\n<p>For general Claude Code usage across all projects, Nay uses <code>agentistics_summary</code> or the full project list.</p>\n<h2 id=\"pdf-report-generation\">PDF report generation</h2>\n<p>Nay can generate a PDF report through a conversational flow:</p>\n<ol>\n<li>User asks: &quot;Generate a PDF&quot; or &quot;Export a report&quot;</li>\n<li>Nay asks for the date range if not specified: &quot;Qual período? 7 dias, 30 dias, 90 dias, ou tudo?&quot;</li>\n<li>User answers (e.g., &quot;30 days&quot;)</li>\n<li>Nay calls <code>agentistics_export_pdf</code> with <code>range: &quot;30d&quot;</code></li>\n<li>A styled <strong>Download PDF</strong> button appears in the chat</li>\n</ol>\n<p>The button uses the <code>pdf:URL</code> link protocol, which the Nay chat renders as an orange download button. Clicking it opens the PDF export modal pre-configured with the requested settings.</p>\n<h2 id=\"navigation-buttons\">Navigation buttons</h2>\n<p>Nay ends data responses with a navigation button that links to the relevant dashboard page. If the response is about a specific project, the link includes a <code>?projects=...</code> filter parameter.</p>\n<p>Examples:</p>\n<ul>\n<li><code>→ Ver custos</code> → <code>/costs</code></li>\n<li><code>→ Ver projetos</code> → <code>/projects?projects=/home/user/my-project</code></li>\n<li><code>→ Abrir layout</code> → <code>/custom</code></li>\n</ul>\n<p>These are rendered as purple inline buttons in the chat.</p>\n<h2 id=\"terminal-commands\">Terminal commands</h2>\n<p>You can also run shell commands directly from Nay:</p>\n<pre><code>/run ls -la ~/.claude/\n/bash git log --oneline -5\n/sh df -h\n</code></pre>\n<p>Code blocks in Nay responses with bash/shell language tags include a <strong>Run</strong> button to execute the command inline.</p>\n<h2 id=\"floating-window-detach\">Floating window (detach)</h2>\n<p>Nay can be detached from the side panel into a free-floating window:</p>\n<ul>\n<li>Click the <strong>⧉</strong> (ExternalLink) icon in the Nay panel header to detach</li>\n<li>The floating window can be dragged and resized</li>\n<li>Click <strong>−</strong> (Minus) in the floating header to minimize</li>\n<li>When minimized, a small <strong>Nay mini FAB</strong> appears above the main corner button — click it to restore the floating window</li>\n<li>Click the re-attach icon to dock Nay back into the panel</li>\n</ul>\n<h3 id=\"fab-layout-when-nay-is-detached\">FAB layout when Nay is detached</h3>\n<table>\n<thead>\n<tr>\n<th>State</th>\n<th>Corner button</th>\n<th>Mini FAB</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Floating, visible</td>\n<td>Shows ⧉ icon (opens panel for Claude)</td>\n<td>—</td>\n</tr>\n<tr>\n<td>Floating, minimized</td>\n<td>Shows ⧉ icon (opens panel for Claude)</td>\n<td>Nay logo circle, above corner button</td>\n</tr>\n</tbody></table>\n<h2 id=\"workspace-setup\">Workspace setup</h2>\n<p>On every server start, <code>ensureNayChat()</code> writes two files to <code>~/.agentistics/nay-chat/</code>:</p>\n<table>\n<thead>\n<tr>\n<th>File</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>CLAUDE.md</code></td>\n<td>Instructions for Nay: identity, tool call protocol, PDF generation flow, &quot;talking to me&quot; context, response format, navigation buttons</td>\n</tr>\n<tr>\n<td><code>.claude/settings.json</code></td>\n<td>MCP server registration + permissions (allows all 13 agentistics tools without prompting)</td>\n</tr>\n</tbody></table>\n<p>It also registers the agentistics MCP at user scope via <code>claude mcp add -s user</code> so that <code>claude --print</code> mode can find the tools. This registration is idempotent — it skips if the URL is already correct.</p>\n<h2 id=\"behavior-rules-enforced-via-claudemd\">Behavior rules (enforced via CLAUDE.md)</h2>\n<ol>\n<li><strong>Identity</strong> — always presents as Nay, not as Claude or a generic AI</li>\n<li><strong>Never answer from memory</strong> — calls tools for every question, even follow-ups</li>\n<li><strong>Never describe what it&#39;s about to do</strong> — calls tools immediately, no &quot;Let me check...&quot;</li>\n<li><strong>Never reference &quot;the Nay agent&quot;</strong> — it has direct tool access, uses it</li>\n<li><strong>Navigation button only when data was fetched</strong> — no button for conversational replies</li>\n<li><strong>&quot;Talking to me&quot; = nay-chat project</strong> — cost queries about Nay specifically filter to <code>~/.agentistics/nay-chat</code></li>\n<li><strong>PDF flow</strong> — asks for date range before calling <code>agentistics_export_pdf</code></li>\n</ol>\n<h2 id=\"see-also\">See also</h2>\n<ul>\n<li><a href=\"https://github.com/blpsoares/agentistics/blob/main/mcp.md\" target=\"_blank\" rel=\"noopener\">MCP tools reference</a> — full list of tools Nay uses</li>\n<li><a href=\"https://github.com/blpsoares/agentistics/blob/main/architecture.md\" target=\"_blank\" rel=\"noopener\">Architecture</a> — how <code>chat-tty.ts</code> streams Claude output</li>\n</ul>\n",
    "headings": [
      {
        "id": "nay-ai-chat-assistant",
        "text": "Nay — AI Chat Assistant",
        "level": 1
      },
      {
        "id": "how-it-works",
        "text": "How it works",
        "level": 2
      },
      {
        "id": "requirements",
        "text": "Requirements",
        "level": 2
      },
      {
        "id": "subscription-and-quota-usage",
        "text": "Subscription and quota usage",
        "level": 2
      },
      {
        "id": "model-selection",
        "text": "Model selection",
        "level": 2
      },
      {
        "id": "nays-identity",
        "text": "Nay's identity",
        "level": 2
      },
      {
        "id": "what-nay-can-answer",
        "text": "What Nay can answer",
        "level": 2
      },
      {
        "id": "how-much-have-i-spent-talking-to-you",
        "text": "\"How much have I spent talking to you?\"",
        "level": 3
      },
      {
        "id": "pdf-report-generation",
        "text": "PDF report generation",
        "level": 2
      },
      {
        "id": "navigation-buttons",
        "text": "Navigation buttons",
        "level": 2
      },
      {
        "id": "terminal-commands",
        "text": "Terminal commands",
        "level": 2
      },
      {
        "id": "floating-window-detach",
        "text": "Floating window (detach)",
        "level": 2
      },
      {
        "id": "fab-layout-when-nay-is-detached",
        "text": "FAB layout when Nay is detached",
        "level": 3
      },
      {
        "id": "workspace-setup",
        "text": "Workspace setup",
        "level": 2
      },
      {
        "id": "behavior-rules-enforced-via-claudemd",
        "text": "Behavior rules (enforced via CLAUDE.md)",
        "level": 2
      },
      {
        "id": "see-also",
        "text": "See also",
        "level": 2
      }
    ]
  },
  "opentelemetry": {
    "id": "opentelemetry",
    "file": "docs/opentelemetry.md",
    "title": "OpenTelemetry",
    "group": "Integrations",
    "html": "<h1 id=\"opentelemetry-integration\">OpenTelemetry Integration</h1>\n<p>agentistics can export usage metrics via <a href=\"https://opentelemetry.io/\" target=\"_blank\" rel=\"noopener\">OpenTelemetry</a> (OTLP/HTTP), enabling integration with Grafana, Datadog, New Relic, Honeycomb, and any OTLP-compatible collector.</p>\n<h2 id=\"how-to-enable\">How to enable</h2>\n<p>Set <code>OTEL_EXPORTER_OTLP_ENDPOINT</code> before starting:</p>\n<pre><code class=\"language-bash\"># Binary\nOTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318 agentop server\n\n# From source\nOTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318 bun run dev\n</code></pre>\n<p>The OTel daemon starts automatically as part of <code>agentop server</code>. To run it standalone (headless, no web server):</p>\n<pre><code class=\"language-bash\">OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318 agentop watch\n</code></pre>\n<h2 id=\"environment-variables\">Environment variables</h2>\n<table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Default</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>OTEL_EXPORTER_OTLP_ENDPOINT</code></td>\n<td><em>(none — export disabled)</em></td>\n<td>OTLP collector endpoint</td>\n</tr>\n<tr>\n<td><code>OTEL_EXPORTER_OTLP_HEADERS</code></td>\n<td><em>(none)</em></td>\n<td>Extra headers, e.g. <code>Authorization=Bearer token</code></td>\n</tr>\n<tr>\n<td><code>OTEL_SERVICE_NAME</code></td>\n<td><code>agentistics</code></td>\n<td>Service name reported in metrics</td>\n</tr>\n<tr>\n<td><code>CLAUDE_STATS_WATCH_INTERVAL</code></td>\n<td><code>30</code></td>\n<td>Polling interval in seconds (minimum: 5)</td>\n</tr>\n</tbody></table>\n<h2 id=\"how-it-works\">How it works</h2>\n<p>The watcher (<code>server/otel-watcher.ts</code>):</p>\n<ol>\n<li><strong>Watches</strong> <code>~/.claude/usage-data/session-meta/</code> and <code>~/.claude/projects/</code> via chokidar</li>\n<li><strong>Rebuilds</strong> a metrics snapshot on every file change (debounced 2s, serialized — no concurrent rebuilds)</li>\n<li><strong>Polls</strong> every <code>CLAUDE_STATS_WATCH_INTERVAL</code> seconds as a fallback for missed events</li>\n<li><strong>Exports</strong> all metrics to the OTLP endpoint when it is configured</li>\n</ol>\n<h2 id=\"exported-metrics\">Exported metrics</h2>\n<p>All metrics use the <code>claude_stats</code> namespace:</p>\n<table>\n<thead>\n<tr>\n<th>Metric</th>\n<th>Type</th>\n<th>Unit</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>claude_stats.messages.total</code></td>\n<td>Counter</td>\n<td>messages</td>\n<td>Total messages</td>\n</tr>\n<tr>\n<td><code>claude_stats.sessions.total</code></td>\n<td>Counter</td>\n<td>sessions</td>\n<td>Total sessions</td>\n</tr>\n<tr>\n<td><code>claude_stats.tool_calls.total</code></td>\n<td>Counter</td>\n<td>calls</td>\n<td>Total tool calls</td>\n</tr>\n<tr>\n<td><code>claude_stats.tokens.input</code></td>\n<td>Counter</td>\n<td>tokens</td>\n<td>Total input tokens</td>\n</tr>\n<tr>\n<td><code>claude_stats.tokens.output</code></td>\n<td>Counter</td>\n<td>tokens</td>\n<td>Total output tokens</td>\n</tr>\n<tr>\n<td><code>claude_stats.cost.usd</code></td>\n<td>Counter</td>\n<td>USD</td>\n<td>Estimated total cost</td>\n</tr>\n<tr>\n<td><code>claude_stats.git.commits</code></td>\n<td>Counter</td>\n<td>commits</td>\n<td>Git commits via AI</td>\n</tr>\n<tr>\n<td><code>claude_stats.git.lines_added</code></td>\n<td>Counter</td>\n<td>lines</td>\n<td>Lines added</td>\n</tr>\n<tr>\n<td><code>claude_stats.git.lines_removed</code></td>\n<td>Counter</td>\n<td>lines</td>\n<td>Lines removed</td>\n</tr>\n<tr>\n<td><code>claude_stats.git.files_modified</code></td>\n<td>Counter</td>\n<td>files</td>\n<td>Files modified</td>\n</tr>\n<tr>\n<td><code>claude_stats.streak</code></td>\n<td>Gauge</td>\n<td>days</td>\n<td>Current streak</td>\n</tr>\n<tr>\n<td><code>claude_stats.longest_session</code></td>\n<td>Gauge</td>\n<td>min</td>\n<td>Longest session duration</td>\n</tr>\n<tr>\n<td><code>claude_stats.active_projects</code></td>\n<td>Gauge</td>\n<td>projects</td>\n<td>Number of active projects</td>\n</tr>\n<tr>\n<td><code>claude_stats.tokens.by_model.input</code></td>\n<td>Counter</td>\n<td>tokens</td>\n<td>Input tokens per model</td>\n</tr>\n<tr>\n<td><code>claude_stats.tokens.by_model.output</code></td>\n<td>Counter</td>\n<td>tokens</td>\n<td>Output tokens per model</td>\n</tr>\n<tr>\n<td><code>claude_stats.tool_calls.by_tool</code></td>\n<td>Counter</td>\n<td>calls</td>\n<td>Tool calls per tool</td>\n</tr>\n</tbody></table>\n<h2 id=\"example-grafana-otel-collector\">Example: Grafana + OTel Collector</h2>\n<pre><code class=\"language-yaml\"># docker-compose.yml\nservices:\n  otel-collector:\n    image: otel/opentelemetry-collector-contrib:latest\n    ports:\n      - &quot;4318:4318&quot;   # OTLP HTTP\n      - &quot;8889:8889&quot;   # Prometheus scrape\n    volumes:\n      - ./otel-config.yaml:/etc/otelcol-contrib/config.yaml\n\n  grafana:\n    image: grafana/grafana:latest\n    ports:\n      - &quot;3000:3000&quot;\n    environment:\n      - GF_AUTH_ANONYMOUS_ENABLED=true\n</code></pre>\n<pre><code class=\"language-yaml\"># otel-config.yaml\nreceivers:\n  otlp:\n    protocols:\n      http:\n        endpoint: 0.0.0.0:4318\n\nexporters:\n  prometheus:\n    endpoint: &quot;0.0.0.0:8889&quot;\n\nservice:\n  pipelines:\n    metrics:\n      receivers: [otlp]\n      exporters: [prometheus]\n</code></pre>\n<p>Then add a Prometheus data source in Grafana pointing to <code>http://otel-collector:8889</code> and create dashboards using the <code>claude_stats.*</code> metrics.</p>\n<p>A pre-built Grafana dashboard JSON is available in <code>grafana/</code> in the repository root.</p>\n<h2 id=\"grafana-dashboard\">Grafana dashboard</h2>\n<p>The <code>grafana/</code> folder contains a ready-to-import dashboard with:</p>\n<ul>\n<li>Total cost over time</li>\n<li>Token breakdown by model</li>\n<li>Session count and streak gauge</li>\n<li>Tool call ranking</li>\n<li>Git activity</li>\n</ul>\n<p>Import it via Grafana → Dashboards → Import → Upload JSON.</p>\n",
    "headings": [
      {
        "id": "opentelemetry-integration",
        "text": "OpenTelemetry Integration",
        "level": 1
      },
      {
        "id": "how-to-enable",
        "text": "How to enable",
        "level": 2
      },
      {
        "id": "environment-variables",
        "text": "Environment variables",
        "level": 2
      },
      {
        "id": "how-it-works",
        "text": "How it works",
        "level": 2
      },
      {
        "id": "exported-metrics",
        "text": "Exported metrics",
        "level": 2
      },
      {
        "id": "example-grafana-otel-collector",
        "text": "Example: Grafana + OTel Collector",
        "level": 2
      },
      {
        "id": "grafana-dashboard",
        "text": "Grafana dashboard",
        "level": 2
      }
    ]
  }
}
